---
project: WIRE-DESKTOP-ELECTRON-MODERNIZATION
updated: 2026-09-22
milestone: M4
active_work_item: PKG-001
state: audit-remediation-in-progress
current_goal: close-mandatory-unsigned-review-handoff
integration_branch: integration/electron-modernization
integration_head_commit: 897e3930392fdc9479f9641c8c03f116947d4e95
upstream_commit: 6f9b6a994500f0fc0ad64e60882ac9f5b099d5f2
fork_url: https://github.com/adamlow-wire/wire-desktop
active_branch: fix/PKG-001-fail-closed-builds-2026-09-15
next_work_item: TST-006
blockers: [fork-publication-approval, profile-compatibility-decision]
---

# Current project status

## Current execution

**PKG-001 configuration download candidate:** baseline `4e7f98b2` records one passing successful-download control and four failing request/stream/invalid-archive/collision targets against the installed copy-config2.3.13 downloader. F-016 confirms unhandled failures leave its promise unsettled, corrupt archives remain, and a pre-existing archive can be overwritten. The local Yarn patch awaits request and stream pipeline completion, returns fixed errors, removes only an archive it opened exclusively, and destroys response streams. All five Node cases pass; restoring upstream source fails the same four targets and restored patch passes again. Fixtures use inert axios responses, owned temp files and actual JSZip/filesystem streams; no real configuration download or GUI executes.

Root/bin/Playwright types, forced scoped lint, immutable installation and fixture syntax pass. All356 Node tooling tests pass with the patch installed. Evidence `/tmp/pkg001-config-download-{baseline,patched,sensitivity,restored,install,immutable,types,lint,bin}.log`. The initial patch install could not write the sandboxed global Yarn cache; an authorized cache-write retry installed it successfully. The exact patch is tracked in `.yarn/patches/@wireapp-copy-config-npm-2.3.13-d9dcaa56d1.patch`. ELC-003's axios1.20 override must be composed with this separate patch and revalidated; this branch still retains its older dependency graph. Next: characterize macOS updater diagnostic failures under existing F-007/INV-010. Publication/profile decisions and final composed platform gates remain open.

**ELC-003 Joi/disposition follow-up:** independent branch `fix/ELC-003-protocol-runtime-2026-09-22` is at `abae1a66`. Baseline/disposition `156b601f` precedes migration from deprecated `@hapi/joi`17.1.1 to `joi`17.13.8, with only four imports changed and schema/options preserved. All40 Node profile/control/event cases,80 renderer tests, root/bin/Playwright/Electron test types, scoped lint, immutable installation and production bundle pass. Unknown-property sensitivity fails3 control/9 renderer tests before restoration. Fresh production npm audit at2026-09-22T16:19:33Z has354 instances and only uuid9.0.1 moderate; its affected v3/v5/v6 buffer APIs are unused by the two argument-free v4 callers, recorded as not applicable to current calls. No high/critical matches remain; this does not close shipped-artifact or holistic dependency review. Logs and source/advisory references are in that branch's review ledger and `/tmp/elc003-joi-*`. Next independent work: updater diagnostic and copy-config failure-propagation characterization, then remaining provenance/coverage and actual artifact qualification. Publication/profile decisions remain pending; no native launch, push or merge occurred.

**ELC-003 axios follow-up:** isolated branch `fix/ELC-003-protocol-runtime-2026-09-22` is now at `7e11946f`. Direct axios is tooling-only and patched from0.21.2 to maintained0.34.0; copy-config's separate1.13.3 resolves to1.20.0. Baselines `569c96af`/`08fea784` protect loopback transport and actual GitHub/Hockey/Ibis/config methods with inert external adapters. All five focused cases,323 tooling tests, root/bin/Playwright/Electron test types, scoped lint, immutable install and production bundle pass. Restoring old config merging or dropping Ibis auth fails intended targets, followed by restoration. Fresh npm production audit at2026-09-22T16:12:25Z finds354 package instances, no axios and only Joi low/uuid moderate matches; no high/critical matches. Both actual tooling axios versions separately audit with zero matches. This is not a whole-development-graph or ASAR qualification claim. Next: assess Joi/uuid reachability and deprecated-package requirements, characterize remaining updater/config-download diagnostic/failure leads, then reconcile and qualify the composed candidate. See isolated branch ledger and `/tmp/elc003-axios-*` evidence. No push/merge/deployment or native execution occurred; publication/profile decisions remain pending.

**ELC-003 updater follow-up:** isolated branch `fix/ELC-003-protocol-runtime-2026-09-22` is now at `16cb19cb`. Baseline `7c3a786e` records6 passing controls/16 failing redirect-credential and YAML-prototype targets. Patched parent resolutions are builder-util-runtime9.7.0 and js-yaml4.3.2; all23 updater and eight certificate cases pass. Removing header stripping fails18 cases and removing total merge-work enforcement fails one. Root/bin/Playwright/Electron test types, scoped lint, immutable installation, production build/bundle and all318 tooling cases pass on that branch. Fresh installed-production audit at2026-09-22T16:00:22Z has25 matches (10 high/13 moderate/two low), across axios/Joi/uuid; none remain for jsrsasign or these updater dependencies. Commands/evidence live in that branch's review ledger and `/tmp/elc003-updater-*`. Next: axios tooling/remediation and production classification; separately characterize macOS updater raw error/feed diagnostics under F-007/INV-010. No actual update/install, native qualification, ASAR audit, composition or remote write is claimed. This packaging branch remains separate; publication/profile decisions are still pending.

**ELC-003 follow-up committed independently:** `ab084936` on `fix/ELC-003-protocol-runtime-2026-09-22` (worktree `/tmp/wire-elc003-protocol-runtime`) patches certificate-check's exact transitive jsrsasign pin from11.1.0 to11.1.1. Baseline `0d792464` has eight real-parser RSA/EC fingerprint/pin/malformed-input tests; weakening all-field pin validation fails three deny cases. Patched tests, root/bin/Playwright/Electron test types, scoped lint, immutable installation and production build/bundle pass. Actual parent resolution and installed graph confirm11.1.1. Fresh npm audit at2026-09-22T15:53:14Z has no jsrsasign matches;31 matches remain (14 high/15 moderate/two low) across five names. No native/ASAR qualification or integration is claimed. Commands and source/advisory links are recorded in that branch's review ledger; local logs `/tmp/elc003-certificate-{patched,types,main-types,lint,build,immutable}.log`. Next independent work: continue updater dependency remediation and remaining axios/Joi/uuid audit on that branch. Packaging remains locally validated through `5e4c23b6`; the two scopes still require reviewed reconciliation and final composed gates.

**F-015 CLI/stream closeout candidate:** CLI baseline `50e86c1a` passes four success controls and fails eight preparation/deployment failure confidentiality cases. All four terminal catches now emit fixed operation errors and retain exit1; all12 cases pass. Restoring raw logging fails all eight targets. Stream baseline `0f6eb9ff` passes successful upload and fails missing-file/dry-run cases: the old Hockey listener throws asynchronously and dry run still opens the file. Candidate delegates stream errors through FormData/transport rejection, destroys the stream in `finally`, and returns before file opening for dry run. Four real-filesystem/FormData cases pass, including immediate transport rejection. Old stream code fails both deny targets; removing cleanup fails the transport completion assertion. No HTTP leaves the fixtures. Dedicated bin types and forced scoped lint pass. All351 Node tooling cases pass on the restored candidate (`node .yarn/releases/yarn-3.3.1.cjs test:bin`). Logs: `/tmp/pkg001-deployment-cli-{baseline,restored,sensitivity,types,lint}.log`, `/tmp/pkg001-hockey-stream-{baseline,restored,sensitivity,cleanup-sensitivity,lint}.log`, `/tmp/pkg001-deployment-closeout-{types,lint,full-bin}.log`.

Next independent implementation: continue ELC-003 in its existing isolated worktree, beginning with the pinned jsrsasign11.1.0 certificate dependency and preserved pin-extraction/verification tests. Keep current packaging fixes scoped and reconcile them only through reviewed PRs once fork publication is authorized. F-015 remains unintegrated; the full TST-006 inventory and final composed platform gates are still open. The older HTTP/dry-run checkpoints below are retained evidence.

**F-015 HTTP failure slice:** baseline `0d30872d` records five passing success controls and ten failing negative cases for GitHub/Hockey creation/upload/rollback with and without HTTP responses. Candidate uses fixed failure messages and corrects GitHub rollback from the release collection to the specific failed draft. All31 deployment cases pass; restoring raw error logging fails ten targets, then source is restored. Dedicated bin types and forced scoped lint pass. All335 Node tooling cases pass on the restored candidate (`node .yarn/releases/yarn-3.3.1.cjs test:bin`); evidence `/tmp/pkg001-deployment-errors-{baseline,restored,sensitivity,types,lint,full-bin}.log`. Next: characterize and sanitize deployment CLI terminal catches (including propagated S3 failures), preserve nonzero exit behavior, and examine Hockey stream error handling. F-015 remains open until these adjacent diagnostic boundaries and aggregate evidence are complete. Publication/profile decisions remain pending; no remote write or native execution occurred.

**F-015 dry-run slice:** baseline `d0a29c8e` records one passing control/one failing confidentiality target using the real GitHub deployer, synthetic inputs and zero HTTP requests. Shared dry-run logging now emits only operation names; arbitrary arguments (credentials, buffers and request bodies) are omitted. All16 deployment cases and dedicated bin types pass; forced scoped lint passes after existing header/import formatting is normalized. Restoring argument logging fails the intended target; restored two-case regression passes. Evidence: `/tmp/pkg001-dry-run-{baseline,restored,sensitivity,final,types,lint}.log`. Command: `node node_modules/mocha/bin/mocha.js --require .babel-register.js 'bin/deploy-tools/**/*.test.ts'`. F-015 remains open: next add inert regression coverage for GitHub/Hockey raw error/response diagnostics, sanitize those paths while preserving failure/cleanup behavior, then run the aggregate tooling suite. No deployment, native app, push or workflow dispatch occurred.

**Independent local progress:** isolated branch `fix/ELC-003-protocol-runtime-2026-09-22` at `e949648d`, worktree `/tmp/wire-elc003-protocol-runtime`, removes the unnecessary protocol-messaging runtime dependency. Baseline `c35af26c` proves numeric BUSY=3 behavior before removal, with wrong-value sensitivity failures in both consumers. Fresh immutable installation confirms protocol-messaging/protobufjs/protobufjs-cli absent;80 renderer tests, nine Node controller/queue cases,318 configured build-tool cases, all type checks, scoped lint and production build/bundle pass. Normal `configure` was required after skip-build installation; its initial129-pass/27-failure missing-plist result is retained. The installed production graph falls from436 to356 package instances, not yet verified in an actual artifact. Both worktrees are independent; the original dependency installation is unchanged.

The ELC-003 candidate's fresh npm audit still reports37 advisory/version matches across six production names (including critical/high); that item is not complete. Its review ledger also confirms **F-015**, a GitHub dry-run upload token reaching diagnostics, using synthetic input and zero network requests. Next independent PKG-001 work: port that reproduction to repository tests, preserve dry-run/no-network behavior and remove credential-bearing deployment diagnostics; then continue ELC-003 remediation on its existing isolated branch. See that branch's [review ledger](/tmp/wire-elc003-protocol-runtime/docs/electron-modernization/review-findings.md). Fork publication and profile-compatibility decisions remain pending.

**Active goal (maintainer confirmed September 22): close every mandatory gate for the review-ready unsigned Wire engineering handoff in [plan.md, section 8](plan.md#current-goal-close-mandatory-unsigned-review-handoff).** The goal is active and incomplete. Later release qualification and optional improvements are separated explicitly there; no finding or gate is waived by this prioritization.

**Current PKG-001 task:** F-014 is corrected locally in `96219916`; signing baselines `01fe145a`, `2e2e3a69` and `faa4bb5a` precede the F-006 candidate below. Finish candidate validation/publication and actual platform preflight, then diagnose retained Windows lifecycle failures and reconcile the branch with its CAP-001 dependency before scoped PR integration. Profile compatibility remains a required product decision; the maintainer was asked on September22 and independent work continues. Preparing the handoff does not authorize contacting Wire or releasing.

**Local follow-up while publication is pending:** baseline `bd46385d` preserves two application-only signing controls and exposes automatic installer-only false success when notarization is absent (two passes/one failure). The config now requires an application identity whenever an installer identity is provided, in either mode. All19 signing cases, the existing config case, dedicated bin types, lint and formatting pass. Both application-only controls fail when signing is temporarily conditioned on an installer; source is restored. Logs: `/tmp/pkg001-installer-identity-{baseline,restored,config,types,lint}.log` and `/tmp/pkg001-application-only-sensitivity.log`. This extends F-006; native signing and platform qualification remain unrun.

ELC-003 read-only preparation confirms production imports of `@wireapp/protocol-messaging` only consume `Availability.Type.BUSY` in renderer badge actions and the main account controller. The installed1.56.0 schema defines BUSY=3. Traversal from declared production/optional/peer dependencies resolves436 installed package instances, with80 reachable exclusively through protocol-messaging; six absent optional/peer edges are disclosed. This is a local installed-graph estimate, not a fresh advisory audit or actual artifact inventory. Before removal, protect numeric3/non-busy badge behavior in both callers and verify the built artifact after dependency pruning. Scratch evidence: `/tmp/wire-handoff-audit/production-graph.{cjs,json}`. Keep implementation on an ELC-003 branch, not the current PKG-001 branch.

**Publication checkpoint:** local candidate `b725d784` is committed, with a clean worktree before this status update. Automatic approval review rejected the push to `fork` (`adamlow-wire/wire-desktop`) as an unverified external destination requiring explicit user authorization to export source/documentation. A specific fork-push/native-package-CI approval question is pending; no push or workflow dispatch occurred and no workaround is authorized. Local commits and read-only failure investigation continued. This is a publication dependency, not a claim that the whole goal is blocked or complete.

Read-only inspection of the latest PR62 Linux [E2E job104605123525](https://github.com/adamlow-wire/wire-desktop/actions/runs/35035967615/job/104605123525) confirms58 passes/18 failures, with repeated `Search people` waits. The job log does not itself contain the previously observed `findDOMNode` browser exception; fresh trace diagnosis remains open. Linux blob artifact10425526364 is4.57GB and was not downloaded wholesale. Evidence: `/tmp/handoff-pr62-latest-linux-e2e.log`. Do not change locators or waive E2E based on these logs alone.

### Return audit — September 22

**F-014 implementation checkpoint:** Jest now excludes Node-only `bin/` suites, which remain mandatory under `test:bin`; root typing delegates tooling tests to `tsconfig.bin.json`, and `test:types` explicitly runs that compiler before Playwright typing. Root/tooling/Playwright types pass together; Jest passes all74 renderer tests in12 suites. Prior129 counted additional tooling tests under the wrong runner. The complete pre-signing-fix Mocha run preserves303 passes/eight signing-target failures. Evidence: `/tmp/pkg001-f014-{jest,types,bin}.log`. Configuration formatting and JS syntax pass. An attempted direct Mocha import exposed competing global declarations and was reverted before these passing results.

**F-006 local candidate:** package → fuses → sign/verify → optional notarization → installer is now explicit outside electron-packager's swallowed-error path. Manual signing uses `@electron/osx-sign` instead of obsolete helper paths and failure-swallowing shell commands. Automatic notarization directly pins the existing2.5.0 dependency and requires complete notarytool credentials/team plus a signing identity. Three incomplete-config targets fail before validation is added. Removing fuses or swallowing automatic sign rejection fails one intended target each; both perturbations are restored. All314 build-tool cases pass at this scope, with root/bin/Playwright/Electron test types, scoped lint and immutable dependency installation passing. A subsequent relative-output regression exposes main-executable child entitlements; normalizing the signing app path fixes it, and the expanded16-case signing suite, dedicated bin typing and scoped lint pass. Logs: `/tmp/pkg001-signing-{options-baseline,config-baseline,final-bin,final-types,final-lint,install,sensitivity-fuses,sensitivity-sign-rejection,relative-baseline,relative-restored}.log` and `/tmp/pkg001-manual-config-baseline.log`. Native signed execution is unrun and remains Wire/M5; all-platform unsigned preflight and reconciliation are still required.

Read-only GitHub verification confirms integration is still `897e3930392fdc9479f9641c8c03f116947d4e95`. The clean starting checkout is `01fe145a` on the existing PKG-001 branch, one unpublished signing-test baseline ahead of remote `55073e83`. PR62 is the only open fork PR. The protected MSI worktree remains at `255bdd54`. Electron is still pinned to43.4.0 under the existing unsigned-handoff scope; this audit does not establish current release currency.

The following completed results supersede the pending CI wording below:

- Archive workflow [35038585587](https://github.com/adamlow-wire/wire-desktop/actions/runs/35038585587) at `974dcfa7` **failed overall**. Linux104613143677 and macOS104613143772 succeeded. [Windows104613143821](https://github.com/adamlow-wire/wire-desktop/actions/runs/35038585587/job/104613143821) stopped before packaging:29 secure-shell passes/one failure, a ten-second timeout in “owns the shell lifecycle and revokes authority on disposal.” No root cause or Windows archive qualification follows from this result.
- [PR62](https://github.com/adamlow-wire/wire-desktop/pull/62) has passing build/lint/analysis and native/package checks, but its latest [full E2E35035967615](https://github.com/adamlow-wire/wire-desktop/actions/runs/35035967615) failed on Linux, macOS and Windows. Report publication succeeded; the PR is not merge-ready. The older webapp exception remains an investigation lead, not a fresh diagnosis of every latest failure.
- Repeated capture/media [35036531801](https://github.com/adamlow-wire/wire-desktop/actions/runs/35036531801) finished with Linux/macOS successful and Windows failing native account lifecycle/cleanup, as previously diagnosed. Successful capture iterations do not resolve that separate failure.

Fresh local validation also exposes a test-runner integration gap: `test:react --runInBand` reports28 passing/six failing suites,129 passing tests; Jest collects Mocha build-tool files and fails on their hooks/context or module loading. Root `tsc --noEmit` reports TS2683 at `build-cli.test.ts:58`, `build-wrapper.test.ts:46` and `macos-signing.test.ts:40`; dedicated `build:ts:bin` passes. These are recorded as F-014 under existing PKG-001/TST-005/TST-006. Logs: `/tmp/electron-return-{react,app-types,bin-types}.log`. No shared-display/native Electron tests, packaging, source perturbations, pushes or merges were performed.

Fresh `node .yarn/releases/yarn-3.3.1.cjs test:bin` completes with303 passes/eight failures, all in the new macOS signing security-target suite. The unsigned/package controls pass; automatic ordering and manual failure propagation remain unsatisfied. The manual-success test also asserts a single signing event whereas the current implementation signs multiple components; review that assertion's abstraction before treating it as a separate defect. Evidence: `/tmp/electron-return-test-bin-unrestricted.log`. The first sandboxed attempt failed child spawning with EPERM and is environmental evidence only (`/tmp/electron-return-test-bin.log`); the completed retry permits inert Node children, not native signing or packaging. `git diff --check` passes for this documentation update.

Accepted execution order:

1. Continue PKG-001 on this branch: repair Jest/TypeScript test ownership without losing Mocha execution; complete F-006 signing/fuse order and native-tool failure propagation from baseline `01fe145a`; preserve metadata recovery and confidential diagnostics. Re-run aggregate checks, not only dedicated bin checks.
2. Diagnose the retained Windows lifecycle failures and latest authenticated E2E failures under TST-005/F-002, then qualify actual Windows/macOS/Linux archives on the resulting head. Do not use blind retries as closure.
3. Reconcile and review CAP-001 PR62 and the existing settings/IPC/SSO candidates against actual integration, keeping scoped PRs and requiring final-head protected checks. F-012 still needs the existing-profile versus fresh-profile-only compatibility decision before changing ciphertext ownership.
4. Complete ELC-003's shipped-dependency audit/remediation and TST-006's full review/provenance/coverage ledger. The current inventory has444 rows, including380 with source review exactly `pending`,412 with test provenance `pending`, and423 with coverage disposition `pending`; these are ledger counts, not a completion percentage.
5. Qualify the final composed unsigned PKG-001/TST-005 candidate and prepare the Wire review handoff. Signing/notarization, released-profile/update/rollback QA, live SSO/E2EI and independent security review remain external release gates under M5; M4 is not complete.

### September 16 checkpoint (retained)

Active goal remains TST-006 review/coverage, ELC-003 dependencies and unsigned PKG-001/TST-005 qualification. Integration remains `897e3930`; no runtime remediation PR has merged. Electron remains43.4.0. Required full E2E and F-012 profile-compatibility decisions remain open; independent work continues.

**PKG-001 active branch:** `fix/PKG-001-fail-closed-builds-2026-09-15`. CLI confidentiality/failure fix `e2920196`, distinct backup/recovery fix `c70432e5`, partial metadata-write fix `12ecd796`. Baselines `7eb299f3`, `5efc93d8`, `03e1666e` record original failing targets separately. All81 Node build-tool cases passed at `12ecd796`. New inert wrapper baseline `1df0ce00` passes21 cases and fails MSI read-error backup cleanup. MSI fix `e5022d63` passes all four MSI cases; moving its read back outside restoration fails the target again. Tool substitutes never execute native packagers/signing; actual artifacts remain to qualify. Next: finish wrapper evidence, package-input allowlisting and signing/fuse order under this branch.

**Package-input candidate:** baseline `9de6fb4a` records16 passing/98 failing real copy-filter cases. The local shared allowlist passes179 policy cases, preserving runtime/native-dependency assets and supporting literal nested/space-containing paths. Restoring old filters reproduces all98 failures before restoration. The complete225-case Node suite passes at the earlier122-policy-case scope; the expanded179-policy-case run also passes, with dedicated bin typing/scoped lint. Removing directory validation fails all seven targets before restoration. Final package artifacts remain unqualified. Allowlist implementation is published at `78d2d5dd`. The next packaging head composes only the three reviewed native fixture/workflow files from CAP-001 `a1cb2b84`, preserving the packaging changes. Composed packaging head `f7c1c359` is published; native/unsigned package preflight35037409000 is running. Next: inspect its actual archives, add explicit content checks, then review signing/fuse order.

**Archive verification:** preflight35037409000 succeeds on Windows104609510260/macOS104609510369/Linux104609510387 at `f7c1c359`; downloaded artifact10424091204 has a verified GitHub ZIP digest. Read-only `unsquashfs` inspection passes the new verifier with12449 entries and22 required files. New local verifier/workflow changes add seeded canaries, archive hashes, actual extracted-AppImage smoke, complete Windows package uploads and permission-preserving macOS tar artifacts. Seventeen real-ASAR tests pass; removing the content check fails ten targets before restoration. All299 Node build-tool cases, dedicated bin typing and scoped lint pass. Workflow YAML and bash syntax pass; published head `974dcfa7` is qualifying in run35038585587. Next: inspect final archive/manifests and address signing/fuse order. Installed electron-packager catches signing errors and still returns a package; source review confirms this additional F-006 failure path, with tests/remediation pending. See [procedure and artifact evidence](packaging-verification.md).

**CAP-001 PR62:** published `a1cb2b84` includes bounded queue/logging and diagnosed native Stop test correction. Hosted Stop sensitivity passes all three platforms; Linux/macOS complete five repeated capture/media iterations in manual run35035452235. Linux104603441117 completes the whole package job successfully; macOS104603441004 was still running. Windows104603441247 fails media setup at Mocha's implicit two seconds, repeating104600525641. Candidate `a1cb2b84` assigns only setup the existing ten-second native fixture budget and fixed timing diagnostics; media bodies retain two seconds. At current head `a1cb2b84`, core35035967619, lint35035967681 and CodeQL35035968060 pass (exact CodeQL analysis still needs review). Native35035967586 passes Linux104605113342/macOS104605113052; Windows104605113576 also passes; full E2E35035967615 remains running. Earlier repeat35035452235 finishes Linux/macOS successfully, with the retained Windows setup failure. Current-head repeat35036531801 completes all five capture/media iterations on each platform. Linux104606783573 finishes successfully. Windows104606783584 subsequently fails account-controller afterEach at ten seconds, then six other lifecycle/permission cases or hooks time out; all five capture/media iterations had passed. Setup diagnostics measured1753ms and8091ms to create/load native views. This does not diagnose the later teardown failure. macOS104606783333 remains running. Retain all earlier failures; no waiver, blind rerun or merge. Separate media-body and session-clear timing failures remain open.

**Other retained candidates:** CAP-002 `b58de5a1` composes settings/IPC/SSO fixes and passes native/package35029959416 on all three platforms. These branches still need reconciliation, scoped PR review and final protected checks. Signing/notarization, live Keycloak/E2EI and released-profile/update QA remain external release qualifications. Protected MSI worktree and user patch remain unchanged; no shared-display tests run.

## Earlier review and qualification evidence

The following records preserve earlier observations; the current execution above supersedes their pending/current wording.

The controlled earlier-head [PR61 Linux comparison](https://github.com/adamlow-wire/wire-desktop/actions/runs/35010907346/job/104570922016) completes with 58 passes/18 failures and 42 Search people failure mentions. Report `104594787687` succeeds. Its original passing attempt remains historical evidence; unchanged earlier code failing now supports the external-environment investigation but does not independently prove the exact browser exception without its traces. Prior PR62 traces already establish that exception in ordinary Chromium participants as well as Electron. The requested compatible test environment and F-012 profile-compatibility decision remain pending while independent work continues.

The separate CAP-002 candidate `b58de5a1` passes complete native/package [run35029959416](https://github.com/adamlow-wire/wire-desktop/actions/runs/35029959416) on Linux `104585894919`, Windows `104585895132` and macOS `104585895144`. Its dedicated SSO/WindowUtil step runs the diagnostic targets and real native failed-load case on each platform. This composed candidate includes the settings and IPC fixes, qualifying their previously unrun Windows native steps as well. No PR or integration merge has occurred for these stacked candidates; all protected final-head checks and reconciliation remain required. The preceding timing failures remain F-002; a later successful run does not diagnose them.

The preceding SEC-003 F-003/F-011 candidate at `a26321e8` passes its 16-case IPC stage on all platforms in [run35027694438](https://github.com/adamlow-wire/wire-desktop/actions/runs/35027694438). Linux/macOS complete native/package checks; Windows fails later in account lifecycle (162 passes/two timeouts). Diagnostic follow-up `38722dff` runs in [run35029234576](https://github.com/adamlow-wire/wire-desktop/actions/runs/35029234576), preserving the ten-second timeout and reporting fixed cleanup stages/durations. Windows `104583548074` fails with 156 lifecycle passes/three failures. Its diagnostic shows view destruction in 5ms and shell destruction in 3ms, then the first session clear still pending at nine seconds; startup had stalled while starting account views. The fixture-server hypothesis does not explain this observed teardown stall. Linux and macOS finish successfully. It is not a blind unchanged-head rerun. No SEC-003 PR or integration merge has occurred. The queue, settings and IPC candidates remain a locally composed stack requiring scoped reconciliation and protected checks before integration.

**Current remediation:** [PR62](https://github.com/adamlow-wire/wire-desktop/pull/62), queue head `28fbf798`, passes build/lint/CodeQL and native/package checks on Windows/macOS/Linux initially. Full E2E fails Linux and macOS with 58 passes/18 failures each; Windows was cancelled; report publication succeeds without qualifying the failed platforms. The PR is not accepted or merged. The first retained Linux trace shows the remote webapp entering its error fallback after the people button, with `TypeError: n.findDOMNode is not a function` from `https://wire-webapp-dev.zinfra.io/min/vendor.js`. This is not evidence for changing the search locator. A controlled unchanged-PR61 Linux comparison is running as job `104570922016`; the original passing attempt remains historical evidence. The maintainer has been asked about access to repair the hosted webapp or a known compatible test URL; other remediation continues, and this is not a gate waiver. The scoped PKG-003 branch is published at `40ff0485`; native/package preflight [run35026361662](https://github.com/adamlow-wire/wire-desktop/actions/runs/35026361662) passes Linux job `104574229533` and macOS job `104574229199` on that exact head. Windows job `104574229469` fails earlier in native account lifecycle (154 passes/six failures), before the selected settings/product stage; Windows settings qualification remains unrun. The first failure is controller teardown timing, followed by filesystem/view timing failures and an ENOTEMPTY cleanup error. No rerun or root-cause claim is made. A PR and merge still require reconciliation with the integration branch and all protected checks; no notification-sending E2E workflow is manually dispatched.

F-010 is a confirmed settings data-loss/diagnostic finding. Separate migration baseline `b122d801` records three passes/five failing security targets; persistence baseline `f5f8684c` records four passes/three failing targets. Current local remediation preserves current/legacy files, rejects corrupt data, uses private staged writes, reports generic errors and preserves Quit after save failure. All 34 filesystem cases and five EnvironmentUtil cases (including two save-failure cases) pass locally; removing the flush/publication protections causes the expected failure. A fixture correction uses the real `fullscreen`/`bounds` keys and passes against the original source. Native Quit/menu and cross-platform filesystem qualification remain pending. [Recovery and test procedure](settings-recovery.md) defines safe synthetic QA and explicit released-migration limits. This is not PKG-003/M5 completion.

Continued TST-006 review confirms F-009 raw SSO console data reaches the log writer and F-011 unbounded pending picture-save work survives successive per-minute quota windows. Reproductions use synthetic data and no native windows. These remain scoped CAP-002 / SEC-003/SEC-004 remediation, not accepted handoff risks. Original SSO baseline provenance is now independently checked against PR5 build logs; its skipped E2E jobs are disclosed in the ledger. F-012 revisits the already-recorded DCP-016 ciphertext ownership gap; the maintainer has been asked whether the unsigned candidate must open existing profiles or may refuse them pending M5 migration. Existing encryption behavior remains unchanged while other remediation continues. F-013 confirms that log rotation does not bound queued log messages; a blocked append retains 1,000 tiny writes in an inert-filesystem reproduction. Its CAP-001 fix must preserve drain-before-delete and avoid recursive overflow logging. F-011 and limiter-lifetime fixes now have repository tests and the native IPC results above. F-009 has the separate repository baseline and local correction above; the findings ledger distinguishes those results from the earlier scratch experiments.

**M3 and functional M4 are integrated.** [PR #60](https://github.com/adamlow-wire/wire-desktop/pull/60) merged as `6e27c614529c162463d7c5b1d4948bd9fa9fcec9`, reviewed head `adbebedc4f033e63f39a662bc82c4797b5eb5bd9`. Reviewed head, tested synthetic merge `d9648c72` and actual merge share tree `bef4b31e75af640f63c271d1b22bd1687a241700`. All ten strict required checks, admin enforcement, conversation resolution, no unresolved threads and SHA guard were verified before merge. Local integration was fast-forwarded cleanly; protected MSI worktree remains `255bdd54`.

Final [build](https://github.com/adamlow-wire/wire-desktop/actions/runs/34993542936), [lint](https://github.com/adamlow-wire/wire-desktop/actions/runs/34993542973), [CodeQL](https://github.com/adamlow-wire/wire-desktop/actions/runs/34993542964), [native/package](https://github.com/adamlow-wire/wire-desktop/actions/runs/34993542993), and [full E2E/report](https://github.com/adamlow-wire/wire-desktop/actions/runs/34993542986) pass. Full E2E completes 76 tests on each of Windows/macOS/Linux initially, with no skips or independent worker errors; report 104476129387 passes. Build executes 112 Jest/964 native/4 media/23 renderer/51 tooling cases. CodeQL 1780506437 has zero findings/no error. Build and native macOS each require one retained unchanged-head rerun after 20-second capture-cancellation timeouts; no assertion or runtime change, and no proven root cause. TST-006 must investigate rather than silently treat reruns as resolution.

The Windows false-success version-probe gap is corrected: job 104463987040 logs both actual packaged startups, authenticated proxy success and the mandatory completion gate. Linux and macOS also execute both package smokes. Historical green Windows jobs without execution remain excluded. This does not qualify signing, installers or unmanaged Windows policy transitions.

**TST-006 is in progress, not accepted.** The review plan merged through [PR61](https://github.com/adamlow-wire/wire-desktop/pull/61) as `897e3930` after all ten checks, 76 initial E2E passes/platform, exact CodeQL/tree verification and SHA guard. Native Linux/Windows each needed one retained unchanged-head rerun; the capture/fixture timeouts remain F-002. [Audit findings and evidence](review-findings.md), [full-delta inventory](review-inventory.csv) and [behavioral traceability](review-traceability.csv) record pending work without a full-coverage claim. The audit confirms packaging input/logging/error-order defects, dependency advisories needing graph-aware remediation and an unbounded account lifecycle queue. Other candidate observations remain explicitly unconfirmed.

**Current scoped remediation: CAP-001 / F-008.** Baseline `8cd6bd18` preserves ordering and stale-authority behavior and fails overload rejection. The local candidate bounds active/queued lifecycle operations at 32 and restores capacity on every settlement, including authorization failure. Three Node-only tests and four temporary perturbations protect copying, authority, overload denial and recovery. No local native/GUI tests ran. Local queue/state/profile validation passes 40 cases; Mocha types, scoped lint and source review pass. Hosted native/product/all-platform gates remain required before merge acceptance. This later regression test is not relabelled as a pre-modernization baseline.

**Next executable step:** diagnose the PR62 E2E failures against retained traces and the unchanged-checkpoint comparison; inspect remaining platform/report results. Merge only after all final-head protected gates and tree checks pass. Qualify the prepared PKG-003 settings branch through the native/package workflow, then reconcile and create its scoped PR after PR62 integration. Continue scoped remediation of the recorded IPC, SSO, logging, packaging and dependency findings and finish coverage/traceability/unsigned-package qualification. M4 and the overall unsigned handoff goal remain open.

**Maintainer decision:** signing/notarization and actual signed-package validation are intended for Wire after engineering review, and remain M5 release gates. Prepare reviewed pipeline order and unsigned evidence now. No individual at Wire is assigned yet. Internal TST-006 review is additional to prior PR reviews and is not independent REL-001 review. Live customer Keycloak/E2EI remains downstream QA. The maintainer reports successful manual preview checks, but has not supplied an exact scenario/platform evidence record; do not mark the entire manual matrix passed.

The [Windows preview](https://github.com/adamlow-wire/wire-desktop/actions/runs/34993542942/artifacts/10407101912) has distinct identity/profile, source `d9648c72`, and actual identity/startup smoke evidence. Its finite-retention artifact is not a durable release download; preserve reproducible build instructions. User patch `/tmp/m3-closeout-review/documentation.patch` and MSI work remain untouched. No local GUI testing resumed.

### Earlier execution checkpoints (historical; current status above takes precedence)

**Execution constraint:** the maintainer paused testing because native windows interrupted their work. Do not run GUI/native Electron suites on the shared DISPLAY. Use hosted CI and non-GUI checks until a truly isolated display is available. The maintainer requests a runnable Windows preview alongside installed Wire; a scoped CI-only preview bundle uses a distinct application/protocol identity and explicit separate profile. It is not release packaging acceptance.

Preview first run [34980042262](https://github.com/adamlow-wire/wire-desktop/actions/runs/34980042262) builds the full Windows application but its package fixture expected unmanaged mode while the runner reported managed mode. Sandbox/immutable bridge/Node-denial observations pass; the exact managed flag comparison fails. The preview-only follow-up sets a temporary explicit managed policy on the disposable runner and retains all original assertions. It also verifies the actual packaged app/protocol identity before publishing the bundle. No runtime policy changes or local GUI tests are introduced.

The requested [Windows preview](https://github.com/adamlow-wire/wire-desktop/actions/runs/34980648583/artifacts/10401532042) is available from candidate `7419d3d8` after [preview run34980648583](https://github.com/adamlow-wire/wire-desktop/actions/runs/34980648583) passes. The complete ZIP includes `WireM4Preview.exe`, its dependencies and `Start-Wire-M4.cmd`, which uses a separate adjacent `Data` profile. The packaged app/protocol identity is verified before its immutable configuration/Node-denial/startup smoke. Source security behavior is unchanged; the unsigned preview does not claim completed M4 or release acceptance. See [manual instructions](manual-windows-preview.md). The maintainer has received the download link.

Candidate `7419d3d8` passes [build/test](https://github.com/adamlow-wire/wire-desktop/actions/runs/34980648619), [CodeQL](https://github.com/adamlow-wire/wire-desktop/actions/runs/34980648545), and [all native platforms](https://github.com/adamlow-wire/wire-desktop/actions/runs/34980648540) initially, including 24 product cases per platform. CodeQL analysis1779726987 on synthetic merge1ce3fa45 reports zero findings/no error. [Full E2E34980648667](https://github.com/adamlow-wire/wire-desktop/actions/runs/34980648667) passes 75 macOS cases initially and 75 Linux cases with one registration retry; Windows passes 74 initially but exhausts all three attempts waiting for the second account unread indicator in notifications.spec.ts. Capture and calling pass on Windows. All three retained Windows traces show the second account has switched from Distraction Group into the new MLS direct conversation and displays Test Message 2 there. The notification fixture therefore no longer has its unread-conversation precondition. A scoped setup correction first establishes and reads that MLS conversation, then selects/asserts Distraction Group and zero unread before the measured messages. Existing unread totals, both account dots and exact notification activation assertions remain intact. Hosted validation of the correction is mandatory; the failed platform is not accepted. Lint flags one extra documentation blank line, corrected locally for the next publication. Prior `6dd6133f` passes all native platforms and all 75 macOS E2E cases initially; Linux/Windows/report are superseded by the requested preview publication and are not accepted. Its build run34977263456 had 963 native passes and one twenty-second visible-chooser-cancellation timeout; source is unchanged in the now-passing build run34980648619. The timeout remains a TST-005 observation without a claimed root cause or weakened assertion. Additional integration run34975642639 exposes call-cell visibility failures on Linux/Windows. Retained Windows snapshots show “Call not established”, calling-service unreachable and offline indicators on all three direct-call attempts (`/tmp/m4-integration-windows-blobs.zip`); this explains the absent call UI but does not establish the underlying network cause. Current composed calling qualification remains mandatory.

Current Linux full E2E completes all 75 cases (74 initially, one registration HTTP409 retry). Separate baseline `650a00c7` reproduces repeated registration addresses from the name-based Faker email generator. Using the fixture's existing random identifier for its email removes that reproducible collision path; all 16 API-fixture cases pass, removal fails the independence assertion and restoration passes (`/tmp/m4-registration-identity-{baseline,restored,sensitivity,final}.log`). These tests open no application windows. The run's response body was not retained, so the particular HTTP409 cause is not asserted beyond registration conflict. The correction is included in the next composed qualification head.

**M3 cleanup acceptance is restored.** [PR #59](https://github.com/adamlow-wire/wire-desktop/pull/59) merged as `51d04739ace3b43a3d5201471da750492c1bd536` after substantive exact-head review, no unresolved threads, build/lint/CodeQL, all native/package platforms and full authenticated E2E/report passed. The tested merge tree equals reviewed `7d5c34bb`. Queued account log writes now settle after native producer closure and before exclusive deletion. Separate baseline `e4cac57f` and a temporary drain removal expose recreation; restoration passes23 focused and925 complete native cases. Both supported authenticated platforms pass48 cases: macOS47 initial/one login retry, Windows46 initial/two post-removal/proxy-readiness retries; no skips, worker errors or ENOTEMPTY log failures. The [M3 acceptance audit](m3-acceptance.md) records this follow-up without erasing earlier evidence.

Functional development/test TST-005 is integrated through [PR #58](https://github.com/adamlow-wire/wire-desktop/pull/58), merged as `6930c8cebe379dc7b04c89e1207fbbcefb80f010`. Reviewed head `c340ad09` passes [build](https://github.com/adamlow-wire/wire-desktop/actions/runs/34970738884), [lint](https://github.com/adamlow-wire/wire-desktop/actions/runs/34970738875), [CodeQL](https://github.com/adamlow-wire/wire-desktop/actions/runs/34970738893), [all native/package platforms](https://github.com/adamlow-wire/wire-desktop/actions/runs/34970738945), and [full three-platform E2E/report](https://github.com/adamlow-wire/wire-desktop/actions/runs/34970738959). Each authenticated platform completes 69 cases: Windows 69 initially; Linux 67 initially plus two login-fixture retries; macOS 68 initially plus group calling on its second allowed retry, after login and call-acceptance timeouts. A dialog-session error accompanies macOS retry timeout cleanup; no independent worker error or skipped test is reported. Native macOS requires one reviewed unchanged-head rerun after the unchanged two-second notification-denial timeout; that rerun passes all 18 product cases and package smoke. Linux/Windows native jobs pass initially. The failed attempts remain evidence, with no assertion or deadline waiver.

Before the SHA-guarded merge, substantive exact-head review found no unresolved threads; actual CodeQL analysis1779128539 on synthetic merge `3a527781` reports zero findings/no error. Actual integration and reviewed `c340ad09` have identical tree `4870ee2e0de5f5b4d8f643daf2517ba843f9ed06`. Protection was strengthened and read back: all ten core/native/platform/report checks are strict and mandatory, with administrator enforcement, conversation resolution and no force pushes/deletion preserved. Packaged release TST-005 remains open under PKG-001.

[Capture PR #60](https://github.com/adamlow-wire/wire-desktop/pull/60) now incorporates that actual integration merge. Preflight `acd3e909` passes [build](https://github.com/adamlow-wire/wire-desktop/actions/runs/34972797456), [lint](https://github.com/adamlow-wire/wire-desktop/actions/runs/34972797425), [CodeQL](https://github.com/adamlow-wire/wire-desktop/actions/runs/34972797451) and [all native/package platforms](https://github.com/adamlow-wire/wire-desktop/actions/runs/34972797514) initially, including all 22 product cases per platform. Actual CodeQL analysis1779248877 on synthetic merge `6914d8dd` reports zero findings/no error. This is preflight evidence, not the reconciled final head.

[Capture authenticated preflight](https://github.com/adamlow-wire/wire-desktop/actions/runs/34972797462) fails Linux/macOS with repeated test-account registration failures. Linux completes 66 initially plus one login retry and has five exhausted cases; macOS completes 64 initially plus one restart retry and has seven exhausted registration cases. Linux also records a backend HTTP 500 during the three-account attempt. Windows/report are still pending. No product or security gate is waived. The registration helper previously discarded the HTTP status from its surfaced error. Separate baseline `b3499b86` fails its safe-status contract; correction `4cabcb61` reports only the HTTP status, without user or response contents. All 15 fixture cases pass, temporary removal fails, restoration passes (`/tmp/m4-registration-status-{baseline,restored,sensitivity,final}.log`). This adds diagnostics, not retries or a claimed backend root cause.

The macOS restart retry exposes the native menu Quit closing the inspector before its reply. An owned loopback baseline `14c74b0c` reproduces this with a deliberately pending inspector reply and real zero process exit. The correction routes explicit restart Quit through the existing exit-authoritative helper, retaining the ten-second zero-exit check, cleanup and original request errors if exit fails. All 13 launcher/proxy cases pass locally. Removing the correction fails both the zero-exit/reply and rejected-request regressions; restoration passes both. Logs: `/tmp/m4-quit-reply-{baseline-forced,restored,sensitivity-restaged,final}.log`. An initial sensitivity invocation omitted the private keyring environment and failed before tests; it is retained separately and is not counted. Final-head qualification remains required. No production permission, capture, pinning or lifecycle behavior changes.

The composed production runtime has fresh local coverage: 112 Jest, 964 native and 23 renderer cases; 455/545 changed statements (83.49%) and 183/199 tracked security branches (91.96%) versus TST dependency `c340ad09`. Cumulatively versus completed M2 `fe0b86cb`: 3732/4373 statements (85.34%) and 1021/1050 security branches (97.24%). Types and the earlier 22-case owned Linux product selection pass. Logs are `/tmp/m4-capture-final-{coverage,cumulative-coverage,types}.log` and `/tmp/m4-capture-diagnostic-product.log`. The unclassified immediate local capture denial remains retained with bounded native focus/grant diagnostics; hosted native capture cases pass initially on every platform. Real capture/privacy/portal behavior remains the explicit [OS QA handoff](qa-display-capture.md).

Next: publish the scoped notification setup and registration-identity corrections with the formatting fix; require every final-head gate and inspect any retry, then close CAP-003/CAP-004 functional acceptance. No application GUI test is run locally after the maintainer pause. Electron **43.4.0**, pinning and every security invariant remain unchanged. Live enterprise/OS QA and packaging/release retain their agreed later scope. No user access or product decision is currently required.

## Superseded qualification history

Functional TST-005 remains under qualification in [PR #58](https://github.com/adamlow-wire/wire-desktop/pull/58). Published `1087bd8a` passes [build](https://github.com/adamlow-wire/wire-desktop/actions/runs/34969442810), [lint](https://github.com/adamlow-wire/wire-desktop/actions/runs/34969442940), [CodeQL](https://github.com/adamlow-wire/wire-desktop/actions/runs/34969442807) and [all native/package platforms](https://github.com/adamlow-wire/wire-desktop/actions/runs/34969442803) initially. Every native platform passes17 product cases, including persisted worker restart; native boundary totals are490 Linux/macOS and491 Windows. [Full E2E](https://github.com/adamlow-wire/wire-desktop/actions/runs/34969442781) is superseded by the cleanup follow-up; it is not final-head evidence. Earlier `be433ea4` fails Windows localization restart on all three attempts (64 initial/two retry passes/one failure); Linux/macOS pass67 with four/one retry passes. That failed gate is not waived.

A subsequent local composed-capture run passes20 of21 cases but fails native exit in the different-proxy-credentials fixture and adds a worker teardown error because cleanup is skipped. Separate baseline `6ddc1283` uses a one-time native Quit veto and proves the owned process remains alive after the required exit assertion fails. The correction always closes the app, then the proxy servers/profile, while preserving the original exit failure and aggregating a second app-cleanup failure. All11 launcher/proxy cases pass locally after correction (`/tmp/m4-quit-cleanup-restored.log`); removing the fix fails the target, restoration passes (`/tmp/m4-quit-cleanup-{sensitivity,final-target}.log`), and types/scoped lint pass. This fixes failure cleanup, not the unexplained original shutdown delay, and changes no deadline, retry or product assertion. Fresh final-head CI is required.

Current TST-005 `c340ad09` passes core checks and initial Linux/Windows native jobs. [Native run34970738945](https://github.com/adamlow-wire/wire-desktop/actions/runs/34970738945) macOS fails the unchanged secure-shell notification-denial case at its existing two-second limit (29 passes/one timeout), before product tests. Source and dependencies match passing1087; one reviewed unchanged-head rerun is underway. No assertion/deadline is changed and no root cause is claimed.

This capture candidate now includes actual integration51d and published TST-005 `c340ad09`. [Fresh TST-005 E2E](https://github.com/adamlow-wire/wire-desktop/actions/runs/34970738959) and all other final gates are running. A new owned persisted-service-worker restart baseline `e5b889e8` reproduces an initial-document stall locally. Protocol evidence shows Playwright buffering a redundant page-owned service-worker attachment until `Page.getFrameTree`, while that paused worker gates the initial document. A narrow version-locked `playwright-core` patch detaches that redundant session immediately; the browser session retains worker instrumentation. All six launcher cases pass locally with the installed patch. Removing only the patch yields one startup stall/two passes in three attempts; restoration passes three attempts in14 seconds. Types and scoped lint pass. Failure diagnostics contain only loading/crash/origin-match flags and worker/modal counts, never raw URLs, IDs or dialog contents. This is an automation correction, not a production policy change; its relationship to the Windows failure still requires fresh hosted qualification. No further startup deadline increase or service-worker disabling is introduced.

Existing capture work is preserved in [draft PR #60](https://github.com/adamlow-wire/wire-desktop/pull/60), head `2b755e78`. Its composed runtime passes112 Jest,964 native,4 media,23 renderer,51 build tools and20 Linux product cases locally; fresh coverage is83.64% changed statements/91.96% tracked security branches versus2fd, and85.34%/97.24% cumulatively versus completed M2. The macOS hide-event fixture correction is sensitive and passes20 coordinator cases. Native preflight passes Linux/macOS initially and Windows on one reviewed unchanged-head retry after setup/cleanup timeouts; the preceding failures remain recorded, with no claimed root cause or weakened assertion. [Full capture E2E](https://github.com/adamlow-wire/wire-desktop/actions/runs/34964731663) passes70 cases each on Linux/macOS (69 initial/one notification or logout retry) but fails Windows localization restart on every attempt (69 other cases pass). Report publication succeeds and does not qualify that failed platform. Final integration-base qualification remains pending.

Capture composition now passes all22 owned Linux product cases without retries (`/tmp/m4-capture-diagnostic-product.log`). The preceding run passed21 and returned immediate NotAllowedError in account capture after selection; its trace is retained and the cause is unclassified. Added test-only diagnostics retain bounded native focus/show/hide events and synthetic enumeration/grant counts while preserving every assertion. Three focused attempts and the complete22-case run pass. The local host uses shared DISPLAY:0; a separate Xvfb could not start with available host permissions, so no private-display result is claimed locally. Hosted Linux CI already uses Xvfb. This does not change capture policy or waive final-head platform gates.

Fresh coverage on capture447f4125 passes112 Jest,964 native and23 renderer cases. Against published TST dependencyc340 it passes455/545 changed statements (83.49%) and183/199 tracked security branches (91.96%); cumulatively versus completed M2fe0b86cb it passes3732/4373 (85.34%) and1021/1050 (97.24%). Application/E2E types pass. Logs: `/tmp/m4-capture-final-{coverage,cumulative-coverage,types}.log`. The capture PR is being retargeted to integration for comprehensive preflight while TST-005 finishes; its actual merged dependency must still be reconciled and every final-head gate must pass before merge.

Next: finish qualifying and merge TST-005 with mandatory platform protection, reconcile CAP-003 with actual integration, then close functional CAP-003/CAP-004 acceptance. Electron **43.4.0**, pinning policy and all invariants remain unchanged. Live enterprise/OS QA and packaging/release work retain their agreed later scope. No user access or product decision is needed.

## Earlier CAP-003 composed validation

Composed capture head `7e1fe22f` is published as [draft PR #60](https://github.com/adamlow-wire/wire-desktop/pull/60), stacked on TST-005 for preflight. Local validation passes112 Jest,964 native,4 media,23 renderer and51 build-tool cases plus types/build; all20 product scenarios pass initially with retries disabled (1.1 minutes). Fresh clean coverage against integration2fd passes460/550 statements (83.64%) and183/199 security branches (91.96%); cumulative M3/M4 versus completed M2 passes3732/4373 statements (85.34%) and1021/1050 security branches (97.24%). Logs: `/tmp/m3-m4-composed-{final-test,native,media,renderer,bin,product,coverage,cumulative-coverage}.log`. The first aggregate native invocation failed before tests due to inherited `ELECTRON_RUN_AS_NODE`; clearing it allowed all native suites to execute, and that failed invocation is retained.

[Capture native preflight](https://github.com/adamlow-wire/wire-desktop/actions/runs/34963994722) passes Linux but exposes a macOS fixture assertion before native hide-event delivery. The scoped test correction awaits the actual parent hide event, then still requires immediate chooser destruction and a denied capture outcome. Removing only production parent-hide revocation fails that corrected assertion locally (`/tmp/m4-capture-hide-sensitivity.log`); production code is restored. This is not a runtime policy change or a waived denial. Final capture-head gates remain required after dependency integration.

## Earlier CAP-003 local qualification

**M3 runtime acceptance is complete.** Reviewed [PR #56](https://github.com/adamlow-wire/wire-desktop/pull/56) merged as `45840fca7eea1a4975e5e1f58bd54c6c2d911289`; local integration is clean and tree-equal to fully qualified `5c596187`. The [M3 acceptance audit](m3-acceptance.md) maps all sixteen work items and every criterion to code, review and final-head evidence. The documentation-only closure [PR #57](https://github.com/adamlow-wire/wire-desktop/pull/57) is merged as `2fd0d390b0f6f6880c4dcaef316503708125ee08` after exact-head review and applicable checks passed. It records the already executed M3 closure checkpoint and reuses the identical qualified runtime’s full E2E.

The actual merged M3 integration head `2fd0d390` also passes its automatically triggered [build/test](https://github.com/adamlow-wire/wire-desktop/actions/runs/34945856099), [lint](https://github.com/adamlow-wire/wire-desktop/actions/runs/34945856086), [CodeQL](https://github.com/adamlow-wire/wire-desktop/actions/runs/34945856192), and [Windows/Linux/macOS native/package jobs](https://github.com/adamlow-wire/wire-desktop/actions/runs/34945856138). All three native jobs pass on attempt1. This is additional merged-head evidence; it does not erase the earlier PR Windows failure/recovery.

Complete local validation passes 112 Jest, 923 main, four media, four renderer and 38 build-tool cases plus types/build. Whole-M3 coverage from completed M2 passes 3245/3830 changed statements (84.73%) and 840/853 tracked security branches (98.48%). [All native platforms](https://github.com/adamlow-wire/wire-desktop/actions/runs/34913964577) pass initially, including 192 account and 11 product cases each. [Full E2E/report](https://github.com/adamlow-wire/wire-desktop/actions/runs/34913964586) passes 48/platform: macOS 48 initial; Windows 47 initial/one retry, no skips or worker errors. The Windows trace exposes missing fixture username setup; TST-005 owns the correction. The startup redirect defect itself is fixed with sensitive native and product tests.

Electron remains **43.4.0**. The approved pinning policy remains unchanged: built-in pins, existing explicit native process-wide override until restart and mandatory Chromium validation; no MDM pinning policy exists. Its broad override remains an independent-review concern. Live SSO/Keycloak/OIDC/ACME enrolment, verified-device restart and renewal remain unrun downstream QA in [qa-sso-e2ei.md](qa-sso-e2ei.md), not an M3 compatibility claim. Packaging and independent release review remain later work.

The maintainer explicitly approved publication of the seven-file M3 documentation commit `31683711`; the exact payload is now published in PR #57. Build/lint/CodeQL and Linux/macOS native jobs pass. Windows attempt1 failed first in account-controller teardown, followed by native/filesystem setup timeouts and a secondary undefined-path cleanup error; unchanged-head attempt2 passes all192 account,473 boundary and11 product cases plus package smoke. The reviewed documentation PR is merged; its skipped E2E jobs are not treated as passes and the identical-runtime full M3 checkpoint remains explicitly linked. No root cause is claimed for the initial timeout, no timeout/assertion is weakened, and the failed attempt remains evidence.

Functional TST-005 now has local implementation: valid independent fixture handles; checked mutation responses; partial account/team setup cleanup that preserves both setup and cleanup errors; a Linux Playwright project with private native Secret Service; required modernization E2E scheduling; failed-attempt trace retention and reliable native teardown; explicit Linux unread-state observations plus native tray coverage; and a reproducible four-schema binary-client generator with an E2E type gate. Separate baseline commits `8ae22780` and `ba2328a2` demonstrate seven response/handle failures and six partial-cleanup failures before correction. All fourteen fixture cases pass after restoration. The actual generator has thirteen passing cases, the whole build-tool suite has 51, both application/E2E types pass, and the private keyring runner has three passing isolation/failure/termination tests. Local logs: `/tmp/m4-partial-setup-restored.log`, `/tmp/m4-types.log`, `/tmp/m4-bin-tests.log`, `/tmp/m4-linux-runner-repo-tests.log`. Full rebuilt validation passes 112 Jest, 923 main, four media, four renderer and 51 build-tool cases plus both type gates (`/tmp/m4-functional-local-test.log`). All eleven Linux product lifecycle/storage/proxy/navigation/E2EI cases pass initially in25.1 seconds through the new private runner (`/tmp/m4-linux-product-final.log`); fourteen fixture cases also pass under the Linux project. Scoped application/tooling lint and workflow YAML parsing pass. Hosted three-platform qualification is still required. No functional M4 item is marked complete yet.

TST-005 local implementation is committed as `3e4c2d26`; no hosted M4 result is claimed. CAP-003 is now implemented locally and under qualification on its stacked scoped branch. [Proposed DEC-011](decisions/0004-consented-display-capture.md) records the compatibility change: bridge version2 removes legacy enumeration; account/PiP native capture stays denied; an isolated local source chooser grants only main-selected video and relays bounded frames. PiP receives only the capture adapter and must retain its exact parent account document. Separate baselines `c1207ac9` and `fd62450c` expose legacy bridge selection and the real product child surviving parent navigation.

Local native qualification passes 52 capture/parent/preload/local-resource cases, including explicit selection, original/clone Stop, stale ownership, malformed authority/ACK/frame data, cross-account Stop denial, source loss, receiver and main-process deadlines, a Stop window independent of hidden owners, and bounded pending native enumeration (`/tmp/m4-capture-reviewed-native.log`). The three real product cases (account sharing, detached-call sharing, parent-navigation closure) pass twice without retries after waiting for the actual startup window to be visible (`/tmp/m4-capture-product-visible.log`). Synthetic owned frames avoid host display capture. A static-source edge case was corrected by repeating only the latest approved pixels under rate/backpressure bounds; native color conversion is allowed by the pixel assertion.

The full rebuilt functional validation passes 112 Jest, 956 main, four media, four renderer and 51 build-tool cases plus application/E2E type checks (`/tmp/m4-capture-full-validation.log`). Scoped capture/wiring/product lint and CI YAML checks pass. All three native CI paths now select the capture policies and product regressions. All fifteen combined Linux product regressions pass initially in47.1 seconds (`/tmp/m4-functional-product-combined.log`). Removing the source-selection approval guard changes a denied result to a started stream and fails its regression; restoration passes. Removing the frame dimension cap fails its target; all nine restored contract cases pass, including uppercase UUID rejection (`/tmp/m4-capture-{approval,frame}-sensitivity.log`, `/tmp/m4-capture-approval-restored.log`, `/tmp/m4-capture-contract-restored.log`). Direct renderer qualification now adds eighteen cases for real generated tracks, test-owned canvas capture, source-label text, cancellation/late denial, malformed frames/ACKs, fixed preload operations and subframe denial; the complete renderer suite passes22. Six additional native cases pass for pending/active revalidation, identity revocation, same/cross-document navigation, parent hiding, invalid metadata and the actual enumeration mapping. Temporary removal of the renderer width cap and replacement of literal labels with HTML each fail their intended assertions; both files are restored and all22 renderer tests pass (`/tmp/m4-capture-renderer-{sensitivity,restored}.log`). No production test hook or security exception was introduced.

The first coverage run exposed that electron-mocha instruments renderer source without saving its counters before closing the renderer. The coverage-only Mocha teardown now writes those counters into the configured nyc directory before completion. Combined collection passes455/545 changed statements (83.49%) and184/199 tracked security branches (92.46%) against M3 integration (`/tmp/m4-capture-combined-diff.log`). A fresh clean coverage run on committed `e9210732` passes112 Jest,962 main and22 renderer cases and reproduces both gate results (`/tmp/m4-functional-clean-coverage.log`). Cumulative M3+functional-M4 coverage from completed M2 also passes3726/4368 changed statements (85.30%) and1022/1050 tracked security branches (97.33%) using those same clean counters (`/tmp/m3-m4-combined-coverage.log`). Application/Mocha/E2E type checks, code lint, scoped tooling lint and tracked-file formatting pass. The broad formatting glob also finds ignored generated locale/test-profile JSON after local builds; those generated files are not modified to satisfy source formatting. Hosted final-head qualification remains required. CAP-004 source review identifies existing account-owned tray/menu/badge/notification routes and tests; its final hosted functional evidence is pending (`/tmp/m4-cap004-review.md`).

The secondary account-log fixture error is reproduced locally by a controlled setup failure: the original fixture reports two failures, including an undefined cleanup path. TST-005 now resets its directory ownership and cleans only after creation succeeds. The injected setup failure remains the sole failure; after restoring normal setup, all six native cleanup cases pass and lint passes (`/tmp/m4-log-fixture-{baseline,controlled,restored}.log`). This narrow fixture repair does not claim to fix the Windows native teardown timeout.

Additional call-transport qualification passes: a generated approved stream crosses a real local WebRTC peer connection and produces the selected synthetic pixels at the receiver. The peers use no external ICE servers and no media devices. Replacing frame contents with empty pixels fails the decoded-video assertion; restoration passes. The complete renderer suite now passes23 cases, with application/Mocha test types and scoped lint passing (`/tmp/m4-capture-rtc-{renderer,sensitivity,restored,types,lint}.log`). This adds transport evidence without changing production code or claiming live customer/OS capture compatibility.

The maintainer has explicitly approved functional M4 publication. Both prepared branches are published; [TST-005 PR #58](https://github.com/adamlow-wire/wire-desktop/pull/58) targets integration. Its first [E2E run](https://github.com/adamlow-wire/wire-desktop/actions/runs/34947030896) exposes a Linux setup-order failure before tests: Electron43 installs its binary on first use, so the sandbox helper does not exist immediately after dependency installation. TST-005 `4ba052a2` explicitly invokes the pinned package’s installer before configuring the helper; sandbox enforcement and all assertions remain unchanged. Linux setup now passes in the new final-head run. The failed attempt is retained. This capture branch includes the subsequent TST-005 `3b18e8fc` trace/restart and Linux menu repairs, retaining both capture and fixture cases in all native platform gates.

The next exact-head run at `4ba052a2` passes [build/test/coverage](https://github.com/adamlow-wire/wire-desktop/actions/runs/34947671145), [lint](https://github.com/adamlow-wire/wire-desktop/actions/runs/34947671121), [CodeQL](https://github.com/adamlow-wire/wire-desktop/actions/runs/34947671088) and [all native/package platforms](https://github.com/adamlow-wire/wire-desktop/actions/runs/34947671182), all on attempt1. Its [full E2E/report](https://github.com/adamlow-wire/wire-desktop/actions/runs/34947671243) exposes the shared fixture stopping tracing on the original closed context after an intentional language restart: Windows/macOS each have61 passes and that one failure. Linux has59 initial passes, the same restart failure, a wrong Settings/Preferences menu expectation, and one notification case passing on retry. These are retained failures, not accepted qualification.

Separate baseline `ab68fb3f` reproduces the restart teardown failure locally. The scoped fixture repair saves one trace per Electron instance before close/native Quit, retains every instance on failure, and forwards cleanup through repeated restarts. Real loopback tests cover native Quit, three archives, latest-instance closure and propagated trace errors with completed native teardown. Omitting tracing on relaunched instances fails the archive-count assertion; the restored three new cases and three existing launcher cases pass6/6, with E2E types and scoped lint passing. Local evidence: `/tmp/m4-trace-restart-{baseline,restored,sensitivity,final,types,lint}.log`. No production runtime, security policy or timeout change is made. Separate baseline `7b6a09d5` also reproduces Linux’s wrong Settings lookup through the helper used by authenticated E2E. That helper now selects Settings only on Windows and Preferences on macOS/Linux, preserving native menu behavior and accelerator assertions. The four short fixture regressions run in every native platform gate as well as full E2E. The expanded local product selection passes15/15 in50.2 seconds, E2E types/scoped lint pass, and workflow YAML/formatting pass (`/tmp/m4-tst005-product-final.log`, `/tmp/m4-tst005-followup-{types,lint}.log`). Renewed final-head hosted qualification remains required.

Next executable work: qualify and merge this scoped TST-005 PR, reconciled with M3 closure integration `2fd0d390`; require all protected/native/full Windows/macOS/Linux E2E/report checks and strengthen/read back mandatory platform protection. CAP-003 implementation and local qualification are preserved on `cap/CAP-003-consented-display-2026-09-15` (`3a851acd`), including sensitive native/renderer/product tests and clean changed-code coverage; integrate that existing work next. CAP-004 functional acceptance uses its reviewed existing account-owned OS integration and final hosted menu/notification results. Packaged-artifact coverage remains the open packaging portion of TST-005, outside the functional handoff.

## Historical checkpoints

The following dated checkpoints retain prior evidence and investigations. They do not override the accepted audit or current execution above.

## Completed account cutover

Reviewed head `78231f74e2fafb62f8f0cfe9c3f0e3fceccce438`, base `d94253c9`, passed:

- [Build/coverage](https://github.com/adamlow-wire/wire-desktop/actions/runs/34833701006), [lint](https://github.com/adamlow-wire/wire-desktop/actions/runs/34833701008), [analysis](https://github.com/adamlow-wire/wire-desktop/actions/runs/34833701083), and [Windows/macOS/Linux native/package gates](https://github.com/adamlow-wire/wire-desktop/actions/runs/34833701114).
- [Authenticated E2E/report](https://github.com/adamlow-wire/wire-desktop/actions/runs/34834177940): all forty cases pass per platform. macOS has 39 initial passes/one retry (multi-account notification login page closed); Windows has 39 initial passes/one retry (logout login readiness). No skips or worker-teardown errors. Windows early storage/metadata gives two passes without retry.
- Substantive source review covers profile validation/atomic persistence, partition uniqueness, native lifecycle/preferences, sender revalidation, shell/account IPC, preload compatibility, exact-target cleanup and localized cancel-default backend approval. No unresolved review threads. Source contains no production `<webview>`, `allowpopups` or enabled webview tag.
- Native lifecycle/hostile-renderer evidence covers resize/focus/hide/show/crash/reload/add/remove/switch, cross-account cookies/storage/IPC, targeted cleanup/retry and cold restart. Permission dependency [PR #48](https://github.com/adamlow-wire/wire-desktop/pull/48) was separately reviewed and fully qualified before merging into #47. Native dialog cancellation, per-scope media/notification allow/deny, foreground/document revalidation, abort/revocation and subframe/unknown denial pass on supported platforms; product permission and authenticated calling paths pass.

The metadata fixture schedules normal native `app.quit()`, requires zero exit/no signal, then disposes its test context. All metadata/identity/partition/cookie/restart assertions and existing ten-second expectation/ninety-second test budgets remain. Five Linux repetitions pass; a one-time quit veto fails the explicit exit assertion, and restoration passes. The earlier [b46 E2E run](https://github.com/adamlow-wire/wire-desktop/actions/runs/34828619874) failed Windows during context cleanup despite a retry pass; it was not accepted. This does not claim a proven Playwright root cause.

The permission review reproduced unconsented thumbnail enumeration with an inert counter and removed its account capability. The target now rejects with zero enumeration before/after device consent. No host screen was captured. Real context-isolation and consent-abort perturbations fail their corresponding targets and are restored. Display enabling belongs to CAP-003/M4 in the authoritative register; later notes had incorrectly promoted a chooser to an M3 blocker. This correction changes no criterion or invariant.

## Earlier acceptance reconciliation

| Open item | Integrated/prepared work | Next acceptance evidence |
| --- | --- | --- |
| SEC-008 | Done: PRs #38/#47/#49 integrate navigation/popup/SSO/PiP/external policy and fail-closed managed destination approval. Final49 native/package and authenticated gates pass. | No remaining M3 acceptance; live provider flows remain CAP-002. |
| CAP-005 | PR #49 integrates registry fail-closed policy and exact challenged-session proxy behavior with all final gates. Existing #52 adds certificate callback/save safety and pending/rejected proxy-state ordering. | Q-005 retained policy is qualified and merged in #54. Supporting configuration/startup diagnostic regressions pass in merged PR #55. |
| SEC-010 | Done: PR #50 merged18235a5a after reviewed final-head native/package and authenticated E2E/report gates. DEC-010 accepted. | No remaining M3 protocol criterion. |
| CAP-006 | Done: reviewed PR #51 mergedc74d116e, with actual second-Electron delivery, primary ownership, zero loser exit, no stale writes and preserved exact installed-Squirrel handling. | Lifecycle and non-secret action/settings/startup diagnostics pass in merged PR #55; no remaining CAP-006 criterion. |
| SEC-013 | Done: PRs #39/#38/#47/#51 combine strict parsing/external denials and authorized startup/running/second-instance selected-account dispatch. | Final lifecycle gates pass; no remaining criterion. |
| CAP-002 | Done: PRs #43/#53 qualify isolated SSO, backend verdicts, one-use/cookie/account isolation and sensitive E2EI transport on supported platforms. | No remaining automated M3 criterion. Live SSO/E2EI remains downstream QA; no live compatibility claim. |

## CAP-005 source and qualification

Registry failures were classified as unconfigured, allowing CLI/per-user/default/saved endpoint fallback. Separate baseline `24cd61b4` fails both targets; fix `527d09b4` treats unavailable/read-failed policy as configured-invalid while preserving genuine absence. Reintroducing the old classification fails both tests and restoration passes. Thirteen reader/environment tests and 47 combined backend/IPC tests pass locally. The real Windows fixture uses only a unique test-owned HKLM leaf and the actual registry module, reader and destination/approval functions; it removes only that leaf.

A real two-account HTTP proxy/native-prompt test separately reproduced an unrelated default-session routing change. Baseline `5da7c871` precedes fix `79255557`: both system credentials and prompt submission now apply to the challenged web contents, and cancellation reloads that view. Authentication succeeds while the unrelated session remains `DIRECT`. Restoring the old binding fails that exact assertion; restoration passes. Sixty-seven focused native proxy/backend/IPC cases pass. This defect also exists on pre-cutover integration `d94253c9`; it was not introduced by #47.

Further review reproduced two credential failures with real HTTP challenges: an environment proxy without credentials never opened the prompt, and credentials for a different proxy were transmitted to the challenged proxy (31 requests in the isolated fixture). Separate baseline `dbb23364` records both failing targets. The candidate matches host/port before automatic authentication and prompts for missing/inapplicable credentials or a settings-reader failure. Direct tests also require failed session setup to cancel authentication without saving proxy state. The native fixture retains its successful request and unrelated `DIRECT` assertions. Restoring the original handler fails both targets again; restoration plus metadata restart gives four product passes in 9.6 seconds. All 21 direct proxy tests, app/Mocha types and changed-source lint pass. A one-time quit veto fails the unchanged ten-second native-exit assertion. Local evidence: `/tmp/m3-proxy-extra-{baseline,sensitivity,restored-product,native}.log` and `/tmp/m3-metadata-quit-dispatch-sensitivity.log`.

[PR #49 native run at b241e1b2](https://github.com/adamlow-wire/wire-desktop/actions/runs/34838496368) passes Windows (including actual registry guard removal/restoration and packaged managed/proxy startup) and Linux. macOS fails before packaging: metadata test timeout plus worker teardown error, despite a passing retry. The new Darwin package builder remains unqualified. [Build/coverage](https://github.com/adamlow-wire/wire-desktop/actions/runs/34838496247) fails changed-code coverage at 5/8 statements; lint and analysis pass. None of these incomplete gates authorizes merge. The candidate adds direct proxy decision tests and retains native product failure traces. A local run then reproduced normal quit closing the inspector before its reply; fixtures now dispatch quit without awaiting that reply and retain the existing native zero-exit assertion and budgets. This is evidence for the local race, not proof of the macOS timeout's cause.

At `42b0ff6a`, [native/package run 34840186103](https://github.com/adamlow-wire/wire-desktop/actions/runs/34840186103) passes Windows and macOS. macOS gives five product passes without retry (17.9 seconds), including metadata normal quit/restart and all three proxy scenarios, then successfully builds the Darwin x64 package, verifies its code seal/fuses, and passes unmanaged/managed startup and authenticated HTTP proxy traffic. Linux fails earlier in the existing consent-hide case: two native account creations and the cancellation assertions shared a two-second budget. The candidate moves only native initialization into a ten-second setup hook; consent request, hide/reselect, abort and both denied-permission assertions keep their original two-second body budget. Removing real hide cancellation fails the abort assertion; restoring it passes.

[Build at 42b0ff6a](https://github.com/adamlow-wire/wire-desktop/actions/runs/34840186099) fails changed coverage at 21/30 (70%). The candidate now directly tests the event binding, real prompt coordinator and original challenged-view/session association, including ordinary server-auth non-interception. Removing the native-close reload fails its target, then restoration passes. Forty-three combined native proxy/view cases pass. These changes require fresh final-head gates; the successful Windows/macOS results above are not reused as final evidence. Local coverage first exposed a missing generated `resources/macos/Info.plist.json` in the temporary checkout; copying the same configured file from the root checkout restores the exact build-tool test (the other 111 Jest tests had passed).

The external startup driver runs a normal executable with a fresh profile and fixed loopback page, without an inspector, injected preload or production test flag. It checks account identity, immutable exact App-lock policy and absent page Node globals. CI repeats after creating a test-owned OS backend value, preserving existing policy and removing only the created value. The second launch uses a real local HTTP 407 challenge and synthetic credentials confined to the child environment. Local missing-configuration and canceled-native-authentication perturbations fail; restoration passes. This proves backend/proxy compatibility, not MDM deployment or every enterprise proxy variant. Cleanup terminates only its own process tree; graceful quit is covered separately.

- [Run 34835432389 at 8f4447e7](https://github.com/adamlow-wire/wire-desktop/actions/runs/34835432389) passes Linux, including actual packaged unmanaged/managed policy and authenticated HTTP proxy startup. macOS and Windows fail earlier native fixture setup. Repairs at `140e1b88` separate native initialization from unchanged two-second denial assertions, preserve the controller's ten-second setup budget, add phase diagnostics and handle partially initialized cleanup. Exact local groups pass 26 secure-shell and 173 account cases. An intentional setup failure produces only the intended failure; restored target passes.
- [Run 34836268540 at 140e1b88](https://github.com/adamlow-wire/wire-desktop/actions/runs/34836268540) passes Linux again and the repaired macOS native suites. Windows detects the deliberately removed registry guard exactly as expected, then its verifier fails decoding UTF-8 output as cp1252. Fix `fc3a962c` specifies UTF-8; the actual downloaded log reproduces the old codec error and preserves the expected assertion with the fix. The restored Windows guard remains pending.
- That macOS run builds the MAS variant and attempts unavailable distribution identities; `--version` takes 110 seconds and the functional driver receives no report within 45 seconds. [Electron's MAS guide](https://github.com/electron/electron/blob/main/docs/tutorial/mac-app-store-submission-guide.md) requires development signing/provisioning for MAS testing. The new CI-only Darwin x64 builder reuses package/fuse functions, applies/verifies an ad-hoc development code seal after fuse writes, and verifies all fuse values are unchanged. Types/lint pass; native functional evidence is pending. The default MAS/release builders remain untouched. This does not claim the exact previous startup stall is proven.

Commands/sensitivity details are in testing.md and `/tmp/m3-{registry-fallback,proxy-session,packaged-driver,packaged-proxy,native-fixture,macos-smoke-build}*`. Local logs do not replace final durable CI evidence.

## Existing integration queue

| Candidate | Current local preparation |
| --- | --- |
| SEC-010 `bbda8530` | Preserves original protocol source exactly with account `78231f74` and CAP-005 `140e1b88`; seven fixed role-specific resources, GET/HEAD, exact session/document identity and conditional script-disabled legacy reader. Builds/types/lint pass, 40 native protocol/proxy cases and three storage/metadata/proxy product cases pass, normal authenticated-proxy startup passes; updated secure-shell fixture gives 27 passes. Adopt later CI/closure records when integrating. |
| CAP-006 `e87dab1a` | Preserves original lifecycle delta exactly atop that protocol composition. Builds/types/lint pass, 42 native routing/window plus 19 ownership/Squirrel/relaunch cases pass, real Linux two-process storage/restart gives one pass in 6.1s. Native CI explicitly selects these contracts. Native Windows/final gates remain required. |
| Certificate `ad1211cd` | Characterization and fail-closed callback/error handling exist; process-global manual pin override remains. Q-005 is unresolved. |
| E2EI `44f8a9ed` | Native navigation/SSO fixture repairs are integrated through #48; remaining candidate is transport-only and does not prove issuance/renewal. |

## Audit, limitations and required inputs

The September 14 restart independently checked code, history, worktrees, PRs, checks and dedicated-provider secret names. Initial integration was `d94253c9`; #47 was draft/behind local work with E2E skipped. Live upstream dev was `d2124109`, seventeen commits beyond recorded `6f9b6a99`; no unrelated upstream synchronization was included. The established fork is public `adamlow-wire/wire-desktop`, with authenticated admin/push access. An automatic publication review initially questioned the destination; read-only ownership/PR verification resolved it and publication/dispatch was approved.

A private temporary D-Bus/GNOME keyring supplies encrypted safeStorage for local authenticated Linux tests without modifying the host keyring or enabling plaintext storage. Supplemental Linux full E2E is not wholly green: Unity is unavailable for badge assertions. Historical `--project=macOS` selectors do not make Linux execution macOS evidence. App, Mocha and build-tool types pass; standalone Playwright types retain nine existing errors outside changed fixtures.

Two questions sent September 14 remain pending; do not repeat them or infer approval from elapsed time:

- Q-005: remove manual pinning override (recommended), or retain exact-certificate/hostname/account-session restart-cleared exceptions. Chromium verification errors remain denied either way.
- Q-011: dedicated staging SSO/E2EI team, OIDC and ACME nonsecret configuration plus approved credential location, or authorization to provision an isolated test team. No shared/production identities or credentials in source/chat.

The sibling webapp uses OIDC `shouldBeRedirectedByProxy`, but deployed same-origin proxy configuration is not established. Foreign account OIDC navigation remains denied. Provider compatibility needs implementation and live evidence; webapp/core retain OIDC, ACME and cryptographic ownership.

## Operating rules

One primary work item per PR; integration PR-only; substantive review and all applicable final-head checks before self-merge. Preserve user changes, the temporary existing-branch checkout `/tmp/m3-cap005-packaged`, protected `wrap/worktrees/wpb-5221-deployment-audit` and MSI artifacts. No broad cleanup, `clear:wrap` or local `build:prepare`. Native GUI suites run serially; TypeScript then bundle precede product tests. No host device capture, trust-store changes or weaker invariants for a pass. Signed installers/updates and independent release review remain M4–M6. M3 is complete only after every remaining criterion is proven on the final integrated head.

At `2fd2f5d1`, [build/coverage](https://github.com/adamlow-wire/wire-desktop/actions/runs/34841078140), lint/analysis and the Linux native/package gate pass. [Native run 34841078114](https://github.com/adamlow-wire/wire-desktop/actions/runs/34841078114) fails Windows before product/package steps: 168 pass, five different `AccountViews` lifecycle/storage tests hit Mocha's default two seconds. The actual registry guard-removal/restoration stage passes. The candidate gives this real-native integration suite a bounded ten-second test/hook budget instead of continuing one-case setup repairs. All assertions/actions remain, the existing crash-recovery limit stays fifteen seconds, and pending-consent bodies explicitly keep two seconds with ten-second setup. This changes test-runner timing only; no production behavior, authorization, skip or retry changes. Final native and authenticated E2E/report gates remain mandatory.

The existing protocol candidate is preserved at `5fca2fba`, reconciled with `2fd2f5d1`. It passes builds/types/lint, 58 focused native cases and five real account/proxy product cases (15.0s). Renew its base with subsequent CAP-005 changes after #49 qualifies; no new branch was created.

The native-budget change passes all 19 account-view cases in 2 seconds after a completed TypeScript/bundle build; Mocha types and changed-test lint pass. An earlier local invocation overlapped preload generation and timed out in the production-preload notification target; it is not accepted as qualification. The serial post-build run is `/tmp/m3-native-account-budget-after-build.log`. No production source changes in this follow-up.

`fd3c5c35` passes final Windows and Linux native/package gates plus build/coverage/lint/analysis. macOS fails metadata normal quit again: the first quit inspector evaluation returns in about seven milliseconds, but native exit remains null at ten seconds; the second inspector request never completes. The retry passes, but the worker teardown error keeps [native run 34841929525](https://github.com/adamlow-wire/wire-desktop/actions/runs/34841929525) failed. The retained trace is artifact `modernization-native-test-failure-macOS` (ID10346557648); `/tmp/m3-fd3-macos.log` and `/tmp/m3-fd3-macos-failure/` preserve local diagnostics. This is not proof of a specific Electron or Playwright cause.

The diagnostic candidate retains a bounded native stdout/stderr tail, logs before-quit/will-quit/content destruction, and samples its owned process on macOS failure. Only after the native exit assertion fails does cleanup terminate that fixture's child; the original failure is rethrown, preventing a worker hang from masking it. A quit veto still fails the ten-second assertion with these diagnostics and no worker-teardown timeout; ten local repetitions pass without reproducing the macOS stall. A bounded manual native-workflow repetition input (1 or5; normal PR default1) permits a five-repeat probe from a temporary diagnostic tag, leaving authenticated `fd3c5c35` E2E run34841959582 active. The metadata file explicitly disables retries so successful failure cleanup cannot turn a native-exit defect into a green flaky result. No production source or permission policy changes.

Existing protocol/lifecycle preparation is current at `e31ff276` and `534a2187`; lifecycle composition passes61 focused native cases and the actual Linux two-process flow in5.1s. These branches remain unmerged until their dependency and own final-head gates qualify.

### Native quit alert diagnosis

The five-repeat diagnostic run [34843742838](https://github.com/adamlow-wire/wire-desktop/actions/runs/34843742838) at `06c9ae3c` passes Windows and Linux native/package gates. macOS records 21 product passes, three failures, one flaky case and two worker errors. Metadata failures reach `will-quit`; both retained macOS process samples show the main thread inside `NSAlert runModal`, reached from Node timer/microtask execution. This establishes a synchronous alert during shutdown, not its error or caller. The next diagnostic logs uncaught exceptions and synchronous dialog text while preserving the original alert behavior and all native-exit assertions. No production workaround is made. Artifact `10347636427` retains the samples; local `/tmp/m3-mac-quit-sample-{0,1}.txt` and `/tmp/m3-06c-macos-probe.log` contain the evidence.

The existing certificate candidate is reconciled at `701e53db` with fd3 dependencies. Build/types/lint pass, but current native certificate qualification is 21 passes/one failure (an extra verification callback in the non-throwing-dialog TLS fixture); it remains unqualified and Q-005 remains pending. Existing protocol `e31ff276` and lifecycle `534a2187` candidates retain their local passing dependency qualification. Next: identify and fix the native alert from actual evidence, complete PR #49 final-head gates, then integrate the existing candidates in order.

The alert-logging probe passes changed-source lint and one rebuilt Linux metadata product case (3.1 seconds), retaining native zero-exit and all persistence assertions; standalone Playwright types report only the nine previously recorded generated/mock declarations. Evidence: `/tmp/m3-native-alert-{build,lint,types,product}.log`. Final fd3 [authenticated E2E/report](https://github.com/adamlow-wire/wire-desktop/actions/runs/34841959582) is now successful: macOS 43 initial passes; Windows 41 initial passes and two retry passes (logout and multi-account notifications), no skips or worker errors. macOS native quit failure still prevents PR #49 merge.

### Delayed native window visibility baseline

Independent source inspection finds an unguarded `main.show()` timer at 800 ms. Extracting that unchanged behavior into `WindowUtil.showAfterStartupDelay` permits direct real-window characterization. The existing-delay positive target passes; destroying the real native window before advancing the timer fails with `Object has been destroyed` (`/tmp/m3-native-startup-delay-before.log`). This proves a shutdown error path; it does not yet establish that the macOS alert is this error. Native alert diagnostic [34845940040](https://github.com/adamlow-wire/wire-desktop/actions/runs/34845940040) runs at `3f0101ba` on the fork. A briefly misdirected diagnostic tag was removed from upstream with an exact-SHA lease; only the intended fork tag remains. No upstream branch or release changed.

The existing certificate candidate is now `38421619`: the isolated TLS fixture passes five 22-case runs and the reconciled full native suite passes 856/856. Q-005 and its final hosted gates remain open. Its earlier extra callback was a Chromium background request, not a repeated completion of the rejected loopback verification.

The delayed-show repair checks the same native window's destruction state before calling `show`, retaining 800 ms and all startup conditions. Both native targets pass; temporarily changing the delay to 799 ms fails the positive target, and removing the guard fails the destruction target with the original exception. Both mutations are restored. Rebuilt product qualification passes **12/12 metadata/restart/proxy cases in 36.3 seconds** on Linux, plus application/Mocha types and changed-source lint. Evidence: `/tmp/m3-native-startup-delay-{before,after,timing-sensitivity,guard-sensitivity,restored,product}.log` and `/tmp/m3-native-startup-delay-fixed-{build,types,lint}.log`. The macOS alert probe is still running; no root-cause or final-head macOS pass is claimed yet.

### Confirmed shutdown cause and native package architecture

The macOS alert probe [34845940040](https://github.com/adamlow-wire/wire-desktop/actions/runs/34845940040) confirms the exact quit failure twice: `Object has been destroyed` at `electron/dist/mainProcess.js:447:31`, emitted by the 800 ms `main.show()` timer. Re-emitting exact `3f0101ba` source maps line447 to that call. Artifact10349345524 and `/tmp/m3-3f-macos-alert.log` retain the evidence. The guarded `729c8d2e` head passes all five macOS native product cases without retry (36.9 seconds), build/coverage/lint/analysis and Windows/Linux native/package gates. Fresh local aggregate passes112 Jest,836 main and four renderer cases;34/39 changed statements are covered (87.18%).

The remaining macOS failure at [34847474271](https://github.com/adamlow-wire/wire-desktop/actions/runs/34847474271) is packaged startup: the Darwin x64 binary on `macos-26-arm64` takes103 seconds to print version, then produces no account report/native output within45 seconds. Packaging, fuses and code seal passed. This does not establish a specific Rosetta/Gatekeeper cause. The CI-only builder now selects the runner's native arm64/x64 architecture and verifies the actual Mach-O architecture with `lipo`; supported release/MAS builders and all fuse values stay unchanged. The driver adds bounded owned-process failure sampling and timing/exit diagnostics while preserving the45-second deadline and every security/authentication assertion. Require fresh final-head native/package and authenticated E2E/report gates; no merge yet. Local build-tool types, lint and completed app build/bundle pass (`/tmp/m3-native-architecture-{types,lint,build}.log`).

Existing downstream candidates preserve the reviewed shutdown fix and local qualification: protocol `2c0801b4` passes56 focused native and five product cases (16.8 seconds); lifecycle `604c33d5` passes63 native and one actual two-process case (5.4 seconds). Certificate `38421619` retains five22-case focused passes and856 full-native passes; Q-005 remains open. Update these existing candidates with the final package-fixture dependency after #49 qualifies; do not recreate them.

The updated external driver passes fresh local development-binary startup and actual authenticated-proxy traffic, with unchanged immutable-policy/bridge/Node and challenge assertions (`/tmp/m3-native-architecture-driver.log`). This is Linux driver validation; the native arm64 package itself still requires its hosted final-head gate. Publishing this necessary package-fixture change starts new final-head checks, and the prior729 E2E run is not reused as qualification.

### Proxy prompt state ordering

The CAP-005 session contract also applies to prompt submission: proxy state must not change until session setup succeeds. Separate baselinea5bcb6a5 reproduces two failures: state is changed while setup is pending and remains changed after setup rejects. The fix moves only state publication after successful application to the original challenged view, retaining authentication order and coordinator retry behavior. Restoring the old order fails both baselines; restoration passes20 action/coordinator/login cases and three real proxy product cases (9.2 seconds). Completed app build/bundle, Mocha types and changed-source lint pass. Evidence `/tmp/m3-proxy-prompt-state-{before,sensitivity,restored,product,types,lint}.log` and `/tmp/m3-proxy-prompt-state-fixed-build.log`. No credential or product-policy relaxation is added. Final combined current-base hosted gates remain required before merging this CAP-005 slice.

### Final protocol integration qualification

PR #49 final head66f9d9db passed [build/coverage](https://github.com/adamlow-wire/wire-desktop/actions/runs/34848889665), [lint](https://github.com/adamlow-wire/wire-desktop/actions/runs/34848889722), [analysis](https://github.com/adamlow-wire/wire-desktop/actions/runs/34848889654), [all native/package platforms](https://github.com/adamlow-wire/wire-desktop/actions/runs/34848889664), and [authenticated E2E/report](https://github.com/adamlow-wire/wire-desktop/actions/runs/34848889715). Both authenticated platforms pass43 cases initially, without retries/skips/worker errors. Substantive review, no unresolved review threads and strict current-base checks preceded merge4f04a8a0. Native macOS arm64 ordinary/managed/authenticated-proxy startup verifies architecture, fuses and ad-hoc seal. SEC-008's programmatic managed-destination cutover requirement now has integrated positive/negative and real-registry evidence; CAP-005 remains open for Q-005 and its separate certificate candidate.

Protocol qualification adopts only the sensitivity-proven native fixture repairs found in lifecycle preflight: ten-second aggregate account-controller bodies for multiple real view operations, and four independent secure-shell denial cases with unchanged two-second bodies, exact assertions and navigation delays. No product timeout, policy or retry changes. Skipping real account removal/cleanup fails the respective controller assertions; allowing the test-owned popup fails the split denial target. Restored local controller40 and secure-shell11 cases pass on the lifecycle composition; renew native qualification on this protocol composition. Evidence `/tmp/m3-controller-budget-{selection-sensitivity,cleanup-sensitivity,restored}.log` and `/tmp/m3-native-denial-split-{sensitivity,final}.log`.

Current integration composition passes completed app build/bundle, Mocha types, changed-source lint,81 focused native cases and five actual storage/metadata/proxy product cases (15.0 seconds). Evidence: `/tmp/m3-protocol-integration-{build,types,lint,native,product}.log`. These are local Linux checks; hosted native/package and authenticated final-head gates remain required.

## Lifecycle qualification history

Lifecycle PR #51 is reconciled with protocol candidate4f867021 while its final checks run. Preflightba38c1a7 passes build/lint/analysis and macOS/Linux native/package gates, but Windows consistently fails the actual second-process delivery assertion across all three attempts: the secondary exits0 while the selected account remains on devices instead of account preferences. This is a functional failure, not a timing-budget failure. Preserve exact delivery/exit/account-count assertions and diagnose the actual Windows event/argv before changing behavior. Evidence [34852932003](https://github.com/adamlow-wire/wire-desktop/actions/runs/34852932003), Windows job104005141428, artifact10352651247, `/tmp/m3-ba38-windows-native.log`.

Protected integration is `4f04a8a0700d388a9b1199e5905df5f2bc70b72e`, after reviewed PR #49 passed every final-head gate. The exact head66f9d9db passes build/coverage, lint, analysis, Windows/macOS/Linux native/package qualification and authenticated Windows/macOS E2E/report:43 initial passes per platform, no retries, skips or worker errors. Native macOS arm64 packaging verifies architecture, preserved fuses and ad-hoc seal, then passes ordinary and managed/authenticated-proxy startup. Durable links and the substantive review are in [PR #49](https://github.com/adamlow-wire/wire-desktop/pull/49).

The Windows handoff diagnostic records only the test-owned primary/secondary argv/profile, delivered second-instance events, exit/signal and the last16KiB of child output. Assertions and deadlines remain unchanged. The fixture does not change production dispatch or grant permission. Qualify the diagnostic on actual Windows; do not infer a parser or timing cause from a successful secondary exit alone.

The rebuilt diagnostic composition passes changed-source lint and the actual Linux two-process/storage/restart case (6.8 seconds); the primary receives the exact synthetic deep-link argv, the selected account changes correctly and the secondary exits0. Standalone Playwright types retain only the same nine existing generated/mock declaration errors, none in this fixture. Logs `/tmp/m3-second-instance-diagnostic-{build,lint,types,product}.log`. Windows diagnosis remains required.

### Actual Windows second-process launcher

Diagnostic1c919c2f [34857916192](https://github.com/adamlow-wire/wire-desktop/actions/runs/34857916192), Windows job104022164711, records three identical failures: primary argv names Electron, but secondary spawnargv begins `C:\Windows\system32\cmd.exe`, exits0 and delivers no second-instance event. Playwright's process handle identifies its Windows wrapper. The fixture now asks the running primary for `process.execPath` and launches that exact Electron executable with unchanged argv/profile. No production lifecycle or parser change is justified by this fixture failure. Preserve exact selected-account delivery, zero exit and account count checks. `/tmp/m3-1c91-windows-native.log` retains the diagnosis. Linux native/product tests pass but package download returns HTTP504; macOS native/package passes.

Protocol final head4f867021 passes build/lint/analysis and all three native/package gates. Its first Windows attempt timed out in the fake-media fixture's beforeEach at2000ms before a permission assertion ran; one unchanged-head job rerun104022719435 passes completely. Both attempts remain in [34857405317](https://github.com/adamlow-wire/wire-desktop/actions/runs/34857405317). Authenticated E2E/report34857460712 remains running; do not merge until it completes successfully.

The corrected executable fixture passes completed TypeScript/bundle, changed-source lint and the real Linux two-process/storage/restart case (6.3 seconds). Standalone Playwright types retain the same nine prior errors outside this fixture. Original Windows1c919 failure provides the baseline; actual Windows qualification is required for the repair. Logs `/tmp/m3-second-instance-executable-{build,lint,types,product}.log`.

### Native cleanup aggregate budget and current preflight gaps

Lifecycle97223344 [34859128832](https://github.com/adamlow-wire/wire-desktop/actions/runs/34859128832) passes Linux native/package qualification. macOS job104026394143 passes172 cases but the exact-target localStorage/IndexedDB/CacheStorage case exceeds its implicit two-second body limit. That case opens four actual documents, seeds/clears/reads storage and verifies the unrelated session survives; it now has a finite ten-second aggregate body budget. Omitting target.clearData still fails the exact storage readback assertion; restored source passes all six cleanup cases (811ms), Mocha types and lint. No action, assertion, production timeout or policy changes. Logs `/tmp/m3-cleanup-budget-{sensitivity,restored,types,lint}.log`.

Windows job104026393883 passes430 boundary cases but a popup-fixture beforeEach fails with ERR_NO_BUFFER_SPACE loading its loopback page. It never reaches the corrected second-process fixture, so Windows actual-Electron delivery remains unqualified. Preserve `/tmp/m3-972-windows-native.log` and `/tmp/m3-972-macos-native.log`; do not infer another dispatch defect. Next: integrate protocol #50 once authenticated E2E/report passes, merge its actual integration head into this existing branch, and require every final-head check including native Windows delivery and full authenticated E2E/report.

Current lifecycle preflight [34861051677](https://github.com/adamlow-wire/wire-desktop/actions/runs/34861051677) at7d15a032 passes all three native/package platforms, including Windows job104033043104. Its diagnostic records the actual Electron executable delivering the exact synthetic deep-link argv to the primary, exit0/no signal, and all five product cases pass initially (35.2 seconds). This proves the fixture launcher correction; no production dispatch change was required. `/tmp/m3-7d15-windows-native.log` retains the evidence. The actual integration mergea4fceebe includes18235a5a with no runtime diff from7d15; a completed TypeScript/bundle rebuild passes (`/tmp/m3-lifecycle-integration-build.log`). Final integration-head hosted gates remain required.

## Earlier CAP-005 candidate qualification

Latest CAP-005 checkpoint: PR #49 is merged as4f04a8a0 after every final-head gate passed (43 initial authenticated cases/platform). Protocol PR #50 at4f867021 is in final integration qualification; lifecycle PR #51 at1c919c2f diagnoses a Windows test launcher error (Playwright spawnfile is cmd.exe, not Electron), with its fix next. Certificate/proxy draft #52 remains on its existing branch; Q-005 certificate policy and dedicated live SSO/E2EI inputs remain pending. No CAP-005 closure or scoped certificate exception is claimed.

The existing certificate candidate now includes CAP-005 PR #49 head `66f9d9db`; prepare a scoped native/build preflight draft on that branch. The runtime fixes remain callback completion, warning-lock recovery and save cancellation. The process-global pinning override is unchanged and **not accepted as the target policy**: Q-005 still requires the product choice already requested. This draft's scoped safety fixes do not establish exception-scope or M3 completion. No host trust store is changed.

Baseline `e3041b90` precedes fail-closed callback fix `6c9be452`; verifier or dialog failures now settle denial once, and a throwing callback is never retried. Chromium success delegates with `-3`, while Chromium errors and failed pins deny with `-2`; unconditional acceptance perturbations fail. Recovery/native targets at `3a5eb9b5` preserve normal six-second cooldown while releasing the warning lock on rejection. Native loopback TLS is rejected before HTTP, including on warning failure; omitting callback fails rather than passing on timeout. Save/detail baseline and fix `a344951c` require actual non-cancellation and a nonempty selected path, preserve exact file contents and owner-bound macOS adapter calls, and retain denial on dialog failure. Write omission and wrong-platform routing perturbations fail and are restored.

Reconciliation initially exposed an extra native TLS verification callback. Diagnostic baseline `79cb322a` captured correctly denied loopback TLS plus an unrelated Chromium background request to `redirector.gvt1.com` with `net::OK`. Fix `38421619` restricts the fixture session to its exact loopback origin, retaining exact one-denial, native error details and zero-HTTP assertions. Five focused runs pass22/22 each and full native qualification passes856/856 in26 seconds; types/lint pass. Logs: `/tmp/m3-certificate-{request-repeats,isolated-native,isolated-full-main,isolated-types,isolated-lint}.log`. The current merge inherits only the reviewed shutdown/package dependencies. Its rebuilt certificate suite passes22/22, and app/Mocha types/lint pass (`/tmp/m3-certificate-stack-{build,native,types,lint}.log`). Fresh aggregate qualification also passes112 Jest,858 native main and four renderer cases; changed statement coverage against66f9d9db is25/26 (96.15%, required80%), recorded in `/tmp/m3-certificate-stack-coverage.log`. It still needs its own hosted preflight/final gates.

PR #49 [native/package34848889664](https://github.com/adamlow-wire/wire-desktop/actions/runs/34848889664) now passes all three platforms, including the actual native-arm64 package policy/proxy path. Build/coverage/lint/analysis pass; authenticated E2E/report34848889715 is still running. Protocol [draft #50](https://github.com/adamlow-wire/wire-desktop/pull/50) and lifecycle [draft #51](https://github.com/adamlow-wire/wire-desktop/pull/51) run scoped preflight on their existing branches. Strict protected-base checks require retargeting/updating each candidate and running every final-head gate before integration.

[PR #47](https://github.com/adamlow-wire/wire-desktop/pull/47) is merged into protected integration as `683ac9af672168d48c9154c47f3dc99a2bdd5d66`. CAP-001, SEC-007 and SEC-009 satisfy their acceptance criteria and are recorded done; DEC-009 is accepted for notification/media consent with display/thumbnail denial retained. **Ten of sixteen M3 work items are done; six remain open.** This is an item count, not a weighted percentage. Electron remains **43.4.0**. No invariant or acceptance criterion is waived.

### Combined CAP-005 lifecycle/protocol qualification

Existing CAP-005 candidate now includes lifecycle final head1eaf7fba (on merged protocol18235a5a); production callback/proxy fixes merge unchanged. Completed TypeScript/bundle, Mocha/build-tool types, changed-source lint,57 native certificate/proxy cases and five actual account storage/metadata/proxy product cases (15.7 seconds) pass. Evidence `/tmp/m3-certificate-final-stack-{build,types,product}.log` and `/tmp/m3-certificate-preconnect-{types,lint,restored}.log`.

The initial combined native run was56 passes/one failure: the correct loopback -2 decision was followed by a legitimate net::OK background redirector.gvt1.com verification despite HTTP-origin filtering. TLS verification can precede that filter. The fixture now denies foreign hosts directly before its production-verifier wrapper, retaining exact [-2], authority-invalid details, one dialog and zero HTTP assertions for its own loopback request. Deliberately returning -3 instead fails both exact native assertions; production source is restored and all57 targets pass. `/tmp/m3-certificate-final-stack-native.log` retains the initial finding; `/tmp/m3-certificate-preconnect-sensitivity.log` proves assertion sensitivity. No production certificate policy, callback contract, trust store or required assertion is relaxed.

Draft #52 targets the existing lifecycle branch for a scoped preflight, with that exact branch added to the four focused workflow filters. After #51 merges, retarget to actual protected integration, merge its head and require every final-head gate including authenticated E2E/report. Q-005 remains pending; current manual pinning bypass is not accepted as a completed target policy.

### CAP-005 current-base coverage qualification

Head1f776851 passes [native/package qualification](https://github.com/adamlow-wire/wire-desktop/actions/runs/34863181810) on all three platforms. Windows initially passes the saved-foreign-destination assertion but exceeds the existing ten-second afterEach limit (job104040336823); one unchanged-head retry104044298887 passes all native groups, five product cases initially (22.1 seconds), and packaging. The ten-second limit and all assertions remain unchanged. macOS/Linux pass; [lint](https://github.com/adamlow-wire/wire-desktop/actions/runs/34863182085) and [analysis](https://github.com/adamlow-wire/wire-desktop/actions/runs/34863181841) pass.

[Build34863181993](https://github.com/adamlow-wire/wire-desktop/actions/runs/34863181993) passes112 Jest,896 native main and four renderer tests, then fails its diff calculation: the event retained former base66f9d9db while the shallow synthetic merge already used current base1eaf7fba. That absent SHA is not a coverage pass. Fresh local `env -u ELECTRON_RUN_AS_NODE DIFF_COVERAGE_BASE=1eaf7fbadcd22a9a4f9c6f286ad8dd7817c6519f node .yarn/releases/yarn-3.3.1.cjs coverage` passes112 Jest,896 main and four renderer cases, with26/27 changed statements covered (96.30%, required80%). Only the temporary checkout's verified generated coverage directories were cleared. Evidence `/tmp/m3-certificate-current-base-coverage.log`, `/tmp/m3-1f77-build.log` and `/tmp/m3-1f77-windows-native{,-retry}.log`.

The evidence checkpoint triggers a fresh synchronize event while the current lifecycle base is stable; require a successful hosted current-base comparison. No workflow threshold, source behavior or assertion changes. Lifecycle final Windows E2E passes; macOS/report remain pending at this checkpoint. After #51 merges, reconcile #52 with actual integration and require every final-head gate including authenticated E2E/report. Q-005 and Q-011 remain unresolved.

Lifecycle final closure: PR #51 mergedc74d116e after every final-head gate passed. Earlier pending lifecycle notes below are historical. Final test logs `/tmp/m3-1eaf-{macos,windows}-e2e.log`; all durable links and review are in PR #51. The certificate candidate merges actual integration without runtime changes and renews its own final qualification.

## Retained pinning policy closeout

The maintainer resolves Q-005 by preserving existing product decisions, not removing the warning checkbox or adding MDM configuration. Built-in pinning starts enabled; native selection bypasses only extra pin checks process-wide until restart. Chromium validation remains mandatory and the original failed request is denied. Added tests characterize default/unselected behavior, explicit selection and broad scope, Chromium failure after selection, ignored checkbox results on Chromium errors, and fresh-module initialization. Initial27 certificate cases, Mocha types and lint pass. Deliberately allowing the selected override to skip Chromium validation fails the new security target with -3 instead of -2; source is restored. Logs `/tmp/m3-pinning-policy-{build,types,lint,native,sensitivity}.log`. Restored qualification and final integrated-head gates remain required. Broad override scope is retained for independent security review, not hidden as a new security guarantee.

CAP-002 is qualifying in PR #53 under the approved automated/live-QA split. Next finish these existing CAP-005 tests, reconcile after #53, qualify both M3 items and run the aggregate acceptance/security audit before M4 functional work. The user authorizes CAP-003/CAP-004 functional completion after M3, stopping before PKG-001 packaging.

## CAP-002 transport qualification history

The existing CAP-002 candidate `44f8a9ed` is reconciled with current CAP-005 dependency `66f9d9db`. Its prior account-navigation fixture changes are already present in that dependency; the remaining test delta is the existing80-line E2EI redirect transport fixture. The current authoritative plan already includes the approved live SSO/E2EI enrolment and renewal acceptance, so no scope change or new work item is introduced. The completed build/bundle and changed-source lint pass; existing navigation plus E2EI transport fixtures pass2/2 locally (`/tmp/m3-e2ei-reconciled-{build,lint,product}.log`). Standalone Playwright types retain only the nine recorded generated/mock declarations, with none in the E2EI fixture. Preserve this qualified candidate; keep Q-011 provider configuration/access pending and do not treat inert redirect values as enrolment.

The fixture has two owned loopback origins: unregistered-provider navigation must be canceled with zero requests, while an account-origin `/oidc` return preserves its session storage across the existing redirect shape. Earlier navigation-guard and SSO-creation perturbations fail their targets and are restored (`/tmp/cap002-e2ei-navigation-{baseline,sensitivity,restored}.log`, `/tmp/cap002-native-sso-sensitivity.log`). This is transport evidence only: no OIDC tokens/state validation, ACME issuance, verified-device state, renewal or live SSO success is established. The sibling webapp/core own cryptography and interactive/silent OIDC behavior. Q-011's dedicated staging team/provider prerequisites are still required; no shared identity or production credential is used.

CAP-002 closeout candidate: 64 deterministic native SSO cases pass; three E2EI-shaped success/cancel/interactive-required transport cases retain exact account storage/query assertions. Removing the real compiled navigation guard fails all three, then the guard is restored. All-platform CI now selects SSO window-control/account-limit contracts and product navigation/E2EI fixtures. Completed build/lint pass; four restored local product cases pass (6.4 seconds); final hosted gates remain required. Live Keycloak/OIDC/ACME QA remains explicitly unrun under the approved handoff.

Retained-policy composition includes CAP-002 final candidate43961f6c on reviewed integration784ffabe. Both approved scope decisions are preserved: downstream live identity QA, and unchanged built-in pinning/native override. Restored certificate suite passes27 cases (770ms), with source perturbation reverted. Qualify this scoped policy slice after CAP-002 integrates; no fresh-module test is labeled actual process restart.

Retained-policy preflight: the existing branch targets CAP-002 candidate43961f6c until #53 merges. Four workflow filters include that exact dependency. Combined TypeScript/bundle completes (`/tmp/m3-pinning-combined-build.log`); runtime source remains identical to integrated784ffabe. The five new native tests and restored27-case result qualify the retained contract locally. Final actual-integration checks and the complete M3 validation/audit remain required before functional M4 work.

Aggregate M3 validation on the composed runtime passes112 Jest,901 native main,4 media,4 renderer and38 build-tool cases plus types and completed build (`/tmp/m3-all-changes-test.log`). Fresh coverage across all M3 changes from completed-M2fe0b86cb initially fails the diff subprocess with ENOBUFS: the actual diff is1,452,367 bytes, above Node's default1MiB. A bounded16MiB maxBuffer fixes only collection capacity, preserving all thresholds. The unchanged fresh reports then pass3174/3758 changed statements (84.46%, required80%) and840/853 tracked security-policy branches (98.48%, required90%). Logs `/tmp/m3-all-changes-{coverage,diff-coverage}.log`; source runtime unchanged. This supplements, not replaces, final actual-integration hosted qualification and the completion audit.
