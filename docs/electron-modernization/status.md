---
project: WIRE-DESKTOP-ELECTRON-MODERNIZATION
updated: 2026-09-09
milestone: M3
active_work_item: CAP-001
state: production-account-cutover
integration_branch: integration/electron-modernization
integration_head_commit: d94253c9937c6e0bac256fc49e4980af00dd6e91
upstream_commit: 6f9b6a994500f0fc0ad64e60882ac9f5b099d5f2
fork_url: https://github.com/adamlow-wire/wire-desktop
active_branch: cap/CAP-001-production-accounts-2026-09-09
next_work_item: SEC-007
blockers: []
---

# Current project status

## Milestone checkpoint

M0, M1 and M2 exit gates are complete. **M3 is not complete.** Electron stays at `43.4.0` under DEC-007; Electron 44 is intentionally deferred. The M1 runtime gate does not imply the broader ELC-003 dependency audit is complete.

| Area | Verified state | Remaining M3 acceptance |
| --- | --- | --- |
| IPC/isolation | SEC-002–006 merged: registered authority, typed contracts, no remote dependency, isolated bridges and sandboxing | Preserve invariants during production cutover |
| Production accounts | Opt-in main-owned collection and targeted deletion; metadata identity protected | SEC-007/CAP-001 production WebContentsView, main-owned state, lifecycle routing and legacy sessions |
| Navigation/links | PRs #38/#39 merged: strict navigation, popup, external and incoming-link parsing | Main-authorized custom-backend changes and lifecycle/account routing under SEC-008/SEC-013/CAP-005/CAP-006 |
| Local shell | CSP PR #42 merged: production/development eval removed | SEC-010 custom scheme and storage migration |
| Permissions/previews | SEC-012 merged in PR #44 after all final-head gates passed | SEC-009 permission user-flow policy |
| Critical capabilities | SSO PR #43 and download containment PR #45 merged with final-head platform evidence | Controlled live IdP checkpoint, certificate/configuration policy and deep-link/single-instance parity |
| Closure | Not started | Audit every M3 acceptance criterion and final cross-platform evidence |

M3 progress estimate: **approximately 65%** (2026-09-09). This is an engineering estimate, not a measured acceptance percentage or time forecast. Progress updates include the estimate, completed acceptance, next work and blockers. Unvalidated implementation and PR counts do not increase completion.

## Merged evidence

- [PR #45](https://github.com/adamlow-wire/wire-desktop/pull/45), enforced download-path containment, merged `d94253c9`: head `858643d6` passed [build/coverage](https://github.com/adamlow-wire/wire-desktop/actions/runs/34337480573), lint, analysis, [all-platform packages](https://github.com/adamlow-wire/wire-desktop/actions/runs/34337480574) and [Windows/macOS E2E/report](https://github.com/adamlow-wire/wire-desktop/actions/runs/34337502792). The final-head Windows package job explicitly passes the real junction denial test. No outstanding review threads. This closes the download slice, not CAP-005 certificate/configuration acceptance.
- [PR #44](https://github.com/adamlow-wire/wire-desktop/pull/44), public-only preview fetches and bounded metadata parsing, merged `62435cc6`: head `9bac19b2` passed [build/coverage](https://github.com/adamlow-wire/wire-desktop/actions/runs/34333303038), lint, analysis, [all-platform packages](https://github.com/adamlow-wire/wire-desktop/actions/runs/34333303096) and [Windows/macOS E2E/report](https://github.com/adamlow-wire/wire-desktop/actions/runs/34333303173). No unresolved review threads. SEC-012 is complete; M3 remains open.
- [PR #46](https://github.com/adamlow-wire/wire-desktop/pull/46), isolated legacy-profile seeding, merged `6f256d59`: final-head build/lint/analysis, [all-platform packages](https://github.com/adamlow-wire/wire-desktop/actions/runs/34329710696) and [Windows/macOS E2E/report](https://github.com/adamlow-wire/wire-desktop/actions/runs/34329781004) pass. The metadata fixture passes without the previous seed-close teardown error; macOS has two disclosed built-in retry passes in existing multi-account flows (32 first-attempt passes). Product shutdown, timeouts and assertions are unchanged.

- [PR #42](https://github.com/adamlow-wire/wire-desktop/pull/42), CSP, merged `9c188bf1`: final-head build/lint/analysis, [all-platform packages](https://github.com/adamlow-wire/wire-desktop/actions/runs/34321374305) and [Windows/macOS E2E/report](https://github.com/adamlow-wire/wire-desktop/actions/runs/34321374718) passed naturally. Production/development startup and ordinary-script eval/Function denial have sensitivity evidence. Custom-scheme acceptance remains open.
- [PR #43](https://github.com/adamlow-wire/wire-desktop/pull/43), native SSO completion, merged `ef050e42`: [build](https://github.com/adamlow-wire/wire-desktop/actions/runs/34239220717), [packages](https://github.com/adamlow-wire/wire-desktop/actions/runs/34239220276), [E2E/report](https://github.com/adamlow-wire/wire-desktop/actions/runs/34239266392) all green. One-use callback secret, allowlist and cookie-scope mutations failed before restoration. The resolved CodeQL redirect comment concerns the deliberate loopback backend fixture; no production exemption or suppression was added. Ordinary login E2E does not prove live IdP completion.
- Earlier PRs #37–41 cover sandboxing, navigation/parser, renderer-loss test harness and metadata identity. Their final-head evidence remains in the plan and linked PRs; no claim that the production view migration is already complete.

## Active branches and next executable work

1. **[Draft PR #47](https://github.com/adamlow-wire/wire-desktop/pull/47), CAP-001**, targets integration; no stacked dependency remains. The new cutover checkpoint replaces the legacy default selected at `e6e71ce4` with native account views, main-owned profile/state and a named shell bridge; renderer JSX webviews are removed. Local lifecycle and restart regressions pass, but this cutover is incomplete and not ready to merge. Next: finish native desktop action/SSO/reload routing, strict exact-account cleanup and environment approval, then complete lifecycle/focus and final-head platform qualification. Default and `persist:UUID` sessions must remain intact.
2. **PR #44 is merged and SEC-012 is complete.** Preserve its public-only preview contract during later account integration. Prior failed heads remain diagnosis evidence, not waived checks; the synchronized final head passed every gate.
3. **PR #45 is merged.** Preserve its normalized home-relative download policy; CAP-005 still needs certificate exception policy and representative managed-config backend tests.
4. Finish SEC-010 local-scheme migration, SEC-009 permissions, SEC-008/SEC-013 follow-ups and CAP-005/CAP-006 parity. Download containment does not close certificate policy or platform managed-config backend coverage.
5. Complete CAP-002 live-provider evidence and the M3 closure audit. Do not mark M3 complete while any required acceptance remains open.

## Local validation and known failures

- **Native start-login compatibility:** the legacy `addAccountWithSession` contract has been checked in integration history: reuse an unfinished account; otherwise add below the configured limit; no account change at the limit. Native desktop dispatch now preserves those outcomes and rejects extra arguments. The target fails against the missing route, then passes with the full **711-test main suite**, zero pending. Application/Mocha types and targeted lint pass. Logs: `/tmp/cap001-start-login-before.log`, `/tmp/cap001-start-login-full.log`. No fresh rebuilt-product result is claimed for this source checkpoint. Next executable work is startup focus and ordered multi-action dispatch, then remaining account cleanup/environment acceptance and final coverage/platform gates.
- **Incoming routing full-suite checkpoint:** restored full main suite passes **710 tests**, zero pending (`/tmp/cap001-incoming-full-main.log`). No sensitivity mutation remains. Fresh aggregate coverage and final-head hosted qualification are still required before PR #47 readiness.
- **Native incoming joins/locations:** desktop dispatch now validates joins through the existing fixed account schema and validates location routes through the strict incoming-link parser. Requests capture the originally selected account, retain joins/readiness events and deliver once only after that account loads. The shared startup hash is removed from the base webapp URL. The new native positive target fails before implementation; routing its location to the other account causes the expected failure and is restored. Focused controller/protocol/window tests pass 66 tests. Rebuilt product fixtures pass **2/2 in 5.6 seconds on Linux**, now explicitly exercising a real launch argument and running-app `open-url` event with different account selections and unchanged other-account hashes. Logs: `/tmp/cap001-incoming-before.log`, `/tmp/cap001-incoming-native.log`, `/tmp/cap001-incoming-sensitivity.log`, `/tmp/cap001-incoming-product.log`. Application/Mocha types, targeted lint and builds pass; standalone Playwright types retain exactly the nine known generated-client/global declaration errors. Start-login, startup focus and multi-action ordering remain open; the legacy start-login contract is unfinished-account reuse, otherwise bounded account creation.
- **Startup queue validation:** full main suite passes **708 tests**, zero pending (`/tmp/cap001-startup-queue-full.log`); application/Mocha types and targeted ESLint pass. Removing the native-binding readiness condition causes the dedicated test to fail on an unexpected shell send (`/tmp/cap001-startup-binding-sensitivity.log`); restored. No fresh rebuilt-product or aggregate coverage result is claimed for this source checkpoint.
- **Pre-window buffering prerequisite:** recognized incoming desktop actions are now retained when the primary window or native action binding is absent, with a 32-action limit and cloned arguments. Native account initialization explicitly flushes startup work, so an SSO request does not depend on a previously logged-in webapp reporting loaded. Tests reproduce the old dropped request and missing queue bound, then verify exactly-once flushing, arbitrary-channel exclusion and retention across the window-before-binding gap. Logs: `/tmp/cap001-startup-queue-before.log`, `/tmp/cap001-startup-queue-after.log`. This does not yet close startup protocol acceptance: location/join/start-login native dispatch, legacy URL-hash removal, focus behavior and multi-action ordering remain required.
- **Final local SSO-initiation checkpoint:** after completed TypeScript/preload builds, the serial full main suite passes **705 tests**, zero pending (`/tmp/cap001-sso-final-main.log`), and rebuilt sandboxed lifecycle/metadata restart fixtures pass **2/2 in 4.9 seconds on Linux** (`/tmp/cap001-sso-product.log`). Targeted lint and application/Mocha types pass. The actual failed-destination test verifies the old native view is hidden without destroying that account. Existing lifecycle fixtures are not a live SSO or pre-window protocol proof. Next: incoming-link buffering/dispatch before the native binding exists, start-login/join/location routing, then remaining CAP-001 cleanup/environment acceptance and full final-head gates.
- **Native automated-SSO initiation:** fixed main-owned desktop dispatch now handles validated SSO codes, atomically creates or reuses an unfinished account, preserves its existing session, hides the old selection during replacement and keeps the code out of display snapshots. Account-limit failures reuse the localized native warning outside the lifecycle queue. State and native targets first fail against the missing code/dispatcher; dropping the code from controller-to-state propagation fails the expected SSO URL assertion; restored. Focused state/controller/parser suites pass 50 tests; invalid and trailing-control-character codes are denied. Logs: `/tmp/cap001-sso-state-before.log`, `/tmp/cap001-sso-native-before.log`, `/tmp/cap001-sso-native.log`, `/tmp/cap001-sso-sensitivity.log`. This is running-app initiation, not controlled live IdP acceptance or complete pre-window incoming-link routing. Final rebuilt validation follows below.
- **Join readiness validation:** full main suite passes **701 tests**, zero pending (`/tmp/cap001-join-readiness-full.log`); targeted ESLint and application/Mocha types pass after a formatting-only correction. No fresh product rebuild or aggregate coverage result is claimed for this source checkpoint.
- **Incoming-join prerequisite:** a new regression reproduces premature join delivery after reload: persisted `SIGNED_IN` state caused the controller to send and clear a join before the replacement webapp signalled readiness. Delivery now also requires current-view readiness, which reload resets. The target verifies retention, no cross-account delivery and exactly-once delivery after the intended replacement loads. The old implementation fails with an unexpected delivered join; the corrected controller suite passes **25 tests**. Logs: `/tmp/cap001-join-readiness-before.log`, `/tmp/cap001-join-readiness-after.log`. This fixes lifecycle delivery, not the still-missing native incoming-link/automated-SSO dispatch path.
- **Reload/About product checkpoint:** rebuilt sandboxed account lifecycle and metadata/cold-restart regressions pass **2/2 in 5.8 seconds on Linux** (`test:e2e --project=macOS accountLifecycle.spec.ts accountMetadata.spec.ts`; `/tmp/cap001-reload-product.log`). These fixtures protect existing lifecycle/storage behavior, not a full macOS relaunch or live SSO claim. Next executable work is native incoming-link/automated-SSO lifecycle routing, followed by remaining cleanup/environment acceptance and fresh coverage/platform gates.
- **Restored reload/About suite:** full main tests pass **700 tests**, zero pending (`/tmp/cap001-reload-full-restored.log`); application/Mocha types and targeted ESLint pass. The initial full run had 698 passes and two About fixture failures because the new test imported the module before the existing fixture set the app path. Moving the test into that app-path-aware fixture corrects the harness; no production workaround or weakened assertion was used. `build:ts` and `bundle` pass. Final product evidence follows below; aggregate coverage and hosted final-head gates remain outstanding.
- **Native reload/About follow-up:** the authorized reload boundary now passes its registered identity to the controller, which rechecks authority when queued work starts, recreates every native view without clearing sessions, preserves selection and reports partial failures while still restarting healthy accounts. macOS relaunch also requests native reload before refreshing the shell. About version requests use the native window binding and original-account readiness queue. New targets fail before implementation; truncating reload to one account causes both session/recovery regressions to fail; restored. The focused controller/reload IPC suite passes 30 tests; types and targeted lint pass. Local logs: `/tmp/cap001-reload-before.log`, `/tmp/cap001-reload-final-native.log`, `/tmp/cap001-reload-sensitivity.log`. Final rebuilt validation follows below. A source search of `electron`/`bin` finds no production sender for `WRAPPER.UPDATE_AVAILABLE`; its retained preload listener is not justification to invent a new updater path. Remaining desktop routing is incoming links/automated SSO and their startup/account lifecycle policy.
- **Restored shortcut checkpoint:** full main suite passes **695 tests**, zero pending; rebuilt sandboxed lifecycle and metadata/cold-restart product regressions pass **2/2 in 5.3 seconds on Linux** (`test:e2e --project=macOS accountLifecycle.spec.ts accountMetadata.spec.ts`). Logs: `/tmp/cap001-shortcuts-full-main.log`, `/tmp/cap001-shortcuts-product.log`. The Playwright project name does not make this macOS evidence, and these two product fixtures do not replace native menu keyboard/visual smoke or final-head hosted gates. Fresh aggregate diff coverage remains required.
- **Native edit/account-switch follow-up:** the same main-owned window binding now routes fixed copy/cut/paste/redo/select-all/undo operations to the selected native account and resolves integer switch indices against main state. Unknown channels, excess arguments and invalid indices are rejected without editing or switching. The new targets first fail against the missing dispatcher, then pass; a temporary copy-to-paste mapping causes the exact-operation assertion to fail and is restored. The focused controller/window suite passes 23 tests before the final strengthened mapping check; full restored validation follows below. Application/Mocha types, targeted ESLint, `build:ts` and `bundle` pass. Remaining routing includes version requests, reload/update, automated SSO and incoming links. Local logs: `/tmp/cap001-shortcuts-before.log`, `/tmp/cap001-shortcuts-sensitivity.log`, `/tmp/cap001-shortcuts-native.log`.
- **Restored menu checkpoint:** full `test:main` passes **693 tests**, zero pending (`/tmp/cap001-menu-full-main.log`). Targeted ESLint and application/Mocha types pass. This source-suite result does not establish fresh bundled product behavior or aggregate diff coverage; both remain required before PR readiness.
- **Native system-menu routing:** main now binds the application window's menu callbacks directly to the account controller. Known conversation/preferences/logout commands capture the selected account, use a bounded per-account readiness queue, drain once on that account's authorized loaded event, and are discarded on reload/removal/approved environment change. Unknown commands are rejected; other windows cannot use this binding. The focused controller/window suite passes **21 tests**; application/Mocha types pass. Routing to the wrong account causes both new controller tests to fail; restored. Logs: `/tmp/cap001-menu-integrated.log`, `/tmp/cap001-menu-sensitivity.log`. This is not complete desktop routing: edit/account-switch shortcuts, version requests, reload/update, automated SSO and incoming links still require native integration and final product/platform evidence.
- **Routing prerequisite:** a new native regression reproduces lost actions when dispatch queues another action during `WindowManager.flushActionsQueue` (expected queue entry versus empty actual queue). Detaching the current batch before dispatch preserves the later action for the next flush; both queue tests now pass. Logs: `/tmp/cap001-queue-before.log`, `/tmp/cap001-queue-after.log`; command: `env -u ELECTRON_RUN_AS_NODE corepack yarn test:main --grep 'WindowManager queued actions'`. This does not close native account routing: per-account readiness/target selection and production menu/deep-link wiring remain required.
- **Published cutover `bb46ddea` (PR #47):** all-platform packages, lint and analysis passed, but [Build and Test](https://github.com/adamlow-wire/wire-desktop/actions/runs/34353724839/job/102473026245) failed changed-code coverage: **476/678 statements (70.21%, required 80%)**; changed security branches pass **8/8 (100%)**. The test suites passed before that gate. Draft E2E/report jobs were skipped, not waived. Do not rerun unchanged code or treat package success as PR readiness.
- **UI characterization follow-up:** React/tooling now passes **104 tests across 25 suites**, including seven new tests for selected-account layout, exact-account retry/cancel, absence of renderer webviews, loading fade/restart, main-only display snapshots and failed-bootstrap rejection. Temporarily disabling fade cancellation, dropping snapshot updates and routing retry to removal each caused the expected failure; all three perturbations are restored. Commands: `corepack yarn test:types` and `corepack yarn test:react --runInBand --modulePathIgnorePatterns='<rootDir>/wrap/'`. Local logs: `/tmp/cap001-slot-mutation.log`, `/tmp/cap001-ui-full-restored.log`. This strengthens UI regression evidence; fresh aggregate coverage is still required and desktop-action routing remains the next implementation slice.
- **Current working-copy cutover:** application/Mocha types, TypeScript and bundled-preload builds pass. The focused native suite passes 35 tests, including shell controls, stale snapshots, native menu queue progress, reload/session retention and per-account load recovery. Stale-response and layout-limit perturbations cause two expected failures; restored. The first full product lifecycle attempt exposed a startup block: native preload synchronous managed-config IPC arrived before a committed frame URL. A deterministic target failed with two unexpected synchronous requests. Native preloads now receive explicit main-owned App-lock bootstrap (true/false preserved; missing/malformed/duplicate values rejected), without weakening origin authorization; the target passes and actual sandboxed startup/quit succeeds on Linux.
- **Default native product checkpoint:** lifecycle and metadata/cold-restart regressions pass **2/2 in 6.4 seconds on Linux** (`test:e2e --project=macOS accountLifecycle.spec.ts accountMetadata.spec.ts`). Exact-target joins/logout, add/cancel, removal and cross-account cookies are retained. Tests select the persistent shell rather than the temporary migration reader, signal real webapp readiness, observe the actual native menu instead of removed HTML, and inspect main-owned persistence instead of obsolete renderer storage. Explicit native-child ownership, disabled shell webviews and private-session omission are asserted. Native menu item callbacks are exercised through privileged test instrumentation; OS keyboard/visual selection is not claimed. A proposed temporary webview-enablement sensitivity run was rejected by execution safety review and immediately restored before any build ran; that additional sensitivity check remains unproven. No temporary diagnostic markers or security mutations remain. Logs: `/tmp/cap001-bootstrap-native.log`, `/tmp/cap001-native-product-pair.log`, `/tmp/cap001-cutover-boundaries-mutation.log` (local evidence, not durable CI).
- **Broader cutover validation:** full main suite passes **689 tests**, zero pending; React/tooling passes **97 tests across 22 suites**. The only initial main-suite failure paired the changed UI with the obsolete preload. Both existing CSP render/eval-denial tests now use the actual named shell preload and authorized snapshot boundary, preserving their assertions. Full ESLint, application and Mocha types pass after correcting import order. Standalone Playwright types retain the nine existing generated-client/global-declaration errors; do not report them as green.
- **Still required for PR #47:** desktop actions still forwarded to the old shell, automated SSO, localized environment approval, strict native log/partition cleanup, broader lifecycle/focus/UI coverage and complete final-head platform qualification. Permissions still default to deny; SEC-009 is not waived. The following 678-test checkpoint predates this cutover.
- **Controller validation checkpoint:** rebuilt types/preloads; 678 main tests (zero pending), 4 renderer and 97 React tests pass. Diff coverage against integration passes at 394/423 statements (93.14%) and 3/3 changed security branches (100%). Controller coverage is 93/93 statements and 39/39 branches; 13 native controller tests include the real bundled account preload. Sandboxed product lifecycle and metadata/restart pass 2/2 in 8.0 seconds on Linux. Full ESLint and tracked-file formatting pass. The repository-wide formatting glob cannot traverse an existing restricted MSI artifact under `wrap/msi-validation.Q61sZg`; tracked-input validation avoids changing artifact permissions or deleting user data.
- **CAP-001 controller/preload integration in progress:** `AccountController` now connects state, native views and both IPC binders. It serializes lifecycle effects, revalidates queued authority and post-approval identity, closes before exact-session clearing, retains failed-deletion records for retry, and rejects metadata that changes the approved origin. Native tests exercise actual named shell/guest IPC, cookies, join routing, replacement and failure paths. The new bundled account preload uses fixed account-event IPC while the existing legacy entry shares the same webapp API installation. The actual bundled bridge updates only its owner and delivers pending joins without a webview host. Five native failures prove authority/destruction-order sensitivity; disabling native event transport fails its mapping test. All perturbations are restored. A new badge regression reproduced missing aggregate refresh after notification activation and is corrected. Production startup still selects the legacy entry and renderer-owned views; controller/startup, display snapshots, shell layout/actions, failure/reload/SSO and permission integration remain required before readiness.
- **Draft CI diagnosis:** prior head `7b4d0eb4` passed [build](https://github.com/adamlow-wire/wire-desktop/actions/runs/34340601007), analysis and [all-platform packages](https://github.com/adamlow-wire/wire-desktop/actions/runs/34340601084). [Lint](https://github.com/adamlow-wire/wire-desktop/actions/runs/34340601041) failed only the multiline formatting of the new malformed-domain assertion; corrected in the current work. E2E was skipped for this draft checkpoint, not waived for final readiness.
- **Earlier event/state checkpoint:** `1473439e` contains the guest-event/state work; `1253ccee` synchronizes merged integration without runtime changes, and draft PR #47 now targets integration directly. Clean diff coverage versus integration passes at 283/286 statements (98.95%) and 3/3 changed security branches (100%). State coverage is 72/72 statements and 35/36 branches. After rebuilding TypeScript/preloads, sandboxed account lifecycle and metadata/restart product regressions pass 2/2 in 5.3 seconds on Linux. Commands: `clear:coverage`, `build:ts`, `bundle`, serial main/renderer/React coverage, `coverage:report`, `DIFF_COVERAGE_BASE=fork/integration/electron-modernization corepack yarn coverage:diff`, and `test:e2e --project=macOS` with the two named specs. No sensitivity mutation or recovery stash remains. Next executable work is the actual controller/preload/UI integration, not another proof-shell variant.
- **CAP-001 guest events/state transitions:** six boundary tests cover every event type, registered identity versus spoofed URL IDs, exact schemas/size bounds, wrong views, absent capabilities, subframes, foreign origins, changed sessions, revocation, failures and per-view quotas. Widened payload/view restrictions fail two tests; restored. Main state now preserves targeted metadata, lifecycle, theme, badge, join and identity-reset behavior; cross-account misrouting fails three tests. All perturbations are restored. Application/Mocha types and rebuilt coverage suites pass: 664 main tests (zero pending), 4 renderer and 97 React tests. Validators remain main-side while shared contracts contain only types/constants. The binders remain unregistered until controller/preload integration; destination changes and destructive lifecycle effects still require controller policy.
- **Resume verification:** goal remains active; branch `cap/CAP-001-production-accounts-2026-09-09` resumed from `30a63481`. PR #45 has now merged after every final-head gate passed. Account-event IPC has local allow/deny evidence but is not production-wired.
- **CAP-001 join compatibility:** the sibling webapp accepts omitted/null domains and resolves its own fallback. The former preload silently dropped these valid joins and the draft profile rejected their pending state. Two new targets reproduced both defects before correction; 27 focused profile/preload tests pass after restoration, including malformed-domain denial. The sandboxed product lifecycle test also passes with federated, omitted-domain and null-domain joins, exact-target logout and cross-account cookies (Linux, 3.4 seconds). Commands: `build:ts:tests`, `test:main --grep 'non-federated|webapp preload event routing|main-owned account profile'`, rebuilt `test:e2e --project=macOS accountLifecycle.spec.ts`. This fixes compatibility, not production view ownership.
- **CAP-005 integration synchronization:** merge `29fa5021` incorporates merged SEC-012 and the validated isolated legacy-profile fixture without changing download policy. Rebuilt application/preloads; application/Mocha types, 598 main tests (zero pending), and sandboxed metadata/navigation regressions (2/2 in 6.9 seconds on Linux) pass. Retargeted final-head hosted gates remain mandatory; the earlier native preflight does not replace them.
- **CAP-001 native view owner:** `AccountViews` registers main-owned account/session authority before navigation, fixes secure preferences, defaults permissions/popups to deny, selects/resizes views and revokes authority before awaited destruction. Twelve real-Electron tests cover legacy default/persistent sessions, cross-account cookies, real renderer loss/recreation, foreign redirects, stale initialization and parent teardown. New targets first reproduced partition aliases, premature recreation during close and cleanup after Electron removes a destroyed view's `webContents`; fixes pass. Isolation/redirect-policy mutations produce two expected failures and are restored. Full main suite: 651 passing, zero pending; owner coverage 91/92 statements and 47/48 branches. This remains **unwired preparation**: production setup, guest-event routing, shell layout/UI and permission user flows must be integrated before PR #47 is ready.
- **CAP-001 shell command boundary:** draft `accounts:control:v1` permits only read/add/select/remove from a capability-authorized application shell, with exact request/response schemas and 120 requests/minute per view. It accepts no renderer-selected session/partition. Seven allow/deny tests pass; temporarily widening the view type and request/response fields produces three expected failures, then all pass after restoration. Inventory marks the binder as not yet registered in production. Remaining wiring must use named shell bridge methods and main-owned guest/view lifecycle, not expose the command transport directly to page code.
- **SEC-012 after PR #46 synchronization:** merge `f266b954` retains unchanged preview runtime, incorporates the validated isolated seeding fixture and reconciles current tracking. Rebuilt TypeScript/preloads; application/Mocha types, 530 main tests (zero pending), and sandboxed metadata/navigation product tests (2/2, 6.2 seconds on Linux) pass. Those synchronized-head gates subsequently passed before PR #44 merged; see merged evidence above.
- **CAP-001 main state owner:** `AccountState` now owns generated identities, selection, unfinished-login reuse and account-limit enforcement. Display snapshots omit session IDs and pending SSO/join secrets; failed saves do not publish transient state. Seven owner tests plus a real-file restart integration test pass. Exposing session IDs and publishing before the write produce two expected failures; restored before commit. Full main suite: 632 passing, zero pending; application/Mocha types and targeted lint pass. Owner coverage: 39/39 statements and 16/16 branches. The next implementation step is authorized guest/shell commands and production view integration; no new IPC or runtime cutover is active yet.
- **CAP-001 groundwork at `11b3e46b`:** rebuilt TypeScript and bundled preloads; 624 main (zero pending), 4 renderer, 97 React and 38 tooling tests pass. Own diff versus #45: 88/90 statements (97.78%). Profile module: 64/64 statements and 34/34 branches; native reader: 24/26 statements and 9/10 branches. Sandbox product lifecycle and metadata/restart checks pass 2/2 in 8.7 seconds on Linux. These results validate the current preparation, not future production wiring.
- **CI repair:** PR #44 trace identified the initial legacy seed `app.close()` stall before metadata assertions. PR #46 isolates that setup and has passed final-head Windows/macOS E2E/report and all-platform packages. PR #44 consumed the repair and subsequently passed its own final-head gates before merge; no blind retry or production shutdown change.
- **CAP-001 preparation:** the four `electron/src/accounts/` files are restored to the active branch; the recovery stash was successfully applied and dropped. Profile validation covers exact legacy mappings, case-aliased partition rejection, unreadable/corrupt state and atomic-write failure preservation. The read-only migration reader uses the exact frame's storage key and supplied session, with page scripts/Node/webviews disabled; native tests cover preferences, retained cookies, isolated sessions, bounded reads, failed-read distinction and complete window destruction. Combined profile/reader run passes 26 tests. Temporarily enabling page scripts fails the preference assertion; removing durable-write synchronization and relaxing the read-size limit produce two expected failures. All guards are restored. Types and targeted lint pass. Commands: `corepack yarn test:types`, `corepack yarn build:ts:tests`, `env -u ELECTRON_RUN_AS_NODE corepack yarn test:main --grep 'main-owned account profile|legacy account profile reader'`. These modules are **not wired into production** yet; production migration acceptance remains open.
- **CAP-001 migration fixture:** old-profile setup now runs a sandboxed, JavaScript-disabled file-origin window without loading the current application/migration logic. Both account lifecycle and metadata/restart baselines pass (8.6 seconds; restored run 7.9 seconds). Changing the stored key so legacy state is absent makes both fail; restored before commit. This prepares real one-time migration testing without changing product runtime or weakening the existing assertions.
- **SEC-012:** separate baselines `41882ee3`/`b31f4b60`, implementation `6d7760fc`. Old transport fails five private-fetch targets; old parser fails inherited-object mutation. Title mutation fails three compatibility tests; private-address/DNS-pinning/compressed-wire mutations cause 22 failures. All restored. Synchronized full suite: 530 main (zero pending), 4 renderer, 97 React, 38 tools; application/Mocha types and lint pass. Diff: 176/180 statements (97.78%), 114/121 security branches (94.21%). Actual sandboxed metadata/navigation pass 2/2 in 7.9 seconds; cleanup passes in 2.0 seconds on Linux.
- **SEC-012 initial CI failure:** head `9d25f910` passed Windows E2E, but [macOS](https://github.com/adamlow-wire/wire-desktop/actions/runs/34322872580/job/102373295119) failed with a 90-second worker-teardown timeout. Metadata restart, account search and authenticated-page timeouts passed on built-in retry (31 passed, 3 flaky). Do not attribute this to staging without evidence. No unchanged-head rerun was requested; the synchronized head requires fresh gates.
- **CAP-005:** baseline `08135866`: 6 pass, skipped-preparation mutation gives 3 failures, restored baseline passes. Five unsafe-path targets fail on old code. Link-denial mutations fail; independently removing download-start revalidation gives one expected failure. All restored. Full suite: 598 main (zero pending), 4 renderer, 97 React, 38 tools; types/lint/formatting pass. Standalone diff vs #44: 39/47 statements (82.98%), 23/23 security branches (100%); integration diff: 215/227 statements (94.71%), 137/144 branches (95.14%). Sandbox product metadata/navigation/cleanup: 3/3 in 9.3 seconds on Linux. Native preflight passes all platforms; [Windows job](https://github.com/adamlow-wire/wire-desktop/actions/runs/34327126999/job/102386861788) explicitly passes the real junction test. Retargeted final-head acceptance is still required.
- Commands: `corepack yarn build:ts && corepack yarn bundle`; `test:types`; `build:ts:tests`; fresh `test:main:coverage`, `test:renderer:coverage`, `test:react:coverage --modulePathIgnorePatterns '<rootDir>/wrap/'`, `test:bin`, `coverage:report`; `DIFF_COVERAGE_BASE=fork/integration/electron-modernization corepack yarn coverage:diff`. CAP-005 standalone coverage uses `DIFF_COVERAGE_BASE=sec/SEC-012-preview-fetch-2026-09-08`. Product checks use `test:e2e --project=macOS` with the named regression specs; that local label is **Linux evidence**, not macOS.

## Maintainer input and operational constraints

- No immediate input is needed for executable work. Controlled live SSO needs a staging SSO code and dedicated IdP test identity, or maintainer-run evidence. Never paste credentials into chat/logs. Signing is M5, not ordinary testing.
- PR-only integration and self-merge authorization remain in force. Require every applicable final-head check, including checks not enforced by protection; no admin bypass. Review substantive comments before resolving.
- Run native GUI suites serially. After branch switches rebuild TypeScript **and** bundled preloads before product tests; never rebuild during a suite. Preserve `chromiumSandbox: true`.
- Clear coverage before aggregating changed sources; diff coverage evaluates committed HEAD. Exclude `wrap/` from Jest. Preserve user worktree `wrap/worktrees/wpb-5221-deployment-audit`; never run local `build:prepare` or `clear:wrap`.
- Standalone Playwright types retain nine known generated-client/`window.wire` errors; not a green check. The seed-only metadata teardown was repaired by PR #46; this does not claim all product startup/quit flakes are fixed. Renderer-loss tests terminate only their verified renderer PID to avoid host dump delays.
- Vault values work. Do not recreate credential normalization, Jira dependencies or production-test credential workarounds. MSI is integrated.
- On September 9 the missing conversation goal was recreated at the maintainer's request. The goal is active; completion is governed by plan acceptance, not the goal record.
