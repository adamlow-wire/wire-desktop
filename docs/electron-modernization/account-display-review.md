# Account snapshot delivery and display bootstrap review — September 22

Reviewed main snapshot producer, AccountShellBridge and configureStore source/fixtures at7a3b6e72. This is not full renderer consumer/native acceptance.

Main's controller changed callback obtains public snapshots, revalidates display capture and sends only the fixed accounts-changed channel when the main window is live. AccountState omits private partition/SSO/join state and controller adds fixed failure flags/messages. This producer is main-owned. Pushed snapshots are not validated again in AccountShellBridge; invoke responses are validated by AccountControlIpc. The bridge therefore trusts the main publisher and is not an independent defense against a compromised main process. Remote account events still pass their registry/schema/controller boundaries before affecting state. Main producer send/revalidation failure handling remains separately open.

The bridge exposes a frozen object of named account controls and subscriptions, not raw IPC or native events. One fixed IPC listener fans out account snapshots to its display listeners, isolating ordinary subscriber exceptions with a fixed warning. Revision tracking makes newer pushed state win over an older in-flight command response. Its unsubscribe removes the display listener and is idempotent; it does not remove the one preload-lifetime IPC handler. Successful store subscriptions intentionally live with the single renderer startup in index.tsx; no repeated initialization/hot-reload lifecycle is claimed. Native contextBridge serialization/freeze semantics were not exercised here.

All4 existing bridge Node cases pass. Bypassing revision protection or unsubscribe each fails1, then source restores and4 pass. Logs `/tmp/tst006-shell-bridge-{revision,unsubscribe}-sensitivity.log` and `/tmp/tst006-shell-bridge-restored.log`. Bridge/store production and original tests originate together inbb46ddea; this review does not infer a separate original baseline.

The bridge's revision protection did not cover the consumer's subscribe-after-read gap. [Bootstrap recovery](cap001-store-bootstrap-recovery.md) records separate failing baseline4135f7a6 and scoped59ae067b fix. Store initialization now subscribes before awaiting read, buffers the latest push and uses it when constructing Redux. Failed read still rejects, even after a push, and unsubscribes. Array copies keep Redux from mutating the incoming array; no renderer persistence fallback is introduced. Subsequent snapshots continue to dispatch normally.

Exact renderer source/tests59ae067b are composed as7a3b6e72, tree8545910eeab9a869f9e957a3fc0a521b127cdbe3.82 renderer tests/12 suites,4 bridge Node cases, root/Electron test types and corrected targeted lint pass. Store sensitivity mutations fail1/2/2/2 and restore4. Earlier147 combined Node cases retain1d7c2a52 attribution; no silent rerun or native acceptance claim.496 original-baseline inventory paths remain exact.

Remaining mandatory review includes renderer actions/selectors/actual consumer reachability, native startup/contextBridge/frame lifetime, main snapshot-publisher error handling, general diagnostic confidentiality and full platform/E2E/unsigned-package qualification. The historical store assertion that failure subscribes zero times was explicitly replaced by exactly-once cleanup under the corrected subscribe-before-read contract, preserving rejection/no-fallback semantics. No native process or remote write occurred.
