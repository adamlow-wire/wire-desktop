# Unsigned Linux ASAR package inventory

Status: **partial ELC-003 / PKG-001 evidence**, 2026-09-23. Source is the local composed candidate `2ba7ade3e43bca9c5263c1bf5b7fa8ac54b20acd` (tree `484448fee75fc5d311f3d73b9b35fff8f9f206d635`) on Electron 43.4.0. This is an unsigned Linux `dir` target built with the repository's actual `buildLinuxConfig` and `electron-builder`, without launching the app. It is not an AppImage, deb, rpm, Windows or macOS artifact.

The built `linux-unpacked/resources/app.asar` has SHA-256 `d765a4ce82ec18af069361369c5b092f0977ce4a84fad7c3b50a979986d57a04`. `node bin/test-tools/verify-package-contents.cjs wrap/recovery-artifacts/linux-dir` passes: 7,695 ASAR entries and all 22 required files. The local-only [inventory utility](../../bin/test-tools/inventory-shipped-asar.cjs) finds **423 installed package instances / 366 distinct names**. It accepts only actual `node_modules` package-root manifests; 93 nested `package.json` metadata files lacking name/version are excluded. The four `package.json` files under `app.asar.unpacked` correspond to the same four package paths in the ASAR inventory. The private detailed inventory and unsigned application directory remain in ignored local `wrap/recovery-artifacts/linux-dir`; they are not committed.

The maintainer explicitly approved a metadata-only query of these 423 package instances to [npm's bulk advisory endpoint](https://docs.npmjs.com/cli/v8/commands/npm-audit/). An initial automatic approval review rejected that query before approval; the approved query then completed twice with identical detailed matches at 2026-09-23T09:43:23Z and 09:47:27Z. It returned **30 version-range matches: one critical, 22 high, five moderate, two low**. The affected names are `tar`, `minimatch`, `brace-expansion`, `semver`, `ip`, `uuid` and `@tootallnate/once`. Detailed matches are kept in a private ignored local JSON file, not committed. Matches establish package/version overlap, not that an application execution path reaches a vulnerable API.

The critical match is shipped `tar` 6.2.1 against [GHSA-23hp-3jrh-7fpw](https://github.com/advisories/GHSA-23hp-3jrh-7fpw), which describes unbounded extraction of untrusted archives. Eight more high matches involve the same `tar` version. `minimatch` 3.1.2/5.1.6 and `brace-expansion` 1.1.13/2.0.1 account for 12 high matches; `semver` 7.5.0 and `ip` 2.0.1 account for one each. The top-level app manifest does not directly declare those five packages; ASAR manifests show `tar` under the `node-gyp`/`cacache` tree, for example. Preliminary declared-dependency traversal does not connect the high/critical instances from the app's 30 root dependencies, but dynamic/peer loading and packaging inclusion still require review. Do **not** use this preliminary observation to accept the findings. The shipped graph also includes `uuid` 9.0.1, a moderate version-range match; the two reviewed app callers use argument-free v4, outside that advisory's supplied-buffer v3/v5/v6 behavior. Transitive callers are not yet disposed.

**ELC-003's no-unaccepted-high/critical acceptance is open.** The earlier installed-production audit counted 354 instances and found no high/critical matches, but it did not represent the 423 instances in this ASAR. The package-content verifier checks first-party inputs and required files; it does not prune or clear all third-party package contents.

Reproduce after building the same unsigned Linux directory with the current builder configuration:

```sh
node bin/test-tools/verify-package-contents.cjs wrap/recovery-artifacts/linux-dir
node bin/test-tools/inventory-shipped-asar.cjs wrap/recovery-artifacts/linux-dir/linux-unpacked/resources/app.asar wrap/recovery-artifacts/linux-dir/shipped-packages.json
node bin/test-tools/query-shipped-advisories.cjs --allow-public-npm-metadata-query wrap/recovery-artifacts/linux-dir/shipped-packages.json wrap/recovery-artifacts/linux-dir/shipped-advisory-matches.json
```

The inventory command reads the ASAR locally, writes its package names/versions to an owner-controlled file with mode `0600`, and prints only hash/counts/path. It makes no network request. The separate, explicit-opt-in query command sends those names/versions to public npm, writes detailed matches mode `0600`, and prints only aggregate counts/path. The build ran on pinned Node 22.22.3 and locked dependencies. The local build command avoided `build:prepare` because that script clears the shared `wrap` directory containing protected worktrees.

Next ELC-003/PKG-001 work: trace the included high/critical package trees and why they enter the ASAR, remove unneeded installed packages from package inputs or update required ones compatibly with behavior tests, then rerun the shipped graph audit on the final composed artifacts. Document any specific non-applicable match with a reviewed call path rather than accepting all included packages as safe. PKG-001/TST-005 must still qualify actual unsigned installers and packaged native startup on all three platforms. The current ASAR does not close either gate.

## Scoped package-input correction and composed Linux check

A separate PKG-001 characterization commit `dda2cc6b` exercises the real electron-packager ignore callback and electron-builder node-module matcher. It preserves nested runtime `semver`/`minimatch`/`brace-expansion` examples and fails on the original top-level `tar` inclusion. Scoped source commit `7280d5d9` excludes only the ten audited incidental top-level roots from both packagers; nested package versions remain eligible. The test passes after the change. A temporary broad glob that also excluded nested `semver` fails its retained-runtime assertion; the source was restored. All 357 tooling tests, tooling types and forced scoped lint pass on the PKG-001 branch.

An unsigned Linux `dir` experiment with the same exclusion patterns on the earlier `2ba7ade3` source changes the ASAR from 423 instances/366 names to **407 instances/359 names**. The pruned ASAR SHA-256 is `d3179eb82e988a3e8df4db4b3fb0fc384f38426e930cb51f940e89df293b1432`; the content verifier passes 7,376 entries/all 22 required files. The approved npm query for this 407-instance inventory returns **zero critical/high**, one moderate (`uuid` 9.0.1), and one low (`@tootallnate/once` 2.0.0) match at 2026-09-23T09:57:46Z. A local declared-dependency traversal has 345 reachable package instances both before and after the exclusion, no removed reachable instance, and no newly unresolved declared dependency. This supports the narrow scope but cannot rule out dynamic or peer loads.

The four source/test commits were then composed onto `2ba7ade3` as `725efb5c7767dc6ec1d05d8a3f9f2970a1c5bff7` (tree `2e77b6c36eae6bae8d67c25bc71c8dbab51db02b`), with its **own immutable dependency install** and pinned Node 22.22.3. Root, tooling and Electron-test TypeScript checks, forced scoped lint, 182 focused package/preload cases, production TypeScript and webpack pass. Its actual unsigned Linux `dir` build passes the content verifier: 7,338 ASAR entries/all 22 required files, SHA-256 `63062ddc07f4bf6694e6d618eca3ae38e7b11585dff645cbf692f3d40361f88e`. Its 407-record package inventory is **byte-for-byte identical** to the authorized, queried experimental inventory. Thus the recorded zero-high/critical advisory result applies to the exact same package paths/names/versions in this composed Linux directory at the query time; there was no need to send the same list again. The different ASAR hash reflects different compiled app bytes.

The standalone PKG-001 source branch `7280d5d9` has an older, uncomposed dependency graph: its own immutable Linux build passes the content verifier but contains 477 instances/414 names (ASAR SHA-256 `79b52ca915da021376795d94c6d2970662054881fb10abcf3602d3c7dd5141a5`). The earlier 423-instance metadata approval did not cover that expanded list; automatic approval review rejected a public query, and no advisory-clearance claim is made for the standalone branch. The final handoff target is the reconciled composition, which still needs all required platform artifacts and checks.

**Gate at the directory-build checkpoint:** review dynamic/peer/native startup behavior after pruning, qualify actual unsigned Windows Squirrel/MSI, macOS and Linux installer outputs on the final composed head, rerun the shipped advisory check on those final artifacts under applicable metadata authorization, and resolve/document the residual moderate/low matches. A Linux directory's package inventory and static closure are not installer or native-app acceptance. ELC-003/PKG-001/TST-005 remain in progress.

## Actual unsigned Linux installers from the composed candidate

The repository's real Linux build wrapper produced AppImage, deb and rpm from the same production source `725efb5c` and documentation head `ffa6074c` (package version `3.44.0-ffa6074c`) on pinned Node 22.22.3. The first wrapper invocation completed AppImage and deb, then failed at rpm because the host lacked `rpmbuild`; it restored its temporary `package.json` and `electron/wire.json` edits. For the rpm-only retry, Ubuntu Jammy RPM tooling was extracted under `/tmp/wire-rpmbuild-local` without a system install. The retry passed and again restored both files; the source worktree remained clean. Logs: ignored local `wrap/recovery-artifacts/linux-installers-build.log` and `linux-rpm-local-toolchain.log`.

| Unsigned artifact in ignored `wrap/dist` | Size (bytes) | SHA-256 |
| --- | ---: | --- |
| `WireInternal-3.44.0-ffa6074c_x86_64.AppImage` | 137643545 | `11798f3447bb12ef8fb8509d70389be190fba645d2d1694a306e9b9e8fdce93a` |
| `WireInternal-3.44.0-ffa6074c_amd64.deb` | 104991820 | `caf474de5dc8bb68ae9e5214b4eba7e116cc773419e656060700a8c2c385658b` |
| `WireInternal-3.44.0-ffa6074c_x86_64.rpm` | 92057741 | `edffc549d2c659a51e7650c158d4317e5a5fb79a956ceb84cf93b063127ad57d` |

All three artifacts were extracted as data, without executing Electron or AppImage. Each contains the same `app.asar` (SHA-256 `b7c3b72396d85239a89cf240f7e908cab1b988fbc2a61beaeedc6e21170929db`), and `verify-package-contents.cjs` passes 7,338 entries and all 22 required files for each. Each ASAR's 407 package instances/359 names match the already approved and queried pruned inventory **byte for byte** (inventory SHA-256 `a9cabd94678d377220da6f05d4ee7fd2ae84dba9443920d87b99b1c434eebca5`); the earlier zero-high/critical, one-moderate/one-low result therefore applies to these exact packaged versions at query time. No new metadata was submitted. Each extracted Electron binary is also byte-identical (SHA-256 `16e14bee59d0cfa71387140de727245efdb6f588639abf9ca6ea02cfe5c217ab`) and has the six configured fuse states: RunAsNode off, cookie encryption on, NODE_OPTIONS off, CLI inspect off, embedded ASAR validation off, and only-load-from-ASAR on. These are static artifact checks, not a startup or signed-integrity result.

Reproduce locally after building the three installers: statically extract AppImage with `unsquashfs -o 188392`, deb with `dpkg-deb -x`, and rpm with `rpm2cpio | cpio -idm`; run `node bin/test-tools/verify-package-contents.cjs` on all three extracted roots and `node bin/test-tools/inventory-shipped-asar.cjs` on each contained ASAR. The SquashFS offset is specific to this AppImage. `@electron/fuses` `getCurrentFuseWire` reads each extracted binary without launching it. Build commands use `bin/build-tools/build-cli.ts linux` with `TS_NODE_PROJECT=tsconfig.bin.json`; the rpm retry sets `LINUX_TARGET=rpm` and prepends the local RPM toolchain to `PATH`/`LD_LIBRARY_PATH`/`RPM_CONFIGDIR`. Do not use `build:prepare` in this shared worktree because it clears `wrap`.

The three artifacts were built in an ad hoc checkout without `APP_ENV` or `.env.defaults`: archived metadata has name `WireInternal` but environment `production`. Their static content, dependency and fuse results stand; they do **not** qualify internal environment/identity separation. A correctly identified internal AppImage is recorded below, while deb/rpm still require an environment-correct rebuild. These artifacts close the **local static Linux installer-content subcheck** only. Linux packaged native startup and product smoke, reproducible CI on supported runners, Windows Squirrel/MSI and macOS artifact inspection/startup, residual advisory disposition, full review and final-head gates remain open. The documentation head changes after this build; the runtime/tooling source remains `725efb5c`, but handoff artifacts must be rebuilt and checked against the eventual final head.

**Subsequent F-021 finding:** static inspection found `Exec=AppRun --no-sandbox %U` in this AppImage desktop entry. The generated `AppRun` also adds `--no-sandbox` if its user-namespace probe fails. The deb/rpm desktop entries have no such flag. The static ASAR/fuse checks above do not qualify effective sandboxing: the AppImage requires a fail-closed launcher correction, rebuilt artifact verification and packaged native smoke before handoff.

## F-021 scoped AppImage sandbox correction

Baseline `84ac22aa` on the isolated PKG-001 branch preserves three Linux builder controls and fails separate desktop-argument and generated-AppRun sandbox targets. Source `45f45f4f` sets `appImage.executableArgs: []` and applies a version-pinned Yarn patch to electron-builder's `app-builder-lib` 26.15.3 AppRun generator. Immutable Yarn resolution selects the patched copy from electron-builder and dmg-builder. The launcher no longer substitutes `--no-sandbox` when the host namespace probe fails; a host unable to provide Chromium sandboxing must fail startup rather than silently disable it.

The first real-wrapper rebuild `wrap/dist/WireInternal-3.44.0-45f45f4f_x86_64.AppImage` (SHA-256 `7fd05489933464b277f35822e1e4a4e8e5d796ff533262fb739520ce6e0e1e12`) used a private dependency install and production TypeScript/webpack output. It reproduced the original local setup limitation: `WireInternal` name with `production` environment, so it is a sandbox-regression artifact, not an internal-identity acceptance artifact. Static SquashFS extraction confirms desktop `Exec=AppRun %U` and no `--no-sandbox` in AppRun. The repository content verifier passes 7,338 entries/all 22 required files; ASAR SHA-256 `97cb9fa72a2c2a5a465f2f1b29cec4a1890cb045feeaaed6b6da6788ac517e90`. Its 407-instance/359-name inventory hash remains `a9cabd94678d377220da6f05d4ee7fd2ae84dba9443920d87b99b1c434eebca5`, byte-identical to the previously approved npm scan input. No new package metadata was submitted. The extracted old artifact fails both new static launcher checks; running its AppRun against a synthetic executable and failed `unshare` probe forwards `--no-sandbox`, while the corrected generated launcher forwards only the benign input argument. No Electron or user desktop was launched.

The hosted Linux workflow now checks both extracted launch files and passes the extracted AppRun to the existing packaged-account smoke. This workflow has only passed YAML parsing locally; no hosted run, direct mounted-AppImage launch, or effective Chromium OS-sandbox probe is claimed. The existing CI smoke grants setuid on an extracted helper, so mounted-AppImage compatibility on hosts without user namespaces remains an explicit qualification gap. For an internally consistent candidate, the wrapper was rerun at source `7771a714` with `APP_ENV=internal`, `LINUX_TARGET=AppImage` and an empty local env file so tracked internal identity is preserved. `WireInternal-3.44.0-7771a714-internal_x86_64.AppImage` has SHA-256 `469477d81105e8f3e98ba1e83194cbdf3ba7c9d099bbe994dce19ebd31e9ce24`; its archived name/environment are `WireInternal`/`internal`, desktop Exec is `AppRun %U`, AppRun contains no `--no-sandbox`, and all six configured fuses match. The ASAR SHA-256 is `37f0c04553d5ee71f198457307c7627ae5544a9cfc83242a35dfbece81e0cc29`; content verification passes 7,338 entries/22 required files, and its 407-package inventory is byte-identical to the approved scan input (SHA-256 `a9cabd94678d377220da6f05d4ee7fd2ae84dba9443920d87b99b1c434eebca5`). No new package metadata was submitted. Root/bin/Electron-test types, forced scoped lint, production TypeScript/webpack and all **380** build-tool cases pass on the scoped branch. The first full-suite attempt failed 33 cases because this new worktree lacked ignored public `.env.defaults` and `resources/macos/Info.plist.json`; copying those existing public generated inputs restored the suite without changing tests or tracked source. The local build logs are ignored at `wrap/recovery-artifacts/appimage-sandbox-{build,internal-build}.log`; the initial and restored complete-suite logs are `/tmp/pkg001-appimage-sandbox-bin{,-restored}.log`. Direct mounted-AppImage launch and hosted effective sandbox checks remain open.
