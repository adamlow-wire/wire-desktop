# Upstream `dev` delta review — September 24, 2026

Scope: compare the current upstream `wireapp/wire-desktop` `dev` at `5504073a7b124e87c9ee769e5dee8f48511deba6` with the last upstream ancestor of `integration/electron-modernization`, `6f9b6a994500f0fc0ad64e60882ac9f5b099d5f2`. The upstream tip was fetched and confirmed against GitHub on September 24. There are 27 upstream commits across 12 first-parent PR merges; the comparison candidate is the still-unmerged unsigned PR #63 head `572bbf25`. This is a source-level relevance review, not a merge, regression run, or approval of upstream's release artifacts. The reviewed candidate remains unchanged.

| Upstream PR | Difference relative to our candidate | Disposition and owner |
| --- | --- | --- |
| [#9739](https://github.com/wireapp/wire-desktop/pull/9739), [#9745](https://github.com/wireapp/wire-desktop/pull/9745) | Windows App-lock override now requires an enabled Wire policy value; MDM enrolment or Entra join alone no longer enables it. Our Windows backend still returns true for those unrelated device markers, and tests explicitly expect that old behavior. | **Carry forward before handoff**, CAP-005. Record a passing old-behavior control and a failing new policy-only target before changing implementation; verify explicit HKLM/HKCU policy remains honored and absent/disabled policy stays false. Update the managed-device contract deliberately, not by blind merge. |
| [#9738](https://github.com/wireapp/wire-desktop/pull/9738) | Exposes optional OS regional locale to the webapp for date formatting. Our `DesktopAppConfig` lacks it. | **Carry forward for product parity**, CAP-001/CAP-005. Read locale after Electron readiness and pass it through the existing main-owned account bootstrap/isolated bridge. Do not restore upstream's unrestricted synchronous preload IPC. Test unavailable locale and unchanged managed settings. |
| [#9743](https://github.com/wireapp/wire-desktop/pull/9743) | Raises total desktop-log retention from 100 to 500 MiB because observed 40–60 MiB/day made the previous cap defeat the intended seven days. Our candidate retains 100 MiB. | **Carry forward after privacy/disk-bound review**, CAP-001/TST-006. Reuse the upstream retention test cases and verify active files and seven-day/size ordering. The larger disk and sensitive-log exposure is a deliberate product tradeoff, not a security patch. |
| [#9748](https://github.com/wireapp/wire-desktop/pull/9748) | Replaces the legacy runtime `open-graph` package with `@wireapp/open-graph`. Its desktop result puts the fetched image data URI in `image.url`, while the current webapp still reads `image.data`. Our candidate retains the latter shape. | **Keep the candidate's `image.data` contract; do not copy the upstream result shape or old fetch/parser path**, SEC-012/DCP-015. The candidate already uses a bounded metadata tokenizer and public-only, address-pinned fetch across redirects. Retain the existing `openGraph.test.main.ts` `image.data` assertion, reconfirm the webapp consumer at final sync, and flag the upstream mismatch for Wire review. Type-only `open-graph` declarations can be resolved under ELC-003. |
| [#9750](https://github.com/wireapp/wire-desktop/pull/9750), [#9751](https://github.com/wireapp/wire-desktop/pull/9751) | Removes unsupported `smctl` flags, verifies each signed EXE/Setup/MSI immediately, and excludes Squirrel's later injected staging `Squirrel.exe` from final app verification. Our Jenkins file still has the old flags and verifies every EXE. | **Required before signed Windows qualification**, PKG-002/SEC-011. Port with fail-closed static tests and test on Wire's actual signing worker. The unsigned PR #63 checks do not exercise this credentialed path. Keep verification of every distributed Wire executable and installer. |
| [#9752](https://github.com/wireapp/wire-desktop/pull/9752) | On Windows, native Alt menu handling and our `before-input-event` handler both toggle the auto-hidden menu. Upstream skips the extra handler on Windows. Our code retains it. | **Carry forward for Windows behavior**, CAP-004. Adapt the native-menu regression because upstream's E2E fixture still searches for a legacy `webview`; test the current main-owned account view. |
| [#9636](https://github.com/wireapp/wire-desktop/pull/9636) | Updated translations, including previously English sidebar labels and Wire Gov strings. | **Include in a reviewed upstream sync**, CAP-004/product localization; no security-boundary code. |
| [#9744](https://github.com/wireapp/wire-desktop/pull/9744) | Points legacy Jenkins Windows/macOS smoke jobs at Hosted Dev. | **Reconcile CI environments** under TST-005. Our modernization GitHub E2E already uses Hosted Dev; do not change production backend selection from this test-only PR. |
| [#9741](https://github.com/wireapp/wire-desktop/pull/9741) | Removes obsolete Linux Jenkins build/source-signing and Linux Jenkins S3 deployment route. | **Reconcile release pipeline before M5**, PKG-001/PKG-002; our GitHub unsigned Linux packages are separate and do not establish the replacement signed repository process. |
| [#9742](https://github.com/wireapp/wire-desktop/pull/9742) | Disables Dependabot updates in favor of Renovate. | **Review in normal repository sync**, ELC-003/GOV-001; no shipped runtime behavior. Confirm the fork has an active dependency-update path before adopting the bot setting. |

The upstream branch still pins Electron 38.8.6 while the candidate pins 43.4.0. Do not merge upstream wholesale or downgrade the runtime/security boundary. Implement the applicable behavior as scoped work under existing owners, then perform a separate reviewed upstream merge and qualify its composed tree. The existing [TST-006 quality review](quality-review.md) and F-012 ciphertext ownership decision remain independent handoff blockers. This delta review does not complete the holistic code review.

The preview result-shape check above compares [desktop `dev` `5504073a`'s `updateMetaDataWithImage`](https://github.com/wireapp/wire-desktop/blob/5504073a7b124e87c9ee769e5dee8f48511deba6/electron/src/lib/openGraph.ts) with [webapp `main` `4b361a4d`'s `toLinkPreviewData`](https://github.com/wireapp/wire-webapp/blob/4b361a4ddda83efdf8dca15864c309a0c3e32e3e/apps/webapp/src/script/repositories/conversation/linkPreviews/index.ts), both inspected on September 24. The latter reads `(openGraphData.image as {data: string})?.data`. This proves a source-level mismatch for that webapp revision; it does not prove which webapp revision every deployment serves or that a preview has failed in production.
