# Fresh-session prompt

```text
Continue the active goal: complete M3 and functional M4 before release packaging (CAP-003, CAP-004 and development/test TST-005). Do not inherit a percentage or blocked verdict. Read AGENTS.md and its six modernization documents in order, then m3-acceptance.md. Audit actual branches, code, PRs, CI and remaining acceptance criteria.

Integration is PR-only in fork adamlow-wire/wire-desktop. Scoped publication and SHA-guarded self-merge are authorized after substantive exact-head review, every applicable final-head check and no unresolved threads. Never push upstream origin. Preserve user changes, the MSI worktree wrap/worktrees/wpb-5221-deployment-audit at255bdd54 and protected artifacts. No broad cleanup, clear:wrap or local build:prepare. No subagents.

The maintainer paused testing because GUI windows interrupted their work. Do not run native Electron or application Playwright suites on the shared display. Use hosted CI or genuinely isolated execution. API-only fixtures, types, lint and read-only trace analysis are safe locally. Never bypass sandbox, native secure storage or permission policy to make tests run.

Integration6930c8ce includes accepted M3 through PR59 (queued-log cleanup) and functional TST-005 foundation PR58. Both merged after exact-head review, required CI, actual CodeQL/tree verification and protection readback. The M3 audit records all acceptance criteria and earlier startup/diagnostic corrections. Additional integration E2E34975642639 failed calling visibility despite the previously qualified identical tree; retained Windows traces show calling-service unreachable/offline, without a proven underlying network cause. Preserve this evidence.

Existing CAP-003 work is in /tmp/m3-cap005-packaged, branch cap/CAP-003-consented-display-2026-09-15, draft PR60 against actual integration. Prefer qualifying it to creating another branch. Current status identifies the latest published candidate. Production uses DEC-011's fixed local chooser/broker, explicit source selection, bounded approved pixel relay and parent-bound PiP. Native remote account/PiP display and legacy empty-media capture stay denied. Source lists/IDs never reach remote views. No implementation defect is currently identified by source review; keep actual OS capture/privacy/performance qualification explicit in qa-display-capture.md.

Candidate7419d3d8 passes build, CodeQL (analysis1779726987, zero findings/no error) and all native/package platforms initially, including24 product cases each. Full E2E34980648667 passes75 macOS initially and75 Linux with one registration409 retry. Windows passes74 but exhausts multi-account notifications: all three traces show the second account opens the new MLS direct conversation before the measured message, defeating the unread precondition. A fixture correction establishes/reads that conversation first, then selects/asserts Distraction Group and zero unread. It preserves all badge and activation assertions. Separate650a00c7/00435dec baseline/fix uses existing random fixture identities for unique email addresses; all16 API-only cases and sensitivity/restoration pass. Lint flags only an extra status blank line, fixed locally. Revalidate publication and qualify the subsequent exact head; old-head results are not final acceptance.

A runnable isolated Windows preview has been delivered: actions/run34980648583, artifact10401532042, from7419d3d8. The complete ZIP includes WireM4Preview.exe, dependencies and Start-Wire-M4.cmd, with a separate adjacent Data profile and distinct wire-m4-preview protocol. Read manual-windows-preview.md. It is unsigned functional preview scope, not release/installer/update acceptance. Profile separation does not isolate server-side account effects. Use test accounts; ordinary wire:// and browser SSO target installed Wire. The package identity and immutable managed-config/Node-denial smoke passed; no local GUI testing is needed to reproduce this artifact.

Keep Electron43.4.0 and all invariants. Preserve existing built-in pins plus the explicit native process-wide override until restart, always retaining Chromium trust validation. No MDM pinning setting exists; do not invent one. Live Keycloak/SSO/OIDC/ACME enrolment, verified-device persistence/restart/renewal remain downstream QA per maintainer scope, not M3 compatibility claims.

Finalize CAP-003/CAP-004 criterion mapping and functional TST-005 evidence only after qualification. Strict integration protection requires ten core/native/Windows/macOS/Linux/full-report checks, with admin enforcement, conversation resolution and no force pushes/deletion. Preserve failed/retried/superseded attempts; report success never qualifies a failed platform. Avoid manual E2E dispatch because it sends external notifications. PR-triggered CI is authorized.

Ask only for genuinely required access or product decisions and continue useful unblocked work. Update status.md before handoff. PKG-001 and remaining packaged TST-005, signing, installers, updates, migration/rollback, independent release review and upstream rollout remain later. The overall goal is complete only when M3 and authorized functional M4 are demonstrably accepted and integrated.
```
