# Fresh-session prompt

```text
Continue the active goal: complete M3 closure documentation, then finish functional M4 before packaging (CAP-003, CAP-004 and applicable development/test TST-005). Do not stop after M3.

Read AGENTS.md and its six required modernization documents in order, then m3-acceptance.md. Audit actual code, branches, PRs and final-head CI; do not inherit percentages or blocked verdicts. Integration is PR-only; push only to fork (adamlow-wire/wire-desktop), never upstream origin. Preserve user changes, wrap/worktrees/wpb-5221-deployment-audit and protected MSI artifacts. No broad cleanup, clear:wrap or local build:prepare. No subagents.

M3 runtime is accepted at45840fca after reviewed PR56, tree-equal to5c596187. All final build/lint/analysis/native Windows/macOS/Linux and full48-case Windows/macOS E2E/report pass. macOS has48 initial passes; Windows47 initial plus one fixture setup retry. Trace shows a logged-in account without a handle at /auth/#/sethandle; fixture username construction admits apostrophes and setUsername ignores unsuccessful responses. TST005 owns that fixture correction. The actual native startup redirect teardown found in PR55 is fixed and sensitivity-tested. Documentation-only closure reuses the identical qualified runtime full E2E as a documentation-only record of the already executed M3 closure checkpoint but still requires its applicable automatic final-head checks. Revalidate whether that closure is merged before continuing.

Whole-M3 final local validation:112 Jest,923 main,4 media,4 renderer,38 build tools, types/build,6 rebuilt Linux navigation/proxy product cases. Fresh coverage from completed-M2fe0b86cb:3245/3830 changed statements84.73%,840/853 tracked security branches98.48%. Durable exact-head links and criterion mapping are in m3-acceptance.md.

Maintainer scope: live SSO/Keycloak/OIDC/ACME provisioning and real enrolment/restart/renewal belong to downstream QA in qa-sso-e2ei.md, not an M3 compatibility claim. Preserve existing pinning policy: built-in pins plus explicit native process-wide override until restart, never bypassing Chromium validation. No MDM pinning policy exists; do not invent one. Preserve Electron43.4.0 and all security invariants.

Reuse existing work and keep one primary work item per branch/PR. Add sensitive tests before behavior changes, demonstrate failure with a temporary perturbation and restore. Native GUI suites run serially; build TypeScript then bundle before consuming assets. A macOS project label executed on Linux is Linux evidence only. Use owned profiles/private native keyring; never plaintext storage or permission bypasses.

Functional M4: qualify existing menu/tray/badge/notification routes under CAP004; add truthful Linux/mandatory development E2E, artifact retention, type/codegen and fixture quality under TST005; implement consented display capture and parent-bound PiP lifetime under CAP003. /tmp/m4-functional-preflight.md and /tmp/m4-drafts contain researched drafts and passing synthetic probes, not production implementation. Electron43.4 gives legacy and modern capture the same empty-media permission shape. Remote account/PiP native capture must stay denied. A fixed trusted local chooser/broker can capture only a main-selected source and relay bounded approved frames into a generated stream. Actual secure-scheme/context-bridge/port/clone-stop probes pass using only a test-owned synthetic frame. Do not substitute a main-world permission override or first-source fallback. Record the architecture decision before implementation.

Scoped PR publication and self-merge are authorized after substantive exact-head review, all applicable checks, no unresolved review threads and SHA-guarded merge. Preserve failures/retries; skipped or older-head jobs are not final evidence. Ask only for genuinely required access/product decisions; continue useful unblocked work. Update status.md before handoff. Packaging/signing/installers/updates, released-profile migration/rollback and independent release review remain later. The overall goal is complete only when M3 and all authorized functional M4 work are demonstrably complete.
```
