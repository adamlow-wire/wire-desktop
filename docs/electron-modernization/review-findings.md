# TST-006 execution ledger

**F-039 — signed-release pipeline defect, SEC-011 / TST-006.** `jenkins/windows.groovy` verifies every Wire Gov application EXE under `production || wireGov`, but before the scoped correction its `Sign application` stage ran only under `production`. A Wire Gov build with `WIRE_GOV` selected and `PRODUCTION` unset would reach verification without first signing the application; this is a source-confirmed release-path failure, not a signed artifact exploit or an observed Jenkins run. Test-only baseline `ef9f6506` fails stage-parity with one neighboring artifact control passing. Scoped `a28a324a`, composed as `26979240`, aligns the signing condition; six local Jenkins artifact/deployment tests, forced test lint and bin types pass. Signed-artifact verification, signing identity, real Wire Gov Jenkins run and release acceptance remain Wire-owned under SEC-011.

**F-038 — CI/test blocker, TST-006 startup fixture / PKG-002.** [Draft PR #72 Build and Test run `36073742060`](https://github.com/adamlow-wire/wire-desktop/actions/runs/36073742060) passed 1,290 main-process tests and failed seven updater-startup cases. All seven failed before their routing assertions because `electron/test/fixtures/updater-startup.cjs` omitted the new `app.getSystemLocale` API and used an empty logger; the production callback caught the missing API and called `logger.warn`, which the fixture did not provide. This is a synthetic fixture drift, not evidence of an actual startup or update failure. A local test-only correction models a fixed `en-GB` locale, the regional-locale state and a fixed-warning lookup failure; the existing updater assertions remain. Nine inert Node cases, test types, lint and format pass, and a temporary wrong-locale perturbation fails the new assertion before restoration. The correction is committed locally as `644a3dc3` but not yet pushed or qualified by hosted native tests; the failed run remains in the record.

**F-037 — medium candidate, SEC-008 context-menu image buffering / TST-006.** `AccountContextMenu.ts` sends a clicked account image URL to `preload-context.ts`; `savePicture` currently calls `response.arrayBuffer()` before the 15 MiB `SavePictureIpc` request limit can reject it. A user-initiated save of a remotely controlled large response can therefore allocate the full response in the account renderer first; the detached promise also has no local rejection handler. This fetch pattern predates modernization, while the new main-side size limit does not protect the earlier allocation. Source reachability is confirmed; a bounded synthetic response regression and real memory impact are **not** yet reproduced. Treat this as an open SEC-008 resource-bound/test gap, not a proven exploit or incident. Determine a streaming/abort and fixed-diagnostic correction under SEC-008, prove a failing pre-fix test, and requalify the composed result before handoff.

**F-036 — high, SEC-003 proxy credential rejection diagnostic / TST-006.** The production proxy prompt preload passes submitted username/password to `submitProxyPrompt`, whose shared rejected-IPC catch forwards the raw Error to `console.error`. A synthetic credential-bearing rejection on the pre-fix four-fix source is captured by the logger; test-only baseline `a2d58e5d` runs 16 existing controls successfully and fails the new fixed-message-only assertion once. This proves a credential-bearing diagnostic sink when an IPC rejection contains that value, not that a real user credential was logged or that all Electron IPC errors echo request data. Additional test-only `ba3890f9` exposes the same raw-error behavior in locale/cancel catches; scoped implementation `fa40a7a4` uses only fixed messages and passes 18 local proxy IPC cases, TypeScript, lint and bundling. This composition includes the fix as `073d0521`; scoped [run `36070507112`](https://github.com/adamlow-wire/wire-desktop/actions/runs/36070507112) passes complete Linux/macOS jobs and Windows native/product/package build steps, but its installed Squirrel/MSI smoke ended with a job failure near the one-hour bound without a completion marker or available step log. Final composed qualification/re-review remain pending. This is still a mandatory engineering-handoff blocker.

**F-035 — high, SEC-008 child-frame popup origin confusion / TST-006.** A separate test-only baseline `e983f58a` on exact composed application `0fd4679a` creates a committed cross-origin child frame, opens a `noopener noreferrer` external link from that frame, and requires no external action. [Hosted native run `36063921163`](https://github.com/adamlow-wire/wire-desktop/actions/runs/36063921163) fails that unchanged deny assertion on Windows, macOS and Linux: the account `setWindowOpenHandler` calls `openExternal('https://example.test/foreign-child')` after the empty referrer is accepted against the top-level `contents.getURL()`. The ordinary top-level noreferrer positive case still passes. This proves an external-link origin-policy bypass in the fixture on all three platforms, not OS code execution or a confirmed SSO/PiP/deep-link exploit. Those other outcomes share the policy input and require separate impact review. [Fork draft PR #71](https://github.com/adamlow-wire/wire-desktop/pull/71) carries a scoped one-use account-main-frame grant fix. Its first candidate [run `36067600554`](https://github.com/adamlow-wire/wire-desktop/actions/runs/36067600554) denied the child but broke top-level chat/deep links on all three OS; the revised code at `3386c9e1` passes [Windows/macOS/Linux native, product and unsigned package jobs](https://github.com/adamlow-wire/wire-desktop/actions/runs/36068535525), including the child denial, ordinary external/deep links, PiP and SSO. One-use target-grant sensitivity is proven locally. This final-composition branch includes that source as `16c35ef7`; SEC-008 remains open until this composed tree is re-reviewed and qualified, so F-035 still blocks handoff.

**F-002 / TST-005 macOS fake-media body timeout follow-up:** [Draft PR #70](https://github.com/adamlow-wire/wire-desktop/pull/70) keeps the exact legacy-capture denial assertion and gives only its body a finite ten seconds after the `0fd4679a` hosted macOS request stalled before reaching that assertion. The test-only change passes types/lint/format locally and is now included as `c832da76` in the final-composition candidate, but its changed native case is not yet qualified on that tree. It does not diagnose a product media-policy failure; compose with other reviewed fixes and run one final exact-head native/package qualification.

**September 24 exact-head ELC-003 screening:** Downloaded Squirrel and Linux deb ASAR inventories from `0fd4679a` are byte-identical to the previously reviewed 351-instance Windows and 407-instance Linux inventories; all Windows name/version pairs occur in Linux. The authorized npm metadata query refreshed at `2026-09-24T21:32:11Z` finds zero high/critical, one moderate `uuid` 9.0.1 and one low `@tootallnate/once` 2.0.0 range match across Linux; Windows lacks the low package. Earlier bounded call-path dispositions still apply to these exact versions. [Full scope and hashes](shipped-linux-asar-audit.md#september-24-exact-four-fix-composed-windowslinux-graph). Exact-head macOS remains unbuilt, so ELC-003 is not closed.

**September 24 F-004 actual unsigned package spot audit:** On `0fd4679a`, the downloaded Squirrel full nupkg's ASAR has 9,043 entries and SHA-256 `f2bba2fedcf99e56d43ec687f832172550aa9687b03e699147dd0e8b7b1ebbf2`, matching the separate MSI payload manifest. The downloaded Linux deb's 7,378-entry ASAR has SHA-256 `4f3cd7ab0bae18fe34c3ae58231dcefe33729f8f0ea321822234e3237308eaf9`, matching the AppImage/deb/rpm CI manifest. Targeted scans of those ASARs and their directly inspected outer Squirrel/deb file lists find no `.env`, private-key extensions/directories, E2E outputs, or root build-source trees. This narrows F-004 for Windows/Linux; it does not qualify unbuilt macOS, arbitrary embedded secret bytes or every MSI/AppImage outer file. Thirteen third-party `logdown` test files remain in Windows ASAR as a hygiene item, not a demonstrated secret. [Exact scope](review-pass-2026-09-24.md).

**September 24 composed finding reconciliation (`0fd4679a`):** F-005 build diagnostics (`e2920196`, `5e4c23b6`), F-009 SSO diagnostics (`b58de5a1`) and F-013 bounded logging (`a8e0854f`) are ancestors of the exact source candidate; historical “open” discovery rows below do not mean those source fixes are missing. F-018's fixed-message deep-link catch is present through `308fceb4`, and F-019's HTTPS/SOCKS proxy-rule correction is present through `73f76118`, although the separately recorded scoped remediation commits are not ancestors. F-012 account-owned ciphertext remains unimplemented and blocks handoff pending the existing-profile contract decision. All present fixes still require final-tree source review and appropriate native/platform evidence. [Run `36058028533`](https://github.com/adamlow-wire/wire-desktop/actions/runs/36058028533) passes Linux; Windows passes full native/package and installed Squirrel/MSI smoke; the newly selected native frame test runs successfully on Windows and Linux. macOS stops before packaging on a two-second fake-media test-body timeout during the first consented device request; its legacy-denial assertion and later F-034 frame test did not run. Do not count a platform or assertion as passing because another platform passed it.

**F-002 / TST-005 corrected-head Windows logout retry:** [PR #63 head `cf0c5279` E2E `35919415510`](https://github.com/adamlow-wire/wire-desktop/actions/runs/35919415510) has three successful OS jobs. Retained blobs have 76 clean first-attempt passes each on Linux/macOS. Windows has 77 attempts for 76 tests, with no worker error: the first `Logout flow` attempt submits login and clicks history confirmation, then fails the 120-second wait for CA’s authenticated account page; the retry passes. The failure occurs after the earlier account-add point and before a proved logout assertion. The snapshot/trace is retained privately, and neither a desktop account-load defect nor a fixture selector issue is yet proved. Preserve the target-account assertion and investigate the transition before accepting TST-005.

**F-002 / TST-005 hosted Windows fake-media setup stall:** [PKG-003 exact head `befbf8d5` baseline `35919774438`](https://github.com/adamlow-wire/wire-desktop/actions/runs/35919774438) passes seeded packaged legacy migration on Linux/macOS, but Windows exits before packaging. Native main passes 257 cases; `test:media` times out in its first `beforeEach` while creating/loading an account view, after the existing 1.5-second phase warning and through the ten-second hook bound. No media permission assertion ran. Local test-only `03ed663f` extends only native setup to a finite 20 seconds and records fixed view/loading/crash state at nine seconds; focused types/lint/format pass. Do not infer a production media-policy failure or Windows package acceptance; requalify and investigate the account-load phase.

**F-002 / PKG-003 hosted Windows schema fixture timeout:** [PR #63 corrected head `cf0c5279` package `35919415394`](https://github.com/adamlow-wire/wire-desktop/actions/runs/35919415394) passes Linux/macOS native/package gates but stops on Windows before installer creation. The 506-case native product step has 505 passes and one two-second Mocha timeout in a synchronous test that writes a current settings file plus corrupt legacy file, then asserts both retain exact bytes. The trace shows no assertion mismatch and does not prove production data loss. Local test-only `f762c584` bounds that one filesystem case at ten seconds without changing its assertions; hosted requalification is pending. No Windows artifact from this run is accepted.

**F-002 / TST-005 final bit-off-head Windows registration retry:** [PR #63 head `dee1eedb` E2E `35915040298`](https://github.com/adamlow-wire/wire-desktop/actions/runs/35915040298) has three successful OS jobs and a successful merged report. Retained blobs show macOS/Linux 76 clean passes each; Windows has 76 passes from 77 attempts. “I want to register a new Wire account” fails its first attempt after handle submission because the expected usage-data consent dialog is absent for its 60-second assertion, then passes on retry. The failure snapshot shows only “Checking for new messages”, and the sanitized trace contains a 500 followed by 409 from the MLS commit-bundles endpoint near the transition. It does not prove whether backend signup completion, desktop account navigation or the webapp modal transition stalled. Preserve the consent assertion and investigate the page/phase transition; do not accept the retry as clean TST-005 qualification.

**F-002 / TST-005 hosted Windows account-add retry:** [PR #63 head `80c7ecf7` E2E run `35910739161`](https://github.com/adamlow-wire/wire-desktop/actions/runs/35910739161) passes all three platform jobs and the merged report. The retained Windows blob artifact `10774546697` has 77 attempts for 76 tests: “Logout from the secondary account while the first one is active” fails once before logout, because `accountsSidebar.addAccount()` does not observe a new visible account within its ten-second poll after a successful plus-button click. The retry passes. macOS/Linux each have 76 passing attempts and no worker error. This is not evidence that logout failed, and a successful retry does not diagnose whether account creation, main-owned selection or the fixture wait stalled. Keep the account-add/selection assertion intact and require bounded phase evidence plus clean final-head execution.

**F-002 / TST-005 hosted macOS native-exit regression:** [PR #63 e760 E2E run `35905939092`](https://github.com/adamlow-wire/wire-desktop/actions/runs/35905939092) passes Windows/Linux 76/76 each but fails macOS. The zero-exit restart case starts the inspector `Evaluate` request, polls `child.exitCode` for ten seconds and observes only `null`; `quitApp` then awaits native close until the 90-second test timeout, followed by a 90-second worker-teardown timeout. The retry passes, leaving 75 macOS passes, one flaky test and one non-test error; the macOS job exits 1. This is a lifecycle reliability failure, not evidence that the product app cannot normally exit. A final-head pass plus fail-closed, bounded native cleanup with a sensitive regression remain open; the trace does not identify why this first child stayed live.

**F-031 S3 promotion and SEC-011 fuse follow-up:** Windows release promotion separately used unversioned `find` calls before deleting static S3 keys. Inert CLI baseline `20c61806` fails requested-version/product selection; local `453273f2` calls the same validated `S3Deployer.findUploadFiles(..., version, "squirrel")` before any deletion. Four focused CLI controls, bin types and the composed 423-case tooling suite pass; no remote release action occurred. The GitHub draft CLI still selects by extension alone, including potentially stale Windows/Linux assets; macOS PKG output is unversioned. This remains a signed-release selector risk under PKG-002, not an assertion of an actual wrong release. SEC-011 baseline `873460bf` and candidate `3350819f` attempted to turn extra file privileges off. [Hosted head `dee1eedb` package `35915040701`](https://github.com/adamlow-wire/wire-desktop/actions/runs/35915040701) fails fresh-profile smoke on Windows/macOS/Linux: the one-time file-origin reader returns `ERR_FILE_NOT_FOUND` from `app.asar`, so no account page reports. The bit-off candidate is rolled back to the earlier hosted `010001011` wire; `bb1d3dc4` fails on the old checker accepting bit 7 off, and `dfbc1c1f` requires bit 7 on in Windows/macOS/Linux package gates. Three focused cases pass; actual seeded legacy-profile package proof remains pending. [Electron fuse guidance](https://www.electronjs.org/docs/latest/tutorial/fuses) advises bit-off only when no local file page is served; this app still has a JavaScript-disabled legacy reader.

**F-031 — high mixed-version Squirrel deployment selection / PKG-001, TST-006, DCP-017.** The production S3 uploader accepts a requested Windows version but used an unversioned `*-full.nupkg` lookup. A controlled fixture with old/new full packages returned the old package for the new requested version, while `RELEASES` named the new package. Test-only baselines `f3764f3b` and `1554b8f8` retain four existing controls and expose five failing version/absence/ambiguity/Setup/RELEASES targets. Scoped local `2136672b` requires one exact-version full package, matching Setup identity (including the real builder's case difference), and matching RELEASES in one directory. Eleven focused S3 cases, 25 S3/deployment/pipeline cases, the full 422-case build-tool suite, bin TypeScript and scoped lint pass. In-memory removal of the exact-version filter or RELEASES check fails its target and restores. Read-only selection of the downloaded Windows installer set from [package run `35905939007`](https://github.com/adamlow-wire/wire-desktop/actions/runs/35905939007) returns its real full nupkg, RELEASES and Setup paths; it does not upload anything. This is a conditional wrong-release risk, not a real deployment incident. The upload fix is published at `80c7ecf7` and needs hosted final-head qualification. S3 promotion has a separate local baseline/fix above; GitHub draft selection retains a release-time disposition.

**F-024/F-006 tenth hosted Windows package result:** [PR #63 package run `35905939007`](https://github.com/adamlow-wire/wire-desktop/actions/runs/35905939007) passes both installed unsigned Windows installer families, managed/proxy account startup, archive identity, effective fuse wire `010001011` in both installed executables, cleanup and upload of all four deliverables. Downloaded bytes/hashes and cross-platform shipped graph are in the [tenth-run audit](shipped-linux-asar-audit.md#tenth-pr-63-exact-head-unsigned-windowsmacoslinux-audit--september-23). This qualifies the scoped unsigned installed-app checks on head `e760f10e`; it does not close signed updates/MSI upgrades, SEC-011 extra-fuse disposition, F-031 final composition or TST-006 full review.

**TST-005 eighth macOS logout retry:** [PR #63 E2E `35895375630`](https://github.com/adamlow-wire/wire-desktop/actions/runs/35895375630) passes the corrected DCP-007 negative call case on all three platforms. Windows/Linux complete 76/76. macOS reports 75 passing and one flaky `Logout flow` case: after clearing Account A data and submitting login/history confirmation, its first attempt waits 120 seconds for the reauthenticated account page; retry passes. The retained private blob `10768841287` contains failure context and trace. The snapshot at failure is an authenticated Account B view, but this alone cannot distinguish lost Account A startup from the login helper's candidate-page filter overlooking an existing view. Network trace includes expected and failed status ranges without a proved causative request. Treat the retry as an open TST-005 qualification issue; preserve both attempts, investigate the page/account transition and require a clean final-head run. No assertion or timeout is weakened.

**F-024/TST-005 eighth Windows fixture repeatability:** [PR #63 package run `35895375588`](https://github.com/adamlow-wire/wire-desktop/actions/runs/35895375588) reaches 221 passing account-lifecycle cases before `AccountController.test.main.ts` times out in `controller.start()` at `starting account views`; cleanup stalls at `clearing session 1` for over 23 seconds. Several later hook/filesystem/profile failures follow in the same Electron process, and no Windows package is built. This matches the _phase_ seen in the earlier independent [SEC-003 Windows diagnostic](https://github.com/adamlow-wire/wire-desktop/actions/runs/35029234576/job/104583548074), not proof of identical root cause. Existing per-case fixture generated two new persistent Chromium partitions each time; local `fdfcedd1` reuses two distinct partitions for the suite while retaining per-case cleanup and account isolation. The eighth failure is the baseline; the change has types/lint/format checks but no hosted outcome. Do not accept repeatability or installed artifact delivery until a fresh run passes; investigate further if the stall recurs.

**F-024 seventh hosted Windows checkpoint and artifact handoff:** [PR #63 package run](https://github.com/adamlow-wire/wire-desktop/actions/runs/35890398583) passes native account/calling boundaries, creates Setup.exe/full nupkg/RELEASES/MSI, verifies unpacked and Squirrel ASAR identity, extracts an MSI containing the same ASAR, and reports the installed Squirrel/MSI account smoke step successful. The common Windows ASAR SHA-256 is `2946311cc02f122668cd03f887295a00f24fb228135e35f9d3960c4496a70fb9`. The package job's upload of both the entire unpacked build tree and four deliverables is still active after the installed step; no completed Windows artifact or final package-job pass is inferred. Test baseline `9997fb9f` fails when the workflow retains that broad upload; `ea9ee0ae` narrows it to the four required installers and makes a missing upload fail. Ten focused Windows cases, bin types, scoped lint, YAML parse and formatting pass. Hosted delivery of that correction, exact installer hashes and final effective Windows fuses remain mandatory.

**F-006 installed Windows fuse gate:** Baseline `d91028f6` fails three targets because no installed executable fuse check existed. `eaec8b35` adds a small V1 reader to the shared Squirrel/MSI installed archive check: it requires nine indexed binary fuse positions, enforces the six configured unsigned security bits, and prints the complete raw wire for the remaining three-position review. Thirteen focused Windows tests, bin types/lint/format, direct execution against the downloaded seventh-run Linux Electron binary (`010001011`), and an in-memory changed-RunAsNode expectation sensitivity check pass. The first implementation incorrectly assumed the library returned a Buffer; the real binary check exposed its indexed object and the corrected tests model that object. Hosted Windows execution and SEC-011 disposition of the file-protocol privilege bit remain open.

**F-006 effective-fuse review extension:** Actual seventh-head unsigned macOS and Linux binaries both have nine V1 fuse bytes `010001011`; the existing package gates assert only the first six configured values. [Electron's fuse schema](https://www.electronjs.org/docs/latest/tutorial/fuses) identifies the remaining positions as browser-specific V8 snapshot off, file-protocol extra privileges on and WASM trap handlers on. The pinned `@electron/fuses` 1.8.0 enum lacks the ninth name even though its reader returns that byte. The only production `file://` page found is the temporary legacy-profile reader with scripts off, no preload/webviews and deny navigation; remote account pages use the custom origin. This bounds the enabled default but does not establish that it is needed or that all package formats share it. SEC-011/PKG-003 must disposition it with the existing-profile migration path and verify the complete effective wire on final Windows/macOS/Linux deliverables. No unsigned artifact is treated as signed-integrity proof. [Exact artifacts](shipped-linux-asar-audit.md#seventh-pr-63-exact-head-macoslinux-audit--september-23).

**F-024 hosted installed-smoke duration.** On the [fourth Windows package job](https://github.com/adamlow-wire/wire-desktop/actions/runs/35874780880), the installed Squirrel/MSI step remained active for more than ten minutes without a result or phase marker. Its `Start-Process -Wait` calls were not step-bounded. Baseline `60db1daa` fails the missing CI duration bound; the scoped workflow correction sets a 15-minute limit and the PowerShell driver emits fixed start/finish labels for each setup, smoke and uninstall phase, without arguments or credentials. Eight focused tests and YAML parse pass; removing the bound fails its target and restoration passes. The next hosted run must reveal the waiting phase and complete both installed paths before F-024 acceptance.

**F-024 hosted installed-policy correction.** [PR #63 third Windows package job](https://github.com/adamlow-wire/wire-desktop/actions/runs/35872062149) passed all four installer deliverables, Squirrel full-package archive identity, MSI administrative extraction/archive identity, and unpacked managed/proxy account smoke. Silent Squirrel installation produced a runnable installed EXE with the expected ASAR. Its account smoke then returned `applockOverride=true` while the driver assumed `false`, so MSI installation was not reached. Windows management detection also accepts machine enrollment and machine policy; deleting the temporary HKCU value from the preceding step cannot establish an unmanaged runner. Baseline `08668b8c` fails a new installed-policy target. The scoped correction gives each installed Squirrel/MSI smoke an owned temporary HKCU managed fixture, requires the exact `true` result, refuses preexisting policy, and removes its value/environment even on failure; the MSI proxy challenge remains a separate assertion. Seven focused Node cases pass; temporarily omitting the exact expectation fails the new target and restoration passes. PowerShell and installed native results require a new hosted run; this is a test-fixture correction, not a product policy change.

**F-028 — medium nested native updater fixture loses the Linux display / TST-005, PKG-002.** In [PR #63 third Build and Test](https://github.com/adamlow-wire/wire-desktop/actions/runs/35872062230), the SUID sandbox helper was configured and 1,258 main tests passed, but 22 updater subprocess cases failed before assertions because their explicit child `env` kept only `PATH`/`SystemRoot` and discarded Xvfb's `DISPLAY`/`XAUTHORITY`. This is test-harness isolation, not a production updater finding. Baseline `3664f570` adds two Node cases for the minimal native fixture environment and fails before the helper exists. Source `5245dd16` keeps only `PATH`, `SystemRoot`, `DISPLAY` and `XAUTHORITY`, plus the caller's explicit fixture mode, in both updater subprocess suites. The two focused cases, test types and scoped lint pass; temporarily omitting `DISPLAY` fails the intended target and restoration passes. The [fourth Build and Test run](https://github.com/adamlow-wire/wire-desktop/actions/runs/35874780861) still reaches 1,258 passing main cases but the same 22 fixtures now time out: inside `electron-mocha`, `process.execPath` is the Electron binary, so passing a display starts the nested child in GUI mode rather than running the Node fixture. Separate baseline `3c22f1c9` fails three exact Node-mode environment targets; the local correction forces `ELECTRON_RUN_AS_NODE=1` after the allowlisted caller variables. Three focused cases and bin types pass; temporarily removing the mode fails all three targets and restoration passes. This remains a test-harness fix. The corrected main suite must pass hosted without weakening updater assertions or leaking other parent environment variables.

**F-027 — medium Windows ASAR verifier path mismatch / PKG-001, TST-005.** [PR #63 second package run](https://github.com/adamlow-wire/wire-desktop/actions/runs/35868771274) built all four Windows installer deliverables but again stopped at the prior archive-content gate, which reported an uncoded `Error`. The downloaded first-run Windows ASAR passes the same policy on Linux, so the content was not thereby proven bad. Installed `@electron/asar` lists files with platform separators and its Windows lookup traverses directories using `path.sep`; the verifier normalized listed paths to `/` for policy matching and incorrectly reused them for `asar.statFile` on Windows. Baseline `32c987a3` adds a real-ASAR fixture simulating Windows stat lookup and fails the nested-file target. Source `8922a4a4` converts only lookup names back to the native separator while retaining normalized policy names and all required/forbidden checks. One focused Windows-path case and the complete 400-case Node tooling suite pass; reversing the second lookup conversion fails the intended case and restoration passes. The third hosted run passes the Windows verifier and MSI administrative extraction. Installed Squirrel reaches account smoke; the F-024 policy-fixture correction and final-head qualification remain open.

**F-026 — medium Linux native package smoke selected the wrong executable / PKG-001, TST-005.** The [second hosted Linux package job](https://github.com/adamlow-wire/wire-desktop/actions/runs/35868771274) built and verified AppImage/deb/rpm archives, then failed before native startup because the deb/rpm smoke step searched only for `*-desktop`. The downloaded internal deb's archived `desktopName`, installed executable and desktop entry all use `wire-desktop-internal`; its runnable executable has no such suffix. Baseline `c5e57389` adds an owned real-ASAR fixture with that name plus missing/symlinked/unsafe-name negatives and fails before the resolver exists. Source `8922a4a4` selects exactly one executable from the archived identity, requires a regular runnable file and retains the separate helper check. Three focused cases and the complete 400-case Node tooling suite pass; forcing the old hard-coded suffix fails the intended case, and the resolver returns the correct path against the downloaded hosted deb. The [third hosted Linux package run](https://github.com/adamlow-wire/wire-desktop/actions/runs/35872062149) now launches the extracted deb and rpm and passes the packaged account, immutable managed configuration and page Node-denial smoke for each. Its following AppImage step still used the old `*-desktop` search and failed before launch. Test-only `ec42fcc4` fails that second selector target; `5245dd16` applies the same archived-identity resolver to AppImage, and reversing the workflow change fails again. Four focused Linux selector cases pass. The [fourth hosted Linux package job](https://github.com/adamlow-wire/wire-desktop/actions/runs/35874780880) passes with the corrected AppImage selector and its native smoke. Final composed-head qualification remains open.

**F-024 — high Windows installer qualification bypass / PKG-001, TST-005, DCP-017/DCP-018.** The required Windows Squirrel and MSI deliverables are not built by `.github/workflows/electron-modernization-baseline.yml`: its Windows matrix runs only `yarn build:win`, while the repository's installer targets are separate `build:win:installer:package` and `build:win:msi:package` commands. Its artifact existence check selects any `wrap/build/**/*.exe`, its content verifier reads the unpacked `wrap/build` ASAR, and its startup smoke launches the first non-Squirrel/Update executable in that unpacked tree. Thus a green Windows package job would not establish that either installer was created, contained the verified app, installed or launched. This is a static source finding; no claim is made about an actual hosted result on this local head. Add a failing gate for missing installer formats, build both unsigned installers in the existing PKG-001 flow, prove their payload/content identity and exercise their installed application paths on disposable Windows CI. Preserve Squirrel/MSI coexistence and the protected MSI worktree. Baseline: current workflow and `package.json` commands at candidate `46465040`; test-only `b7a1d004` has five failing targets. Scoped `4f10a6fb` runs both unsigned installer commands under consistent internal configuration, requires Setup.exe/full nupkg/RELEASES/MSI, checks the Squirrel app archive against the verified unpacked ASAR, and requests MSI administrative extraction plus identical archive verification on hosted Windows. Five focused tests and 395 build-tool tests pass; missing-format, payload-identity and omitted-build perturbations each fail their intended target and are restored. YAML and 25 Bash steps parse. Separate failing baseline `a1e995d8` adds the installed-app gate (five pass/one fail). Scoped `87f85212` requests silent Squirrel Setup and MSI installation on disposable hosted Windows, verifies each installed ASAR against the build, launches the installed EXEs through the existing account smoke, exercises managed/proxy behavior from the MSI install and uninstalls both. Removing this workflow step fails the new target; restoration passes six focused cases. No Windows installer ran locally. The third hosted Windows job passes MSI administrative extraction and Squirrel installation/ASAR identity, then stops at the installed Squirrel policy expectation before MSI installation. Administrative extraction checks bytes only; the owned installed-policy correction and MSI install/startup/cleanup need final-head hosted execution. F-024 remains open.

**F-022 — high missing packaged runtime images / PKG-001, DCP-005/DCP-020.** Actual earlier Linux ASARs passed the 22-file verifier while omitting `electron/img/logo.256.png`, `taskbar.overlay.png`, and the six platform tray/badge PNG variants selected by `TrayHandler`. `AboutWindow` also references the absent logo; Windows' custom download-complete dialog referenced a separate `download-dialog/logo@2x.png` that does not exist in generated assets. This is a package/runtime input failure, not a claim of an observed native crash. Baseline `7b532d29` has 26 controls pass/eight real-ASAR image targets fail; source `49ccd76b` makes the archive gate require those eight files (34 focused cases pass and an actual earlier AppImage now fails). Source `8cb97698` points the Windows dialog to that packaged logo. Baseline `c5e1aaab` detects the default Electron Linux installer icon (six pass/one fail); `b8a10744` selects the two supplied Wire PNGs (seven pass). The final `9e058e73` internal Linux installer trio contains 30 required files, with byte-exact installed Wire icons; all 388 tooling cases pass after the verifier change, with focused Linux tests passing after subsequent desktop changes. Direct native tray/About/Windows dialog and final platform qualification remain open. [Artifact evidence](shipped-linux-asar-audit.md#f-022f-023-branded-linux-installer-rebuild).

**F-023 — medium Linux desktop/window association mismatch / PKG-001, DCP-020.** The real prior AppImage and deb/rpm desktop entries had `StartupWMClass=WireInternal` but no archived `desktopName`; electron-builder warns that the running Electron window may not associate with the installed desktop entry. Baseline `30716e1f` has seven controls pass/one identity target fail. Source `196145ef` sets archived `desktopName` from the configured Linux executable, enables filename sync and aligns `StartupWMClass`; a configurable-name regression remains in `c3d22973`. The proposed hosted static check, executed locally, fails against the actual old AppImage and passes the rebuilt one; CI `32b31c2d` adds deb/rpm archive, fuse, icon, identity and extracted-native-smoke checks but is unrun remotely. The rebuilt trio has `WireInternal-desktop.desktop`, `Icon` and `StartupWMClass` equal to archived `WireInternal-desktop`. Visible desktop association on a supported Linux desktop is still untested. [Artifact evidence](shipped-linux-asar-audit.md#f-022f-023-branded-linux-installer-rebuild).

**F-021 — high AppImage launcher disables Chromium sandbox / PKG-001, INV-001, DCP-020.** Actual unsigned AppImage from composed source `725efb5c` contains a desktop entry with `Exec=AppRun --no-sandbox %U`. Its generated `AppRun` also appends `--no-sandbox` when an `unshare -Ur` probe fails. The installed electron-builder 26.15.3 code generates both defaults. The desktop launch path therefore passes the disable flag unconditionally; this violates the retained remote-renderer sandbox invariant. No Electron/native launch or real compromised renderer is claimed. Baseline `84ac22aa` records three passing Linux builder controls/two failing sandbox targets. Scoped fix `45f45f4f` explicitly sets empty AppImage executable arguments and pins a minimal electron-builder launcher patch; the two targets pass. A generated-launcher shell fixture forwards a benign argument to a synthetic executable after a failed namespace probe without adding the flag; the old actual AppRun adds it in the same fixture. The environment-correct `7771a714` unsigned internal AppImage passes the then-current 22-file ASAR check, sandbox launcher, environment identity and fuse checks, but misses the later F-022 runtime-image gate; all380 tooling cases pass after restoring public generated test inputs. The earlier ad hoc artifact mixed internal branding with production environment and does not qualify identity separation. Hosted packaged native startup and final composition remain open. Deb/rpm desktop entries do not include the flag; their startup remains unqualified. [Static artifact evidence](shipped-linux-asar-audit.md#actual-unsigned-linux-installers-from-the-composed-candidate).

## Current catalogued high-finding checkpoint

This is a **source-presence audit at local candidate `46465040`**, not an acceptance result or a claim that the 528-path review found every issue. `git merge-base --is-ancestor <fix> 46465040` succeeds for the fix commits listed below; scoped package pruning was recomposed as `725efb5c` and verified in the final Linux ASAR. The historical finding table below records original discoveries and may still say “open”; use this checkpoint for current source presence and its explicit limits.

| Finding | Current local source evidence | Still mandatory before unsigned handoff |
| --- | --- | --- |
| F-004/F-022 package inputs and runtime images | `78d2d5dd`, `725efb5c`, `49ccd76b` and final 30-file Linux trio; old actual ASAR fails image gate | Windows/macOS actual archive review and native tray/About behavior; final composed head |
| F-005/F-015 confidential build/deploy diagnostics | `e2920196`, `5e4c23b6`; synthetic token/error regressions recorded | Re-review all active diagnostic paths, hosted packaging/deploy failure behavior, final source |
| F-006 signing/fuse/failure order | `b725d784` with inert failure/order sensitivity; F-021 `45f45f4f` removes AppImage sandbox bypass | Unsigned Windows/macOS artifact and native checks; real signing/notarization remains downstream M5 |
| F-008 bounded account queue | `28fbf798` caps pending operations at 32 after failing baseline `8cd6bd18` | Final native/controller/IPC stress and platform behavior |
| F-009 SSO diagnostic confidentiality | `b58de5a1` follows synthetic private-error baseline | Native failed-load/redirect/cookie handling and broad diagnostic review |
| F-010 settings preservation | `ccf4c144` follows separate migration/write-failure baselines | Native Windows/macOS/Linux persistence/recovery and profile contract |
| F-011 bounded picture saves | `ac55c392` bounds pending saves across accounts | Final native cancellation/dialog and platform qualification |
| F-012 ciphertext account ownership | **No accepted implementation**; profile compatibility decision pending | Decide existing-profile versus fresh-only contract, then sensitivity-test ownership, migration and native key-store behavior |
| F-013 bounded log admission | `a8e0854f` follows real-writer controlled-load baseline | Final native writer/resource and teardown qualification |
| F-021 AppImage sandbox launch | `45f45f4f` plus final static AppImage with `Exec=AppRun %U` and no launcher bypass | Hosted effective sandbox and mounted/extracted startup on supported Linux |

F-017 log-export data-loss correction `39bdfe5c` is also in this candidate; its 17 Node filesystem/ZIP cases pass on the current scoped source. This table does not cover unreviewed paths, lower-severity findings, final PR composition or platform gates. [Exact 528-path accounting](inventory-reconciliation.md#latest-appimageruntime-imagedesktop-and-advisory-recomposition) and the [mandatory handoff gates](plan.md#current-goal-close-mandatory-unsigned-review-handoff) remain controlling.

September22 **F-015 — deployment diagnostics disclose credentials**, high, PKG-001/ELC-003, INV-010. Repository baseline `bin/deploy-tools/dry-run-confidentiality.test.ts` calls the real GitHub dry-run methods with synthetic credentials and inert HTTP/logger substitutes: one characterization passes (draft id0, zero requests, operation names retained), one security target fails on the synthetic token. Shared `logDry` dumps arbitrary arguments, including Authorization and asset buffers. Raw Axios response/error logging in GitHub/Hockey is a separate source-confirmed path awaiting repository regressions and remediation. No real credential exposure incident is asserted. Baseline command: `node node_modules/mocha/bin/mocha.js --require .babel-register.js bin/deploy-tools/dry-run-confidentiality.test.ts`; output `/tmp/pkg001-dry-run-baseline.log`.

This is an open internal review, not acceptance or independent REL-001 review. Follow [the execution plan](quality-review.md). No completion percentage or full-coverage claim is made.

**F-020 — medium active shell rejection diagnostics / CAP-001, INV-010, DCP-001/DCP-002/DCP-004.** Four security targets with synthetic private Errors fail against active index/sidebar/account-slot logging while11 routing controls pass (baseline74590da5). Scopedeb82163b reports fixed operation labels; composition5f2852cd passes86 renderer cases, root types, targeted lint and production build. Wrong generic layout label fails its target. No real credential incident or account-view disk forwarding is claimed. [Scoped evidence](renderer-rejection-confidentiality-review.md). Test-onlyd5a47d03 additionally protects the failed-removal retry: raw logging mutation fails1, restored5 Webview cases pass at2ba7ade3. Final native/platform/E2E/unsigned-package and full diagnostic review remain open.

The active account preload is another CAP-001/INV-010 diagnostic path under F-020. Baseline `f655d236` forwards the exact event and fails on synthetic private rejected-Error logging; scoped `3bfa7434` emits a fixed message, and a raw-Error restoration fails its target before recovery. Combined `725efb5c` passes two focused and182 package/preload cases, relevant types/lint and production build. This is inert module evidence, not native console persistence or full main/preload diagnostic review. [Evidence](renderer-rejection-confidentiality-review.md#active-account-preload-rejection-follow-up).

## Verified scope and checkpoint

September22 inventory correction under existing F-001/TST-006: [inventory reconciliation](inventory-reconciliation.md) verifies484 unique rows against baseline1b82b085 → local composed09214ca9, with no missing/extra paths or status mismatch. The prior ledger omitted20 paths and duplicated one CLI path. Matching-blob candidate annotations and explicit historical provenance restore accounting; source/coverage completion remains unproven. This supersedes older file-count statements below for that exact validation target only.

The original modernization baseline is `1b82b085ac1436a7f21d81cb944d2ee2f4ba4a4a`. Its ancestor upstream `e1ba98c50dce28b26b05466169fbdf941f0285f3` has identical application/tooling inputs (`git diff e1ba98c5 1b82b085 -- electron bin package.json yarn.lock` is empty); the intervening three commits are documentation. The full baseline-to-functional-checkpoint `6e27c614` diff contains 423 paths, including subsequent upstream/MSI integration. Origin does not exempt retained code from composed review.

[PR61](https://github.com/adamlow-wire/wire-desktop/pull/61) integrates the review plan as `897e3930392fdc9479f9641c8c03f116947d4e95`. Reviewed `7a3e875a`, tested synthetic `ede3cb84` and actual merge share tree `f9f7a380c97bd8d0a97bf9988a1e327ceaa8ba5f`. All ten strict checks, admin/conversation enforcement, no unresolved threads and SHA guard passed. [Build](https://github.com/adamlow-wire/wire-desktop/actions/runs/35010907386), [lint](https://github.com/adamlow-wire/wire-desktop/actions/runs/35010907499), [CodeQL](https://github.com/adamlow-wire/wire-desktop/actions/runs/35010907384) and [full E2E/report](https://github.com/adamlow-wire/wire-desktop/actions/runs/35010907346) pass. All 76 E2E cases/platform passed initially; report job `104535071887` passes. Exact CodeQL analysis `1781510397` reports zero findings/no error.

[Native/package run](https://github.com/adamlow-wire/wire-desktop/actions/runs/35010907311) passes after one unchanged-head failed-job rerun: Linux `104525933836`, Windows `104525934283`; macOS passed initially in `104522324872`. Initial Linux capture Stop timed out after 20 seconds; initial Windows controller setup timed out after ten seconds at “starting account views.” These are retained under F-002, not diagnosed by their successful reruns. Protected MSI worktree remains `255bdd54`; no shared-display tests or installed application were used.

[Module inventory](review-inventory.csv) accounts for the complete original delta plus current queue work. An initial source pass does not close test provenance, counter disposition or final composed review. [Behavioral traceability](review-traceability.csv) starts with every DCP, INV and incoming IPC channel, including dormant channels and separate SSO/proxy operations. Pending cells are deliberately unaccepted; command variants and main-to-preload contracts still need detailed mapping. These ledgers must be updated as the candidate changes.

## Findings and remediation

September22 **F-019 — proxy endpoint/transport loss / CAP-005, DCP-011**: actual applyProxySettings emits socks=undefined for SOCKS4/5 and loses HTTPS proxy transport by omitting its scheme. Baseline d49d3bf3 has3 controls pass/6 failures; scoped1381a854 uses the host and explicit HTTPS/SOCKS schemes, with9 cases passing. Scheme removal and full-URL copying fail6/8 targets. Scoped lint and composed-overlay tests/types/TypeScript compilation pass. [Evidence and primary protocol reference](cap005-proxy-protocol-review.md). This is reproduced configuration generation, not observed network/credential exposure. CAP-005 is reopened; actual native resolution/transport and final platform qualification remain open.

September22 **F-018 — medium deep-link dispatch diagnostic confidentiality / SEC-013, INV-010, DCP-010**: actual CoreProtocol forwards the original failed-dispatch Error and nested data to its logger. Synthetic Node baselinebdbe1595 preserves5 controls and exposes4 diagnostic failures; scoped candidatea2d77b19 emits a fixed operation message and passes9 cases. Omitted delivery fails its control; omitted diagnostic fails4. Composed-overlay tests/types and scoped lint pass. This establishes a boundary defect, not a real credential incident or an exploit through every dispatcher. [Scoped evidence](sec013-dispatch-diagnostics.md). Existing SEC-013 is reopened; native routing/final candidate qualification remain open.

September22 remediation: F-014 is locally corrected by `96219916`: renderer Jest74/12 suites and aggregate root/bin/Playwright types pass; the complete Mocha suite still executes, retaining303 passes/eight known signing failures before F-006 correction. F-006 now has explicit post-fuse signing and propagated promise failures, preserving metadata recovery and fixed diagnostics. Baselines `01fe145a`/`2e2e3a69` record4 passes/eight failures; `faa4bb5a` adds incomplete-config deny targets. Restored tooling passes314 cases; omitted-fuse and swallowed-sign-error perturbations fail and are reverted. A later relative-output entitlement regression fails before path normalization, then all16 signing cases, dedicated bin types and scoped lint pass. See current [status](status.md) and [signing procedure](packaging-verification.md). Neither finding is integrated or signed-platform-qualified yet.

September22 return validation adds **F-014 — aggregate test-runner/type integration failure**, owned by PKG-001/TST-005/TST-006. At `01fe145a`, `node .yarn/releases/yarn-3.3.1.cjs test:react --runInBand` has28 passing/six failing suites and129 passing tests: Jest discovers Mocha build-tool tests and fails on `before`, Mocha `this.timeout`, or dependency module loading. `node node_modules/typescript/bin/tsc --noEmit` also fails TS2683 in three build-tool suites (`build-cli`, `build-wrapper`, `macos-signing`), although dedicated `build:ts:bin` passes. This blocks aggregate qualification; fix test ownership/configuration while preserving explicit Mocha collection and typing, then verify both runners and the root compiler. Local evidence: `/tmp/electron-return-react.log`, `/tmp/electron-return-app-types.log`, `/tmp/electron-return-bin-types.log`. No runtime regression or historical integration failure is inferred from this branch-local result.

| ID | Severity / owner | Evidence and consequence | State / required follow-up |
| --- | --- | --- | --- |
| F-017 | Medium local destination data loss / CAP-004, DCP-005 | Failed log archive creation/write truncates an existing selected destination. Baseline5367256f preserves ten controls and exposes two failures. Candidate39bdfe5c stages privately and publishes complete ZIPs by rename with0600 output. | Local17-case candidate and recomposed68-case Node logging run pass; native/platform filesystem and chooser qualification remain open. |
| F-001 | Handoff evidence gap / TST-001, TST-006 | Fresh hosted counters omit actual application E2E execution; `mainProcess.ts` has 0/381 statements despite real product tests. Existing diff gate is not a full-baseline/per-module report. | Open: improve collection/reporting, audit exclusions, test reachable gaps and disposition every remainder. |
| F-002 | Medium test reliability / TST-005, CAP-003, CAP-001 | Repeated native capture cancellation/Stop timeouts and a separate account-startup fixture timeout, including PR61. Waiting for an inspector reply from a closing broker is a hypothesis, not established cause. | Open: bounded stage diagnostics and sensitive regression; preserve actual Stop/track/owner assertions, record all attempts. |
| F-003 | Medium resource lifetime / SEC-003 | `AuthorizedIpc` rate-limit map retains webContents IDs for binder lifetime without eviction. | Local repository remedy uses weak keys for actual webContents objects; baseline3bb1e04e and re-registration sensitivity pass as described below. Integration/native qualification pending; no privilege bypass established. |
| F-004 | High package input inclusion / PKG-001 | Real packager copy-filter traversal of synthetic Windows/macOS workspaces copies `.env`, `e2e-tests/.env`, `keys/example-private.pem` and test-result traces. macOS also has a source-exclusion typo. | Open: restrictive reviewed runtime file policy, sensitive copy fixtures and real artifact content checks. |
| F-005 | High credential-bearing diagnostics / PKG-001, INV-010 | Build CLI recursively logs macOS configuration, including `notarizeApplePassword` and `osxNotarize.appleIdPassword`. | Open: synthetic logger-capture baseline and safe diagnostics; no actual credential exposure is asserted. |
| F-006 | High packaging failure/order defects / PKG-001, SEC-011 | Windows/macOS wrappers swallow packager/fuse failures; several wrappers mutate files before restoration `try`; automatic macOS signing may precede fuse changes; manual signing uses `execAsync`, which swallows command failure. | Open: fault-injected restoration/error/order tests and fixes. Actual signed verification remains Wire/M5. |
| F-007 | Medium retained updater defect / PKG-002, DCP-019 | The internal macOS updater is inside a second `ready` listener registered after startup; normal startup never reaches it. Inherited upstream `19268e651`, still present in composition. | Open: sensitive startup/update-routing test and scoped fix. Do not activate updates in unsigned preview fixtures or claim signed-update qualification. |
| F-008 | High unbounded queued payload retention / CAP-001, INV-010 | A pending native environment approval holds the controller queue indefinitely. Safe Node-only reproduction accepts 600 tiny metadata events with none rejected; the IPC contract permits 600/minute and up to 2 MiB picture values. No large allocation/OOM test was used. | Local candidate fixes the queue at 32 active/queued operations. Baseline `8cd6bd18`, sensitivity and local tests below; final native/product/platform qualification still required. |
| F-009 | High sensitive SSO diagnostics / CAP-002, INV-010 | A Node-only synthetic window/event adapter confirms `fixture-secret-only` reaches the actual SSO console log-writer call unchanged. Electron 43.4.0 also includes the failed URL in load errors; the current SSO initialization path propagates that error. | Open: replace raw authentication-page/error diagnostics with safe bounded events and sensitive tests. The console path is confirmed; no real credential exposure incident is asserted. |
| F-010 | High settings loss / PKG-003, DCP-021, INV-010 | Migration moves legacy over current before parsing, then writes shared defaults after a parse failure. Malformed contents reach logs. Ordinary reads also substitute defaults for corruption; writes are not atomic and failures are swallowed. | Local remediation and separate baselines `b122d801` / `f5f8684c`; see [settings recovery](settings-recovery.md). Native/cross-platform qualification and final review pending. |
| F-011 | High pending picture-save retention / SEC-003, SEC-004 | Real save-picture binder with valid identity, one-byte payloads, controlled clock and unresolved native-boundary promises admits 50 simultaneous save operations across five quota windows. Production awaits a native save dialog while retaining up to 15 MiB per request. | Local repository remedy bounds native saves at one active operation across accounts; baseline3bb1e04e exposes the gap and release sensitivity is verified. Native/composed qualification remains pending. No large allocation or native dialog was used in reproduction. |
| F-012 | High account-ownership contract gap / SEC-003, DCP-016, INV-004 | The safe-storage binder authorizes each account but sends all ciphertext to the same OS decryption primitive without an owner envelope. DCP-016 already lists ciphertext ownership as unresolved. Upstream webapp stores opaque encrypted secret records, so changing the format has legacy-profile implications. | Open: qualify native boundaries and design account ownership plus migration. A product question is pending on existing-profile support versus a fresh-profile-only unsigned candidate. No silent format change, key-store bypass or data deletion is authorized. |
| F-013 | High log queue/resource bound gap / CAP-001, INV-010 | With logging enabled, account/SSO console events reach a writer that bounds rotation size but not message size or pending bytes/count. A real-writer, inert-filesystem reproduction admits 1,000 one-character writes while the first append is blocked; all eventually append. | Open: bound individual entries and aggregate pending work while preserving drain-before-delete and recovery. Avoid recursive overflow diagnostics: main log-write errors themselves use the log writer. No real files or large allocations were used in reproduction. |

High findings block handoff. A source observation labelled candidate is not a confirmed exploit. Lower-severity entries still require a reviewed disposition, not automatic acceptance.

## Coverage and baseline provenance audit

Fresh accepted-runtime [integration build](https://github.com/adamlow-wire/wire-desktop/actions/runs/34997388051) artifact `10407783188` contains Electron **4397/5340 statements, 2326/2793 branches, 997/1315 functions** across 176 files, and renderer **282/478 statements, 141/269 branches, 78/148 functions** across 32 files. These are counter totals, not product completion. All maintained main TypeScript files are present except `runtime/wallClockLoader.mts`; JavaScript/tooling/native behavior requires separate accounting. Renderer test/spec files are excluded appropriately. Local September 10 coverage is stale and is not accepted evidence.

There are uncovered counters in 74 main and 24 renderer modules. The shell still runs the React/Redux renderer: it cannot be excluded wholesale as legacy. Some retained actions/context-menu helpers appear dormant and need graph proof. Six composition-root ignore comments require review; real E2E execution outside collection does not justify claiming instrumented coverage. Source search found no `.skip`, `.only` or `fixme` test declarations; conditional execution, mocks and actual CI consumption remain under review.

Verified history examples, not a blanket baseline-first verdict:

- `b31f4b60` adds only preview metadata characterization before parser replacement `6d7760fc`; original-run/sensitivity evidence still needs durable mapping.
- `e3fbd6a2` adds native permission callback characterization before permission foundation `565035fe`. That foundation adds its policy and policy tests together; these tests cannot be called pre-refactor legacy evidence or observed originally failing targets without further evidence.
- Current queue baseline `8cd6bd18` is a later security regression, not an original modernization baseline. Before the fix, ordering and stale-authority tests pass and overload rejection fails. An initial test expectation used the wrong authorization error wording; correcting it to the existing registry error produced the recorded two-pass/one-fail result without changing production.

## Dependency audit

The Yarn recursive production audit is incomplete for this purpose: it reports a transitive development Axios version while omitting advisories specific to shipped Axios 0.21.2. The actual [preview artifact](https://github.com/adamlow-wire/wire-desktop/actions/runs/34993542942/artifacts/10407101912) ASAR contains 433 installed package instances/370 distinct names. Querying the official npm bulk advisory endpoint with these exact names/versions returns 16 affected names/76 version-range advisories. These are advisory matches, not 76 demonstrated application vulnerabilities.

Only real `node_modules` package roots count. Nested dependency fixture manifests were removed before the final query; a test fixture whose name collides with a malware advisory is not evidence that the malicious package is installed. Production graph, tooling graph, actual reachability and compatibility remediation remain ELC-003 work.

Most affected protobuf/documentation tooling comes through `@wireapp/protocol-messaging`, currently imported by desktop only for `Availability.Type.BUSY`. Verify enum compatibility before removing that unnecessary runtime dependency. Other candidates are compatible certificate-parser/updater/YAML/UUID updates and maintained Joi, with existing schema, certificate and deployment contracts protected first. Electron stays 43.4.0; no blanket latest-major update is authorized by this audit.

The preview archive was inventoried, never launched locally. Its environment filenames are only `.env.defaults` and `e2e-tests/.env.tpl`; no `keys` or test-results directory was found. This targeted check identifies no actual private environment/key inclusion, but is not proof that every archive byte is secret-free and does not resolve F-004.

## Current CAP-001 reproduction and validation

Run without Electron or native windows:

```sh
node .yarn/releases/yarn-3.3.1.cjs mocha --require .babel-register.js electron/src/accounts/AccountControllerQueue.test.main.ts
node .yarn/releases/yarn-3.3.1.cjs tsc -P tsconfig.mocha.json --noEmit
node .yarn/releases/yarn-3.3.1.cjs eslint electron/src/accounts/AccountController.ts electron/src/accounts/AccountControllerQueue.test.main.ts
```

The fixture uses real controller/state/registry objects, an in-memory profile and inert view adapters. The three local tests pass, and the combined queue/state/profile run passes 40 cases; Mocha types and scoped lint pass. These tests preserve FIFO/copied payloads, reject 31 stale queued events without state changes, restore all 32 slots for a replacement identity, reject overload while approval is pending and recover after cancellation. They do not replace native IPC or actual dialog/platform tests.

Temporary removal of payload copying fails the exact ordered-name assertion; removal of queued authority checking fails expected rejection. Removing the new limit fails immediate overload rejection; removing slot release fails both replacement-identity and post-cancellation recovery. All perturbations are restored. Local scratch logs use `/tmp/tst006-queue-*`; the separate baseline and PR CI must provide durable reproduction, not dependence on those temporary files.

Next: qualify and integrate F-008, investigate F-002, remediate packaging/dependency findings under existing owners, continue every review/traceability/coverage row, then perform final composed unsigned qualification. Signing, live Keycloak/E2EI, actual OS/hardware gaps and independent review remain the explicit later gates in the plan.

## PKG-003 settings baseline and remediation

The original migration fixture passes three legacy cases and fails five security targets. Persistence passes four cases and fails three targets. Both baselines were committed before their corresponding source changes. The initial migration fixture used noncanonical window keys; a correction to `SettingsType.FULL_SCREEN` (`fullscreen`) and `SettingsType.WINDOW_BOUNDS` (`bounds`) also passes against the original source. No intended deletion of those values is activated: the old deletion block was unreachable for JSON input because the shared schema supplied `configVersion` first.

Local Node-only filesystem tests now pass 26 cases; two environment-selection failure cases also pass. Removing legacy value copying breaks characterization. Omitting the persistence flush or replacing exclusive migration publication with rename breaks the relevant regression. All perturbations are restored. Native Quit/menu tests are authored but not run on the shared display; their hosted selection is explicit. Read/write failures now use generic diagnostics. Failed saves preserve the previous file, preserve environment selection and still allow Quit. Full released-version fixtures, installer rollback and signed-platform validation remain M5.

The extended Node-only run passes 37 cases (32 settings and five EnvironmentUtil). Focused Babel/Istanbul counters cover 36/36 statements and 19/19 branches in SchemaUpdater, and 50/50 statements and 18/18 branches in ConfigurationPersistence. This includes unavailable runtime path, already-loaded settings, invalid in-memory values and cleanup failure. These module counters do not qualify native callers, real OS failure behavior, the complete application denominator or every supported released-version migration.

## Verified original SSO baseline provenance

[PR5](https://github.com/adamlow-wire/wire-desktop/pull/5), reviewed head `f67d4835`, contains test commits `c27cfa6a` and `6e1fc7f0` before the subsequent SSO runtime changes. The retained [build log](https://github.com/adamlow-wire/wire-desktop/actions/runs/32413714251/job/96569886580) confirms all 18 SSO characterization cases passed, including the owned local Electron flow. The native suite reports 54 passes/three pending targets on both ordinary and coverage invocations. The three pending targets were secret consumption, response-type allowlisting and foreign cookie scope, explicitly allocated to CAP-002. Later implementation removes those quarantines.

PR5's authenticated E2E/report jobs were skipped; its Windows/macOS/Linux package jobs did not select the SSO tests. Thus the original SSO test evidence is the build job, not three-platform SSO qualification. Historical protocol/error-response sensitivity is recorded in TST-002, but its original mutation logs have not been independently recovered in this audit. Later/current tests are not relabelled as original baseline evidence.

## Native encryption and diagnostic source checks

Electron 43.4.0's [load-error implementation](https://github.com/electron/electron/blob/v43.4.0/lib/browser/api/web-contents.ts#L265) includes the URL in the rejected error. Combined with the local SSO propagation path, this supports F-009's load-error risk; a targeted native regression is still required.

The candidate plaintext-fallback concern is not established: Electron's synchronous [safe-storage implementation](https://github.com/electron/electron/blob/v43.4.0/shell/browser/api/electron_api_safe_storage.cc#L135) checks availability, and its [fallback flag defaults to false](https://github.com/electron/electron/blob/v43.4.0/shell/browser/api/electron_api_safe_storage.h#L78). Desktop does not call `setUsePlainTextEncryption`. Missing-key-store and actual packaged key-store behavior still need explicit native tests; no plaintext fallback has been enabled.

For F-012 compatibility analysis, upstream webapp revision `4396da0761bbe559043837b2dda718eecad98c63` wraps the [version-1 native bridge](https://github.com/wireapp/wire-webapp/blob/4396da0761bbe559043837b2dda718eecad98c63/apps/webapp/src/script/service/utils/systemCryptoWrapper.ts) and stores opaque ciphertext in [EncryptedStore](https://github.com/wireapp/wire-webapp/blob/4396da0761bbe559043837b2dda718eecad98c63/libraries/core/src/secretStore/encryptedStore.ts). It supports older plaintext encoding conventions. Rejecting all old ciphertext without understanding application recovery could disrupt existing profiles; the ownership/migration policy must be explicit. This source revision is a compatibility reference, not proof of the version deployed by a customer.

The final local settings review adds two exclusive-open collision regressions: neither migration nor persistence may delete a staging file it failed to create. Deliberately claiming ownership before successful open fails both tests; restoration passes 39 Node-only cases (34 settings/five environment) with the same focused 86/86 statement, 37/37 branch and 8/8 function counters. Hosted qualification remains required.

## Pending-save remediation preparation

A scratch SEC-003 candidate limits save-picture work to one active native operation across account views, independently of the existing per-view quota. Its test-only baseline retains the three current contract cases, adds synchronous/asynchronous failure recovery and reproduces the pending-save defect (four passes/one failure). The candidate passes all five cases, including denial across three advanced quota windows and capacity recovery after cancellation. Omitting release fails three tests; restoration passes. This uses Node-only adapters and one-byte synthetic data, not real dialogs.

The candidate is prepared separately under `/tmp/tst006-sec003-draft` with baseline/implementation patches. It has not changed repository runtime, received native qualification or been integrated. After the queue/settings PRs qualify, apply the test-only baseline under SEC-003 first, re-run it against the actual integration, then apply and re-review the fix with native dialog/cancellation checks. Temporary proof logs are not a substitute for durable PR/CI evidence.

## SSO confidentiality and IPC lifetime remediation preparation

The scratch CAP-002 diagnostic candidate at `/tmp/tst006-sso-draft` removes authentication-page console forwarding and replaces raw load/cleanup/finalization/cookie exceptions with fixed bounded messages. The initial seven-case Node adapter has one pass/six failures against current source. Strengthening the diagnostic assertion to require a useful message and prohibit forwarding original Error objects also catches parser-input properties; that final baseline has seven failures, and the draft passes all seven. Omitting cleanup diagnostics fails two tests; restoration passes. A real Electron failed-load regression, retained native SSO suite and shared external-opening error-path review are still required. No repository SSO runtime or product contract has changed yet.

The SEC-003 draft now also replaces the lifetime-retained numeric rate-limit map with a WeakMap keyed by the actual webContents object. It keeps the same view's quota across identity revocation/re-registration without retaining dead views solely for limiter state. Seven IPC baseline cases pass before this change, including new synchronous policy/quota and same-view re-registration assertions. Deliberately keying by registration identity breaks the latter; restoration passes all 12 combined IPC/save-picture draft cases. This verifies retained quota semantics; it is not a nondeterministic garbage-collection timing test or native IPC qualification. Baseline and implementation patches remain separate in `/tmp/tst006-ipc-lifetime-*` for application under SEC-003 after existing work integrates.

## PR62 current full-E2E failure investigation

September22 follow-up: the latest retained Linux run35035967615 at a1cb2b84 independently reproduces the same hosted-webapp exception during Connect. [Sanitized trace evidence](candidate-reconciliation.md#latest-retained-pr62-e2e-diagnosis--september-22) distinguishes actual Electron-app runtime events from historical report metadata and the other browser context. No E2E gate is closed.

[Linux job104540124260](https://github.com/adamlow-wire/wire-desktop/actions/runs/35016198815/job/104540124260) and [macOS job104540124914](https://github.com/adamlow-wire/wire-desktop/actions/runs/35016198815/job/104540124914) each finish with 58 passes/18 failures. Their logs each contain 42 failed waits for `#start-ui`'s “Search people” input. The first retained Linux trace instead shows the “Something went wrong” error dialog immediately after the people-button click, and `TypeError: n.findDOMNode is not a function` from the hosted webapp's `https://wire-webapp-dev.zinfra.io/min/vendor.js`. Do not replace this evidence with an assumed locator change or bypass the failing operation.

A single controlled [unchanged PR61 Linux comparison](https://github.com/adamlow-wire/wire-desktop/actions/runs/35010907346/job/104570922016) is running against the current hosted endpoint. It is a counterfactual to distinguish the hosted webapp from PR62, not a blind retry of PR62 or new acceptance evidence. Original PR61 attempt results remain recorded above. Current PR62 Windows/report results, other retained traces and the comparison result are pending. No gate is waived and no root cause beyond the observed remote exception is asserted.

Raw diagnostic artifacts stay outside Git and must be handled as sensitive: browser traces include synthetic test-account tokens and network data. Inspect only sanitized extracts and prepare sanitized handoff evidence. Linux artifact `10419076217` has now been fully downloaded (4,573,675,940 bytes); SHA-256 `f22346ed24a8ddfce3b9fe66f239e60930a29b3f85042a8179041f6c66679a70` matches GitHub metadata. Outer extraction verifies the report member CRC. Its 163-member inner report is available for sanitized analysis; no browser was launched. Earlier first-trace inspection used a partial stream and was not treated as complete-artifact validation.

## Packaging input and diagnostic preparation

A scratch runtime-input policy at `/tmp/tst006-runtime-package-policy.cjs` passes actual electron-packager copy-filter traversal for 22 required runtime files and 20 denied inputs. It retains compiled main/preload modules, locale data, owned HTML/CSS/images, renderer bundle and third-party runtime assets; it denies root environment files, private-key examples, traces, source/tests and owned source maps. Allowing every input or omitting required assets makes the fixture fail; restoration passes. Linux-builder pattern parity, configured directory handling, platform configuration integration and actual packaged startup/content verification remain pending under PKG-001. Third-party asset inclusion still requires graph-aware ELC-003 review; this fixture does not prove arbitrary dependency contents are secret-free.

A direct logger-transport capture independently confirms F-005: synthetic direct `notarizeApplePassword` and nested `osxNotarize.appleIdPassword` reach the current build diagnostics. No packaging was started and no actual credential was supplied. Replace recursive configuration dumps with useful fixed-stage diagnostics and test the CLI boundary before qualification.

Sanitized analysis of the completed Linux report confirms 112 test attempts: 58 passing results and 54 failed/timed-out results across the 18 exhausted cases, with no independent worker errors. All 18 initial-failure application traces were inspected for the specific exception; 16 contain `findDOMNode is not a function`. The other two initial failures still require separate trace diagnosis; do not attribute every failure to the same cause from the aggregate count.

The two remaining initial Linux failures are notifications tests whose second participant is created by `createPage` using the ordinary Playwright browser (`e2e-tests/fixtures.ts`). Their separate runner/browser traces each contain the same `findDOMNode` exception and error-dialog snapshot. The earlier application-only scan omitted that participant. All 18 initial failed cases therefore show the exception in the relevant browser trace; two demonstrate it outside Electron. This strengthens the hosted-webapp attribution without claiming a source-level fix or full regression qualification. Windows is now cancelled, not passed; report merging remains pending.

Settings native/package preflight [run35026361662](https://github.com/adamlow-wire/wire-desktop/actions/runs/35026361662) runs published head `40ff0485e32fa3327530865d877322fe67fbbbfd`. Only the existing native workflow was dispatched; no E2E manual dispatch or integration merge occurred. Final composed PR checks remain mandatory.

## Current SEC-003 repository candidate

Baseline `3bb1e04e` is committed before implementation and records 11 passes/one pending-save failure. The repository fix passes 15 IPC/save/safe-storage cases. A wrong registration-identity key fails one test; omitting pending-save release fails three; restoration passes. Mocha types and scoped lint pass. `SavePictureNative.test.main.ts` and its fixed-operation preload fixture are authored for hosted execution: two sandboxed renderers invoke real IPC, share the pending-save bound, reject malformed/revoked callers and recover from controlled native cancellation into a real temporary-file write. The Electron dialog response is stubbed; this does not qualify OS dialog interaction. The test is explicitly selected on all three native platforms and has not been run locally.

Settings preflight `35026361662` passes native/package Linux `104574229533` and macOS `104574229199`. Windows `104574229469` fails before its selected settings/product stage: 154 account-lifecycle cases pass and six tests/hooks fail, starting with controller teardown after the SSO account-limit case. Later failures include timed-out filesystem/view operations and an ENOTEMPTY log-cleanup teardown. Its earlier deliberate registry-guard sensitivity failure is expected and is followed by a passing restored test; that is not the job failure. No native failure artifact was produced. Retain this under F-002; it does not establish a settings defect or qualify Windows settings.

SEC-003 boundary cases have their own native-workflow step before the broader account-lifecycle suite. This prevents that suite's observed Windows teardown failure from hiding the new IPC result; both steps remain required for a passing job. No failed check is skipped or treated as accepted.

SEC-003 implementation `ac55c392` and native-step ordering `a26321e8` are published for native/package preflight [run35027694438](https://github.com/adamlow-wire/wire-desktop/actions/runs/35027694438). All 15 related Node-only contract cases, sensitivity restoration, type checks, scoped lint and formatting passed before publication. The native test result and all final composed PR gates remain pending.

Native run `35027694438` executes and passes all 16 selected IPC cases on Windows/macOS/Linux, including the new real-IPC cancellation case. Linux job `104578560409` completes successfully. Windows `104578560169` has 162 account-lifecycle passes/two timeout failures afterward: controller teardown after the version-routing case and the explicit-consent account-view case. These differ from the earlier failing test labels; they are not automatically the same underlying bug. macOS is still building its package. Final composed acceptance remains open.

The local TST-005 diagnostic follow-up tracks controller teardown stages (view destruction, shell destruction, each session clear and fixture-server close), reporting fixed labels and durations at nine seconds while preserving the existing ten-second hook budget, original operations and propagated failures. It passes types/lint but is not yet run natively. A Node-only owned-loopback reproduction also proves that `server.close()` waits for an unused accepted TCP connection until `closeAllConnections()` releases it. That is a candidate fixture mechanism, not proof that the Windows timeout reached server closure. Obtain stage evidence before claiming causation or changing a deadline. No GUI was opened.

## CAP-002 diagnostic confidentiality remediation

Baseline `1a1093f5` ports the scratch cases to typed repository tests and adds the shared external-opening boundary. All nine Node-only targets fail against unchanged runtime (including both synchronous and asynchronous native opener errors). The local correction passes nine; removing cleanup diagnostics fails two cases, and forwarding original external-opening errors fails two. All source perturbations are restored. The new native failed-load test uses an owned loopback server that closes the requested connection and an actual isolated Electron window; it is authored, type-checked, and **unrun**, not counted as a passing baseline. Existing native SSO tests remain required.

Reproduction: `node .yarn/releases/yarn-3.3.1.cjs mocha --require ./.babel-register.js --require ./electron/test/node-sso-context.cjs electron/src/sso/SingleSignOnDiagnostics.test.main.ts`. The adapter explicitly refuses Electron execution and only supplies owned Node test objects; hosted tests use the actual Electron module. Local evidence: `/tmp/tst006-sso-repository-{baseline-final,fixed,restored}.log`, `/tmp/tst006-sso-{cleanup,external}-sensitivity.log`. This changes diagnostics, not authentication/navigation/cookie authority. Live Keycloak/E2EI remains separate external QA.

SEC-003 preflight `35027694438` finishes Linux/macOS successfully; Windows's 16 IPC cases pass before 162 account-lifecycle passes/two timeouts. Diagnostic head `38722dff` is now running as `35029234576`. PR62 report `104570687419` succeeds after Linux/macOS failures and Windows cancellation; it does not qualify those results. The controlled original-PR61 Linux comparison remains running as `104570922016`.

Windows diagnostic job [104583548074](https://github.com/adamlow-wire/wire-desktop/actions/runs/35029234576/job/104583548074) records 156 lifecycle passes/three failures: controller startup, controller teardown and a permission-consent timeout. Teardown reaches `clearing session 1`; view destruction took 5ms and shell destruction 3ms, but session clearing is still pending at 9,002ms. Startup stopped at `starting account views`. The earlier owned Node socket experiment proves a possible HTTP fixture mechanism only; it does **not** explain this observed session-clear stall. No socket workaround, timeout change or production change is justified by this evidence alone. Raw log remains private at `/tmp/tst006-sec003-windows-diagnostic.log`.

## CAP-001 bounded logging remediation

The existing queue-remediation branch now also owns F-013. Separate baseline `aaa7e250` records four passing filesystem failure/recovery controls and seven failing targets: count/byte admission, UTF-8 entry size, mutable queued input, excessive path retention, oversized BOM-prefixed content and unbounded rotation collisions. The writer fix retains ordering, cross-file progress and existing flush/maintenance behavior. It limits active/pending entries to 256, encoded entry payloads to 1MiB and each entry to 64KiB; these are payload bounds, not a claim about total process RSS. Path strings have a 32,768-character retention bound. Overflow settles without rejection and only a bounded dropped count is retained for the next admitted message. Rotation stops after 128 destination probes, preserving the current file when no destination is available.

All 32 Node-only admission/writer/maintenance cases pass. Count, bytes, immutable input, reservation release, collision termination and entry truncation perturbations each fail the intended target(s), then restore to a passing suite. Evidence: `/tmp/tst006-log-admission-repository-{baseline,fixed,final}.log` and `/tmp/tst006-log-admission-{count,bytes,snapshot,release,rotation,entry}-sensitivity.log`. Reproduce with `node .yarn/releases/yarn-3.3.1.cjs mocha --require ./.babel-register.js electron/src/logging/boundedLogAdmission.test.main.ts electron/src/logging/boundedLogWriter.test.main.ts electron/src/logging/logMaintenance.test.main.ts`.

Hosted application target `cbf24648` emits synthetic oversized console messages through each real account renderer, requires bounded valid UTF-8 and retained access-token redaction in each account's actual log file, then checks removal deletes only the owning account's log. It is authored but unrun. The listener bounds input before regex work, uses a synchronous callback so the original large input is not retained while writing, and reports fixed failures to stderr instead of re-entering the file logger. Native writer tests now use the platform line ending in their exact content/byte assertions. This is still an open finding pending native and final composed qualification; the maintenance entry points and full coverage ledger remain part of TST-006.

The separate SSO candidate `b58de5a1` completes [native/package qualification](https://github.com/adamlow-wire/wire-desktop/actions/runs/35029959416) on all platforms: Linux `104585894919`, Windows `104585895132`, macOS `104585895144`. The dedicated SSO/WindowUtil stage includes the real failed-load regression. Its settings/IPC dependencies also execute on Windows; the earlier failed or unrun attempts remain retained. No diagnostic root-cause resolution, protected PR qualification or merge is implied by this preflight.

Additional EEXIST/ENOTEMPTY rotation-race targets both fail against original `28fbf798` source with a bounded synthetic stop, then pass with the collision cap restored (`/tmp/tst006-log-rotation-races-original.log`). This is a retrospective original-source check of later tests, not a claim that those two tests predated earlier development of the draft. The final local writer/admission/maintenance suite grows to 32 after the focused coverage follow-up below.

Focused fresh Node coverage of `boundedLogWriter.ts` after the follow-up cases is 76/76 statements, 34/34 branches and 12/12 functions, with 32 passing admission/writer/maintenance cases (`/tmp/tst006-log-admission-coverage-final.log`, report `/tmp/tst006-log-admission-coverage-final`). Added tests cover a partially exhausted byte allowance, ordinary/multibyte calls through the public console-text bound, and non-collision rotation failures/recovery. Removing only the remaining-byte guard fails its new target (`/tmp/tst006-log-admission-remainder-sensitivity.log`); restoration also passes all 32 cases with a Node-only CRLF adapter (`/tmp/tst006-log-admission-crlf-final.log`). These numbers describe one instrumented policy module, not the native console listener, complete app coverage, Windows OS behavior or every numeric input. Full native failure paths and integrated qualification remain open.

## PR62 logging qualification follow-up

Published `a8e0854f` fails [Build and Test](https://github.com/adamlow-wire/wire-desktop/actions/runs/35031593080/job/104591107851) before test execution in `build:ts:tests`: two TS2339 errors follow an empty-array assertion that narrows the subsequent recovery array to `never[]`. The local follow-up asserts the same zero append count, retaining all recovery/content checks. `tsc --noEmit -p tsconfig.mocha.json` and 32 Node cases pass (`/tmp/tst006-log-admission-test-types-final.log`, `/tmp/tst006-log-admission-type-fix-tests.log`). Earlier `tsconfig.json` and `tsconfig.playwright.json` checks passed but did not include the Electron test source; record this validation-scope omission instead of describing those checks as complete test typing.

Native Linux [104591111196](https://github.com/adamlow-wire/wire-desktop/actions/runs/35031593087/job/104591111196) passes 28 logging admission/writer cases and 197 account lifecycle cases, then reaches 526 selected passes/one 20-second capture Stop timeout. The logging selector had used `log maintenance coordinator` while the actual suite is `desktop log maintenance coordination`, omitting four cases; the local workflow now selects the exact suite. This is an actual execution gap, not 32 native passes. macOS `104591110927` and Windows `104591111142` finish native/package checks successfully, including the new real account-log assertions. The capture follow-up only reports fixed progress stages after 18 seconds within the existing 20-second budget, preserving source, pixel, live/ended-track and broker-destruction assertions. No capture cause or resolution is yet established. Raw logs remain private in `/tmp/tst006-cap001-log-{build,linux-native}.log`.

The already published composed SSO/settings/IPC checkpoint `b58de5a1` also passes the dedicated Electron-test compiler in a detached checkout: `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.mocha.json`, log `/tmp/tst006-composed-electron-test-types.log`. Checkout `/tmp/tst006-composed-typecheck` uses the unchanged repository dependency installation by symlink, with no native execution or output emission. This closes the same local test-typing check for that candidate; its final protected PR qualification remains separate.

## PKG-001 CLI confidentiality and failure baseline

Separate `7eb299f3` records eight passing compatibility controls and ten failing security/exit-status targets against the real build CLI, real Commander parser and original configuration-log traversal, with inert native builder substitutes. The fixtures use synthetic direct, nested and unknown future credential fields. The local correction removes all nine config-dump calls and the now-unused `logEntries` helper (repository search finds no remaining production caller), emits fixed platform-stage and failure messages, and gives an unsupported platform a nonzero exit status. All 18 cases pass; raw-error restoration fails two cases, missing stage diagnostics fails one, and restoring successful help exit fails one. Existing valid aliases, configuration forwarding and missing-required-argument rejection remain controlled. Evidence: `/tmp/tst006-build-cli-repository-{baseline,fixed,final}.log`, `/tmp/tst006-build-cli-{error,status,stage}-sensitivity.log`. Run `node .yarn/releases/yarn-3.3.1.cjs mocha --require ./.babel-register.js bin/build-tools/build-cli.test.ts`; tests spawn only inert Node children. The first sandbox attempt was denied child-process creation, and an early test-loader path issue occurred before execution; neither is counted as the security baseline.

F-006 also includes a confirmed metadata-backup collision: `backupFiles` names backups only by basename. Two owned input paths ending in `config.json` receive the same backup destination; restoring after synthetic mutation loses one input's original content. `/tmp/tst006-build-backup-collision.cjs` runs only owned filesystem operations and reports `distinctBackupPaths:false`, `firstRestored:false`, `secondRestored:true`, with zero project files changed (`/tmp/tst006-build-backup-collision.log`). This remains uncorrected pending separate regression tests and the broader failure/restoration work.

CAP-001 `da385ecf` passes all 32 logging cases on macOS before three two-second fake-media timeouts; Linux/Windows native/package jobs complete successfully. Core `35032607286` reaches the separate hidden-owner Stop test's 20-second timeout. No existing diagnostics establish either cause. Logs: `/tmp/tst006-cap001-followup-{build,macos}.log`. The unchanged PR61 Linux comparison `104570922016` finishes 58 passed/18 failed, with 42 Search people failure mentions; its final report succeeds. This supports current-environment failure on older code but is not a vendor-exception trace confirmation. No test gate is waived, and no further blind rerun is started.

The complete Node-only build-tool suite passes 69 cases after the CLI correction and removal of the unused dump helper (`node .yarn/releases/yarn-3.3.1.cjs test:bin`, `/tmp/tst006-build-tools-cli-final.log`). Its existing missing-Squirrel-input fixture emits its expected ENOENT diagnostic; no native build or signing is performed. Dedicated build-tool types and explicitly unignored CLI lint pass.

## PKG-001 metadata and wrapper recovery verification

Backup baseline `5efc93d8` records two passing controls/three failures; `c70432e5` fixes distinct basename recovery, mutable caller paths and pending-copy cleanup. Five filesystem cases pass and removal of indexed naming fails the collision target. Partial-write baseline `03e1666e` records seven failures; `12ecd796` moves Windows/macOS/Linux/Squirrel metadata work inside restoration and propagates Windows/macOS failures with generic diagnostics. Seven cases and the full81-case Node suite pass; removing Windows restoration fails two cases.

New inert subprocess wrapper baseline `1df0ce00` passes21 compatibility/failure/order/recovery controls and fails MSI read-error backup cleanup. The local fix moves that read inside existing restoration; four MSI cases pass and reversing it causes the exact target to fail again. Fixtures replace only native tool/config/logger boundaries, retain real wrappers/filesystem/backups, and assert failure identity, no secret diagnostics, operation sequence, exact restored bytes and retained originals when restore fails. This does not qualify actual packaged files or automatic macOS signing order. Dedicated bin typing and scoped test lint pass. Logs `/tmp/tst006-wrapper-tools-{baseline,msi-fixed,types-final}.log` and `/tmp/tst006-msi-read-{sensitivity,restored}.log`.

Retrospective check: the same22 wrapper cases run against wrapper source from `c70432e5` (before partial-write/error-propagation fixes), compiled with the repository TypeScript/Node Babel presets, yield12 passes/10 failures. This establishes retrospective sensitivity for packager/fuse/installer errors, raw diagnostic leakage and read-error cleanup; these tests were added after those fixes and are not claimed as prior characterization. An initial outside-repository TypeScript loader mismatch failed all22 cases and is discarded as harness failure. The corrected log is `/tmp/tst006-wrapper-tools-retrospective-compiled.log`; original source copies/compiled scratch are under `/tmp/tst006-wrapper-original`.

## PKG-001 package-input allowlist candidate

Baseline `9de6fb4a` exercises actual electron-packager copy filters and electron-builder main-file matchers on owned fixtures:16 passes/98 failures across Windows/macOS/Linux at default and nested application paths. All six runtime-asset controls pass; most environment/key/trace/source/map/test-file exclusion targets fail. Local `packageInputs.ts` restricts owned application content to compiled runtime code, locale resources, fixed documents/styles/bundle/licenses and images, retaining production dependency assets and packager default exclusions. Literal relative application directories support spaces and nesting; glob/escape/absolute paths fail closed.

Corrected fixtures generate config from their actual project directory and provide the existing macOS plist. Restoring old production filters with those fixtures reproduces16 passes/98 failures, then the restored allowlist passes114. Additional literal-directory/dependency cases and paths containing spaces bring current policy checks to179 passes. Dependency matching uses electron-builder's separate module matcher; no claim of actual dependency pruning, archive contents, package startup or signed integrity follows from these copy-filter checks. The complete225-case build-tool run passes at the earlier122-policy-case scope; expanded179 policy cases also pass, with dedicated bin typing and scoped lint. Removing directory validation fails all seven targets before restoration. Artifact qualification remains pending. Logs `/tmp/tst006-package-inputs-{baseline,sensitivity,restored,expanded-final,types-final,lint-final}.log`.

## Actual package verification and artifact completeness

At preflight `f7c1c359`, Linux job104609510387/run35037409000 passes; artifact10424091204 is downloaded with its GitHub digest verified. `unsquashfs` extraction without app execution yields an ASAR passing22 required-file checks and owned-content validation (12449 entries). Exact hashes and procedure are in [packaging-verification.md](packaging-verification.md).

New verifier tests use real ASAR fixtures, reject forbidden inputs/missing roles/dependencies/wrong entry points, detect changed bytes and refuse canary overwrite. Seventeen cases pass; removing the forbidden-content assertion fails ten targets, then restoration passes. A discovered ASAR cache issue is corrected by uncaching before inspection. The older retained preview fails the content validator. All299 Node build-tool cases pass; bin types/scoped lint pass. Removing cache invalidation independently fails the changed-archive test, then restoration passes. New workflow wiring remains unqualified: seeded canaries, manifest artifacts, smoke against actual extracted AppImage, complete Windows package resources and macOS permission-preserving tar. Previously uploading only Windows EXEs did not provide a runnable full package; that earlier artifact is not a complete handoff.

F-006 signing review: installed electron-packager `src/mac.js` `signAppIfSpecified()` catches `signApp` rejection, emits a raw warning and returns successfully; `create()` then proceeds to notarization/move. The wrapper's existing post-packager fuse write also occurs after that signing step. Merely propagating wrapper rejection cannot fix a swallowed library error. This is a confirmed source finding; an inert signing-failure/order baseline and remediation are next, with actual credentialed signing still Wire/M5. The manual path separately uses hard-coded obsolete helper paths and `execAsync` failure swallowing, as already recorded.

Archive verifier/workflow head `974dcfa7` is published and running in35038585587. All299 Node build-tool cases passed before publication. The previous `f7c1c359` native/package run35037409000 passes on all three platforms; it does not qualify the new archive checks or artifact upload changes.

F-015 local dry-run correction following baseline `d0a29c8e`: shared logger emits fixed operation diagnostics without option values. All16 deployment tests and bin types pass. Forced scoped lint passes. Reintroducing argument dumping fails the confidentiality target; restored two-case suite passes. This closes only the dry-run path locally; raw error/response paths, integration and aggregate final-head checks remain open. Evidence and next step are in [status](status.md).

F-015 failure-path baseline `0d30872d` calls both real deployer classes with inert HTTP/asset streams: five success cases pass and ten response/offline failure cases fail. Besides raw diagnostics, GitHub upload rollback incorrectly targets the releases collection rather than the failed draft. Candidate fixed messages omit Axios objects, response bodies/status text and nested credentials; rollback targets `/releases/{draftId}` and preserves rejection even if cleanup fails. All31 deployment cases pass; restoring raw error logging fails all ten negative targets. Dedicated bin types and forced scoped lint pass after preserving fixed Hockey error logging. No HTTP requests leave the fixture. CLI terminal catches still log arbitrary errors and S3 errors propagate to them; these boundaries require their own characterization before F-015 can close.

Aggregate F-015 checkpoint: `node .yarn/releases/yarn-3.3.1.cjs test:bin` passes335 cases after restoration, `/tmp/pkg001-deployment-errors-full-bin.log`. This is Node-only evidence, not final composed native/package qualification.

F-015 CLI/stream follow-up: baseline `50e86c1a` has four passing CLI controls/eight failing negative cases before fixed terminal diagnostics. All12 pass; restored raw logging fails eight. Stream baseline `0f6eb9ff` has one success/two failures: missing-file errors escape the upload promise and dry runs open assets. The corrected stream lifetime has four passing real-file/FormData cases (success/missing/dry/early transport rejection); original code and omitted-cleanup perturbations fail intended cases and are restored. The cleanup perturbation initially reports missing fixture completion because the unconsumed stream never closes; the test now asserts completion explicitly before parsing output. No timing sleep is used: fixture awaits actual close events. Tooling types and forced scoped lint pass. Full Node tooling qualification passes351 cases (`node .yarn/releases/yarn-3.3.1.cjs test:bin`), evidence `/tmp/pkg001-deployment-closeout-full-bin.log`. No actual remote deployment was performed; review/integration/final composed evidence remain open.

### F-016 — Configuration downloader failure propagation and archive ownership

Owner: existing PKG-001, INV-010. Baseline `4e7f98b2` passes successful extraction and fails four targets: axios rejection and response stream error escape the returned download promise; malformed ZIP failure leaves the archive; pre-existing `archive.zip` is overwritten. The synthetic fixture observes real promise settlement/unhandled events at process exit rather than relying on a timeout, captures no real credentials and performs no network request. This establishes library behavior, not a claim that every current CLI failure exits successfully.

A scoped Yarn patch to copy-config2.3.13 awaits axios and Node's stream pipeline, uses exclusive archive creation and removes only the archive it owns. Fixed errors prevent raw request/stream payloads reaching the existing CLI catch. Five cases pass; temporarily restoring the original installed module reproduces four failures and restoration passes. Root/bin/Playwright types, scoped lint, immutable install and fixture syntax pass. All356 Node tooling tests pass with the patch installed (`/tmp/pkg001-config-download-bin.log`). The first sandboxed install failed only on global cache writes and was retried with authorized cache access.

Reproduce: `node node_modules/mocha/bin/mocha.js --require .babel-register.js bin/deploy-tools/config-download.test.ts`; `yarn test:types`; `yarn test:bin`; `yarn install --immutable --mode=skip-build`. Logs `/tmp/pkg001-config-download-*`. The patch is local maintenance until an upstream version supplies equivalent behavior; remove it only after the same targets pass without it. Actual configuration-server failures, native platform behavior and composition with ELC-003's patched axios remain unqualified. Source maps in the vendor package are unchanged; tests execute the patched JavaScript directly.
