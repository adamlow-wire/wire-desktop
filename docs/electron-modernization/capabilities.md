# Desktop capability and confidence matrix

## Review-handoff evidence

M3 and functional M4 are integrated at 6e27c614 through PR60; [status](status.md) records final qualification. TST-006 must map every retained row below to baseline provenance, current assertions, sensitivity, actual platform evidence and uncovered behavior before handoff. Existing confidence labels do not certify full coverage. The maintainer reports successful Windows preview checks, but the exact build/scenario/OS record is not yet supplied; do not upgrade all manual confidence from this report. Signing validation is deferred to Wire after review; packaging, real OS and live SSO/E2EI rows remain explicitly limited until evidenced.

The current TST-006 audit reopens CAP-001 for F-008: pending lifecycle work behind an environment-approval dialog was unbounded. The local 32-operation queue candidate has sensitive Node-only ordering/authority/overload/recovery tests; hosted qualification is pending. Earlier DCP-002/DCP-004 acceptance is a historical checkpoint, not acceptance of this remediation. See [findings](review-findings.md).

Packaged legacy-account migration has a bounded new checkpoint: [PKG-003 draft #65 package run `35919774438`](https://github.com/adamlow-wire/wire-desktop/actions/runs/35919774438) starts the actual unsigned Linux AppImage and macOS app from a seeded original file-origin profile and observes the exact migrated account ID. Windows stops earlier in the fake-media native fixture, so packaged Windows migration and released-installation upgrade/rollback remain unqualified. This does not resolve ciphertext ownership or the full DCP-021 settings/cache matrix.

## Rules

September 15 reconciliation: the [M3 acceptance audit](m3-acceptance.md) records integrated final-head qualification. Checkpoint paragraphs below are historical evidence, not current open-item or deployment claims. Functional M4 and later packaged/customer qualification remain distinct. Use `status.md` for current execution.

- SEC-010 CSP checkpoint: PR #42 passed final-head hosted gates and merged. Real production/development shell startup and ordinary-script eval/Function denial are sensitivity-tested. Custom-scheme/account-state migration remains open; this is not global SEC-010 completion.

- CAP-001 renderer-loss harness checkpoint: PR #40 passed all-platform gates and merged. Actual renderer termination preserves recovery/runtime assertions and deadlines, with a sensitivity-proven pre-replacement revocation check. This does not claim production account migration complete.
- CAP-001 metadata checkpoint: sensitivity-proven reducer tests preserve targeted known metadata updates; nine hostile cases reproduce and then reject desktop-owned identity/session/state overwrites. Malformed-message and spoofed-property guards are tested. Two further targets prevent cross-account picture removal and prior-state/action mutation. The real sandboxed product fixture verifies metadata rejection and separate default/persistent-session cookies across cold restarts; re-enabling unknown fields fails it. PR #41 passed final-head hosted gates and merged; main-owned production state and custom-backend destination authorization remain open.
- CAP-001 profile preparation: 26 local profile/reader tests cover exact legacy mappings, single import, isolated storage reads, retained cookies, bounded data and failed-write preservation. The reader disables page scripts, Node and webviews. This is not yet production-wired and does not raise DCP-002/DCP-004 migration confidence.
- CAP-001 native owner preparation: twelve real-Electron tests cover pre-navigation main-owned identity, effective preferences, exact native selection/bounds, legacy sessions, isolated cookies, awaited closure, stale initialization and real renderer-loss recovery. Partition alias/close races and destroyed-view cleanup were reproduced and fixed; isolation/redirect mutations fail. The owner is not production-wired, so DCP-002/DCP-004 cutover acceptance remains open.
- This matrix tracks behavioral confidence, not implementation progress alone.
- CAP-001 controller checkpoint: 16 real-Electron tests connect named shell/guest IPC, state and native views, including the bundled webapp preload without a webview host. Targeted metadata/join, aggregate badges, isolated cookie deletion, failed-deletion retry, stale authority and destination approval paths pass. Added native menu queue, reload/session retention and per-account load recovery checks pass locally. The working copy now switches default startup/UI, but full product qualification remains open; this does not raise DCP-002/DCP-004 confidence or close acceptance.
- CAP-001 default cutover checkpoint: two actual sandboxed product regressions pass on Linux, preserving exact-account lifecycle operations, native-menu removal, main-owned metadata identity and default/persistent cookies across cold restart. Tests explicitly require native child views and disabled shell webviews. App-lock bootstrap replaces the reproduced premature synchronous configuration read without an origin exception. Desktop action/SSO routing, full native keyboard/focus behavior, cleanup policy and cross-platform qualification remain open; no confidence increase is claimed yet.
- `current confidence` describes qualified behavior, including the integrated migration; it does not imply unrun release or customer-environment qualification.
- `target evidence` describes what must exist before migration is accepted.
- Update a row when tests, platform support, product scope, or confidence changes.
- Detailed work remains in `plan.md`; do not create competing task lists here.

Confidence values are `none`, `low`, `medium`, or `high`. `High` requires representative automated behavior and packaged/platform evidence where applicable.

| Capability ID | Capability | Platforms | Current evidence | Current confidence | Characterization owner/work | Migration work | Target evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| DCP-022 | E2EI enrolment and renewal | Windows, macOS, Linux | Merged PR #53 qualifies sensitive same-account authorization/cancellation/interactive-auth-required transport and foreign-navigation denial on all three native platforms with full Windows/macOS E2E/report; live OIDC/ACME enrolment, verified identity and renewal remain unrun downstream QA | none | TST-004, CAP-002 | CAP-002, SEC-008 | M3 automated desktop-boundary acceptance per approved plan revision1.5.43; downstream QA must qualify actual enrolment/verified identity/restart, failure/renewal and account isolation before customer-compatibility claims |
| DCP-001 | Normal login and registration | Windows, macOS, Linux | Final PR #56 passes all-platform native startup/ownership/security tests and 48-case Windows/macOS E2E; sensitive redirect and non-secret failure tests preserve the live owning account | medium | BASE-002 | CAP-001 | Development E2E plus packaged login smoke on supported platforms |
| DCP-002 | Multi-account add/switch/remove | Windows, macOS, Linux | PR #47 merged after final-head Windows/macOS critical/regression E2E and all-platform native/package gates; main-owned add/switch/remove, original-account action queues, IPC/session/cookie isolation and exact-target cleanup/retry are sensitivity-proven | medium | BASE-002, TST-004 | CAP-001 | Account-targeted product action routing, lifecycle E2E, and packaged three-account switch/remove smoke |
| DCP-003 | Enterprise and automated SSO | Windows, macOS, Linux | Deterministic Electron flow, main-created isolated-session popup, navigation/redirect deny cases, initiating-page cleanup, account-owned close/focus controls, and shell-only account-limit warning; CAP-002 branch reproduces then repairs backend-style native success/error delivery with error labels, per-flow sessions, callback replay/ambiguity rejection, account-cookie isolation and cancellation; all three former quarantines pass; PRs #43/#53 are merged with final all-platform native and Windows/macOS E2E/report gates; live IdP evidence remains pending for downstream QA, separate from M3 automated acceptance | medium | TST-002 | CAP-002 | Controlled IdP E2E, protocol abuse tests, cookie/account isolation, cleanup |
| DCP-004 | Logout, data clearing, and persistence | Windows, macOS, Linux | PR #47 qualifies exact-target logout/removal, retained unrelated cookies/storage, retry and cold restart on supported native platforms; identity/partition metadata denial and normal native quit remain explicit assertions | medium | BASE-002 | CAP-001, PKG-003 | Restart persistence and released-version migration fixtures |
| DCP-005 | Tray and native application menu | Windows, macOS, Linux | Reviewed CAP-004 audit and PR #60 qualify native tray/menu policy and development E2E. F-022 exposed eight missing About/tray/badge PNGs in actual earlier ASARs; scoped verifier and all three rebuilt internal Linux installers now contain them. Windows download-complete icon uses the packaged logo. Visible packaged tray/menu/tooltip and Windows dialog smoke remain open | medium | TST-003 | CAP-004, PKG-001 | Final cutover product/platform evidence; packaged native tray/menu/tooltip smoke |
| DCP-006 | Notifications, badges, and activation routing | Windows, macOS, Linux | PR #60 head 41066868 qualifies account-aware unread totals and exact notification account/conversation activation on all three platforms; native activation/badge IPC denies malformed and foreign authority. Tests observe real Notification objects and invoke onclick programmatically, not OS toast clicks | medium | TST-003 | CAP-004 | Correct account/conversation routing and packaged platform smoke |
| DCP-007 | Camera and microphone calling | Windows, macOS, Linux | PR #60 head 41066868 passes supported-platform authenticated calling and native synthetic-device allow/deny; device consent never grants native remote display capture. The negative call contract requires a rejected native media consent, a visible user warning and no call cell for either participant; the current webapp shows a Microphone disabled heading rather than the old No camera access text. Actual OS permission/hardware and packaged call smoke remain QA | medium | BASE-002, TST-004 | CAP-003 | Allow/deny permission tests and packaged call smoke |
| DCP-008 | Display capture and screen sharing | Windows, macOS, Linux | PR #60 head 41066868 qualifies the fixed local chooser, explicit main-selected source, native remote legacy/display denial, bounded selected-video relay, cancellation/Stop/clone ending and owner-loss tests on all three platforms. Sources are test-owned synthetic frames; real OS capture/privacy/performance QA remains open | medium | TST-004 | SEC-009, CAP-003 | User-gesture authorization, source selection, permission deny cases, and packaged smoke |
| DCP-009 | Picture-in-picture call windows | Windows, macOS, Linux | PR #60 head 41066868 qualifies fixed sandboxed/isolated PiP preferences, capture-only preload, exact parent account/session/document, one child/account and parent navigation/crash/destruction cleanup; actual account/PiP product capture fixtures pass on all three platforms | medium | BASE-002, TST-004 | CAP-003 | Fixed preferences, navigation policy, lifecycle and interaction tests |
| DCP-010 | Deep links and single-instance routing | Windows, macOS, Linux | SEC-013 local strict parser/dispatch tests preserve recognized federated and file-list routes and reject malformed actions; application-shell-only exact bounded/rate-limited IPC remains intact. PRs #39/#38 passed hosted gates and cover internal popup routing; account-targeted lifecycle policy is accepted in reviewed PR #51 with final native/package and authenticated E2E/report gates | medium | TST-004 | CAP-006, SEC-013 | Valid lifecycle routing plus malformed, oversized, recursive, and hostile input; F-018 diagnostic candidatea2d77b19 under reopened SEC-013 awaits final native/composed qualification |
| DCP-011 | Proxy configuration and authentication | Windows, macOS, Linux | Proxy parsing, exact prompt identity, sender authorization, bounded credentials, one-shot submit/cancel, retry, and reload tests; real stylesheet loading and explicit unknown-resource cancellation pass locally under SEC-008 | high | BASE-002 | CAP-005 | F-019 candidate1381a854 preserves proxy endpoint/HTTPS/SOCKS transport;9 Node cases pass; actual native resolution and packaged authenticated-proxy smoke per OS/enterprise variants remain open; F-036 synthetic rejected-submit error reaches the production prompt console sink and needs fixed diagnostics |
| DCP-012 | Certificate verification and interception handling | Windows, macOS, Linux | PR #52 integrates fail-closed callback/warning/save behavior and native TLS coverage. PR #54 adds sensitive retained pinning-override and mandatory Chromium tests, with27 focused native cases and all final-head build/lint/analysis, native platforms and full E2E/report passing before merge2ee0be97 | medium | BASE-002, TST-004 | CAP-005 | Final-head pin/Chromium failure, retained native override, retry, save and fail-closed tests; broad process-wide override remains an independent-review concern |
| DCP-013 | Managed configuration | Windows, macOS, Linux | PRs #45/#49/#52 qualify immutable authorized configuration, fail-closed machine endpoint reads, saved-destination and download containment, real Windows registry/junction denial, representative Linux/macOS backends and exact proxy-session routing; all historical final native/package and Windows/macOS E2E/report gates pass. The optional OS regional-locale bridge parity follow-up from PR #69 has local type/Node evidence; native preload and composed platform qualification remain pending | medium | BASE-002 | CAP-005 | Backend fixtures, packaged managed-device tests, and optional regional-locale exposure |
| DCP-014 | External links and context menus | Windows, macOS, Linux | Historical SEC-008 URL confusion, credentials, control-character, app-protocol recursion and size deny tests; F-035 real-Electron foreign-child noreferrer external-link bypass reproduces on all three OSes and blocks handoff pending a scoped fix; sandbox-compatible main-owned image copy, account-only save-picture contract, and sensitivity-proven context-menu actions; local F-011 single-active-save remedy awaits native/composed qualification | medium | TST-004 | SEC-004, SEC-008, SEC-013 | Allowed/denied protocol tests, isolated image-action bridge, and packaged OS-browser/save smoke |
| DCP-015 | Open Graph retrieval | Main process | Sensitivity-proven metadata behavior, account authorization and bounded schemas/quotas; public-only pinned-DNS transport and bounded parsing pass allow/deny tests; actual sandboxed IPC rejects a local target with zero requests. PR #44 passed final-head build/coverage, all-platform packages and Windows/macOS E2E/report, then merged | medium | TST-004 | SEC-012 | SSRF, DNS/redirect, protocol, credential, timeout, and response-byte tests |
| DCP-016 | Safe storage bridge | Windows, macOS, Linux | Account-only sender authorization, request/response size schemas, per-view quotas, hostile-sender and no-side-effect deny tests; ciphertext ownership and packaged key-store behavior remain gaps | medium | TST-004 | SEC-002, SEC-003 | Authorized account contract and hostile/cross-account deny tests |
| DCP-017 | Squirrel install and updates | Windows | PR #63 head e760f10e builds/downloads unsigned Setup/full nupkg/RELEASES; installed startup, ASAR identity, managed policy and effective fuses pass in hosted Windows. F-031 upload selector is published; S3 promotion/fuse follow-ups are local and need final composition. GitHub draft asset selection and signed update/rollback remain unrun | medium | BASE-002 | PKG-001, PKG-002 | Final composed unsigned artifact/selector check now; signed fresh install/update/rollback and legacy migration later |
| DCP-018 | MSI managed deployment | Windows | PR #63 head e760f10e builds/downloads unsigned MSI; hosted administrative ASAR extraction, installed startup, managed/proxy policy, effective fuses and uninstall pass. Signed upgrade/repair and enterprise coexistence remain unrun | medium | BASE-002 | PKG-001, PKG-002 | Final composed unsigned check now; signed install/upgrade/repair/uninstall and Squirrel coexistence later |
| DCP-019 | macOS signing, notarization, and update | macOS | F-006 candidate has Node-only signing order/failure/entitlement contracts; actual signed qualification and F-007 updater remediation remain open | low | BASE-002 | PKG-001, PKG-002 | Signed/notarized launch and update on supported macOS |
| DCP-020 | Linux packaging and desktop integration | Linux | Reviewed PR #58/60 qualify development Linux E2E/native package boundaries. Local internal AppImage/deb/rpm from source 9e058e73 now pass 30-file ASAR, identical approved 407-package inventory, Wire installed icons, archived desktopName/desktop-entry match and six-fuse checks; F-021 AppImage sandbox bypass is absent. CI 32b31c2d requests all-format build/static/extracted-native checks but has not run. Actual install/visible desktop association, protocol/tray behavior and final-head CI remain open | low | BASE-002, TST-005 | PKG-001, PKG-002 | Packaged install/launch, protocol, tray/menu and representative E2E |
| DCP-021 | Settings and user-data migration | Windows, macOS, Linux | F-010 settings migration/persistence fixtures cover preservation, corruption, write failure and recovery locally; native/platform qualification pending | low | BASE-002 | PKG-003 | Released-version fixtures, recovery path, no silent loss/corruption |

## Acceptance validation

`Automated`, `packaged`, and `manual` are separate obligations. `N/A` requires an approved scope decision; an empty result never implies a pass.

| ID | Required behavior | Automated validation | Packaged validation | Manual/provider validation | Acceptance authority |
| --- | --- | --- | --- | --- | --- |
| DCP-022 | E2EI-enabled accounts enrol and renew through authenticated OIDC/ACME flows without escaping account boundaries | M3: deterministic desktop redirect/callback isolation, cancellation/error transport and SSO lifecycle; actual enrolment, verified state and renewal belong to downstream live QA | Enrolment and retained identity smoke on each OS | Downstream QA environment under Q-011 and qa-sso-e2ei.md; distinct from SSO login | Product and Security |
| DCP-001 | Login/registration opens the intended account in its isolated session | Critical login/register E2E and partition assertion | Fresh-profile login on each supported OS | Mandatory backend/federation variants | Product; Security for session boundary |
| DCP-002 | Add/switch/remove affects only the selected account and routes activation correctly | Lifecycle E2E plus cross-account IPC/storage deny tests | PR #47 merged after final-head Windows/macOS critical/regression E2E and all-platform native/package gates; main-owned add/switch/remove, original-account action queues, IPC/session/cookie isolation and exact-target cleanup/retry are sensitivity-proven | Keyboard/accessibility review | Product and Security |
| DCP-003 | SSO uses one-time validated responses and moves only intended cookies to the intended account | Protocol abuse, lifecycle, cookie isolation, controlled IdP fixture | SSO launch/cancel/success on each OS | Approved IdP/federation matrix under Q-002 | Product and Security |
| DCP-004 | Logout/removal clears only approved state and retained state survives restart | Per-account deletion and restart/migration fixtures | PR #47 qualifies exact-target logout/removal, retained unrelated cookies/storage, retry and cold restart on supported native platforms; identity/partition metadata denial and normal native quit remain explicit assertions | Data-retention expectation review | Product and Security |
| DCP-005 | Tray/menu actions and unread state match the active application/account state | Policy and platform-adapter tests | Native tray/menu/tooltip smoke per OS | Visual and accessibility check | Product and Platform |
| DCP-006 | Notifications/badges activate the correct account/conversation without data crossover | Routing and deny tests | Signed notification, app badge, overlay/bounce smoke | OS settings/permission review | Product, Security, Platform |
| DCP-007 | Camera/microphone work only for authorized Wire views and user flows | Permission allow/deny plus fake-device call E2E | One call smoke per OS | Native permission prompt/revocation | Product and Security |
| DCP-008 | Display capture requires authorized origin/view and user choice | Source-selection, gesture, cancel, hostile-view tests | Real-source selection, privacy denial and Stop smoke per OS under PKG-001; unsigned Windows preview is available, not a manual pass | Native chooser and permission review | Product and Security |
| DCP-009 | PiP has fixed safe preferences, correct lifecycle, and no navigation escape | Preference/navigation/focus/cleanup tests | Create/interact/close smoke per OS | Accessibility/always-on-top review | Product and Security |
| DCP-010 | Valid deep links route once at every lifecycle point; malformed/hostile inputs do nothing privileged | Parser and startup/second-instance matrix | Registered-protocol fresh/running-app smoke | OS browser invocation | Product and Security |
| DCP-011 | Proxy settings/auth apply only to intended sessions and cancellation fails closed | Exact-prompt sender, parser, bounded credential, one-shot callback, cancellation, retry, and session-scope tests | Authenticated proxy smoke per OS | Enterprise proxy variants | Enterprise Product and Security |
| DCP-012 | Certificate validation fails closed; the retained native override cannot bypass Chromium trust | Pin/verifier/dialog failure, one-shot callbacks, explicit override/reset and save cancellation | Native TLS rejection and certificate/save routing; OS trust UI smoke remains later | Broad process-wide override review; no MDM pinning policy | Enterprise Product and Security |
| DCP-013 | Managed config is read from the correct backend and exposed as immutable authorized policy; Windows App-lock override requires an explicit enabled Wire policy value, not generic MDM enrollment or Entra join | Backend fixtures, generic-enrollment denial and bridge authorization | Managed/unmanaged profile smoke per OS | Wire policy deployment/readback | Enterprise Product and Security |
| DCP-014 | Only approved external schemes leave the app; context actions do not expose broader privilege | URL/protocol/encoding deny matrix | Default-browser and context-menu smoke | OS association review | Product and Security |
| DCP-015 | Open Graph retrieval accepts only safe destinations/content within hard limits | SSRF, DNS/redirect, credential, time, byte, type tests | N/A after policy approval | Enterprise proxy interaction if retained | Security and Product |
| DCP-016 | Encryption/decryption is available only to the owning authorized account contract | Schema, sender, cross-account, size/error tests | Keychain/credential-store round trip per OS | Locked/unavailable key store behavior | Security |
| DCP-017 | Signed Squirrel install/update/rollback preserves state and protocol ownership | Selection/version/update metadata tests | Fresh, upgrade, rollback, uninstall on supported Windows | Windows enterprise coexistence | Release Engineering and Security |
| DCP-018 | MSI install/upgrade/repair/uninstall and Squirrel coexistence follow the approved deployment contract | Build/selection tests after upstream merge | Signed Windows matrix | Software-deployment tooling | Enterprise Product and Release Engineering |
| DCP-019 | Signed/notarized macOS app launches, notifies, updates, and survives restart | Build config/update metadata tests | Signed/notarized install/update/rollback | Gatekeeper and permission prompts | Release Engineering and Security |
| DCP-020 | Linux artifact installs/launches with protocol, tray/menu, sandbox/fuses, and defined feature parity | Build config and representative E2E | AppImage/deb/rpm install/launch as approved | Desktop-environment matrix under Q-006 | Product and Platform |
| DCP-021 | Released user state migrates without silent loss and has recovery/rollback | Versioned fixtures, interrupted/corrupt migration tests | Upgrade and rollback from approved releases per OS | Recovery messaging and support procedure | Product and Security |

The matrix is complete as an engineering acceptance specification. Q-008 maps its role labels to the solo maintainer; they remain useful descriptions of the decision being made rather than claims that several reviewers exist. Merging PR #1 records the maintainer's M0 acceptance of this matrix, which is then recorded by the M0 closure PR.

## Coverage interpretation

CAP-005 September 14 reconciliation: exact-origin native fixture isolation removes independently observed Chromium background TLS traffic without relaxing denial assertions. Five focused runs pass 22/22 each and the full current native suite passes 856/856 on Linux. Q-005 and final hosted certificate qualification remain open.

CAP-005 certificate completion: seven synthetic decision baselines and four error/callback targets pass locally. Verifier and dialog exceptions previously left Electron's callback unanswered; the repair denies once, without changing pinning exception scope or retrying a throwing callback. Replacing Chromium delegation with unconditional success fails the positive targets. These stubbed-boundary tests do not establish native TLS, trust-store, exception-scope or packaged DCP-012 acceptance.

CAP-005 native denial/recovery: two regressions detect permanently suppressed warnings after dialog rejection; the repair preserves the sensitivity-proven normal cooldown. Two real Electron TLS tests reject an untrusted loopback certificate, including on dialog failure, before any HTTP request. Omitting the callback fails the target rather than passing on timeout. The combined suite passes 16/16 on Linux. Trusted-chain acceptance, pinning exceptions, OS trust UI and other-platform evidence remain open.

CAP-005 certificate detail/save: actual temporary-file contents and no-write cancellation assertions protect save behavior. Correct Electron cancellation payloads expose empty-path and stale-path writes; the repair requires non-cancellation and a nonempty path. Mac adapter tests assert owner/certificate routing and rejection recovery without opening native trust UI. Write omission and incorrect platform routing fail their targets. Combined certificate/TLS tests pass 22/22 on Linux; simulated macOS branching is not macOS qualification.

The existing CAP-002 transport candidate is reconciled with current native accounts. Its unregistered-provider denial and same-origin OIDC-return storage assertions are intentionally separate from actual enrolment/renewal. Existing account-navigation fixes are already integrated; the remaining candidate fixture does not establish OIDC/ACME or verified-device acceptance. Dedicated live provider inputs remain pending under Q-011.

PR #47 merged as `683ac9af` after reviewed head `78231f74` passed [all native/package platforms](https://github.com/adamlow-wire/wire-desktop/actions/runs/34833701114) and [authenticated Windows/macOS E2E/report](https://github.com/adamlow-wire/wire-desktop/actions/runs/34834177940). CAP-001/SEC-007/SEC-009 are complete; each E2E platform has 39 initial passes/one login retry, no skips or teardown errors. Main-owned account/session behavior and scoped device consent are integrated. Display/thumbnail capture remains denied under CAP-003/M4. Remaining enterprise/protocol/lifecycle/provider criteria are listed in status.md.

CAP-005 machine endpoint reader now treats an unavailable registry module or failed read as configured-invalid, blocking CLI/per-user/default/saved-account fallback with a non-secret diagnostic. Two new targets fail before the change; thirteen reader/environment cases pass afterward. Successful reads with no value remain unmanaged. Actual Windows registry/destination and final hosted gates remain required; this does not close certificate policy.

CAP-005 backend candidate: 23 fixture-adapter characterization targets cover Linux configuration, macOS preferences, Windows machine/per-user policy and error fallbacks. Seven intended targets failed opt-out/key/per-user perturbations before restoration. The original combined backend/central/authorized-IPC group passed 35/35 on Linux. The adapter declaration matches registry-js string hive tags and readonly results without changing emitted behavior. These fixtures do not prove native registry loading or deployment; native Windows reader-to-destination qualification is being prepared.

September 14 CAP-001 integration: PR #48 passed native/package checks on Windows/macOS/Linux and authenticated Windows/macOS E2E/report before merging into the account branch. The existing managed-destination candidate now constrains saved URLs and native server-change approval to the configured canonical HTTPS origin. Invalid policy and foreign destinations fail before navigation, prompting or profile replacement; saved records remain intact. Combined Linux qualification passes 58 focused native tests and two product fixtures. PR #47 final-head qualification is complete; CAP-005 native Windows registry-to-product evidence remains required.

September 14 SEC-009 security review found that display-media denial did not stop the separate desktop-source IPC from returning thumbnails. The product regression safely replaces only native enumeration with an inert counter: unchanged production makes one unapproved call. Production accounts now omit that capability pending an authorized source-selection flow. The regression requires rejection with zero enumeration calls both before and after device consent. No host screen is captured; enabling display sharing remains CAP-003/M4; M3 retains sensitive capture and thumbnail denial.

PR #63 seventh-run DCP-007 diagnostic: [Linux](https://github.com/adamlow-wire/wire-desktop/actions/runs/35890398948) macOS and Windows each pass 75 authenticated cases and fail only the negative calling case's old `No camera access` wording. On each failed attempt the test has already observed a rejected native microphone/camera consent and no caller call cell. The retained Linux accessibility snapshots show a `Microphone disabled` dialog; its device-not-found explanation is current webapp copy, not evidence that native consent was granted. The scoped test candidate asserts that visible heading plus absence of call cells for both accounts, preserving the native-denial check. The [eighth hosted E2E run](https://github.com/adamlow-wire/wire-desktop/actions/runs/35895375630) passes that corrected negative calling case on Windows/macOS/Linux, retaining the native-denial and both no-call assertions. Windows/Linux each pass 76/76; macOS has 75 passing plus one unrelated logout case that passed on retry after a two-minute reauthentication wait. The DCP-007 observed warning/denial contract is now qualified on this head, while final composed-head E2E and the macOS logout retry remain open. The warning's device-not-found explanation should receive product-copy review under DCP-007 external QA.

General-launcher DCP-007 preparation now exercises actual account permission policy with pre-start test-only dialog responses and synthetic devices, without fake permission UI. Allow/deny and selected-account/reopen fixtures pass 3/3 locally; an incorrectly approving deny response fails. The negative calling E2E now requests denial through that harness instead of overriding the session handler. Authenticated calling and native OS qualification are still pending; confidence is unchanged.

The actual E2E launcher now has fresh-profile and restored-selected-account regression fixtures. Returning the first account deliberately fails; correct selection passes including reopen, with an intermittent shutdown failure retained. The active-account watcher now consumes the existing main-owned snapshot bridge; the old DOM observer produces no callback in the regression. Two-way selection and repeated-selection suppression pass, and forcing the first snapshot account fails. A combined permission failure records an unfocused owner and no camera consent request. Establishing real focus before each media user flow yields five passing product tests; deliberately hiding the owner still fails approval. Production policy/assertions are unchanged. These tests do not establish final DCP-002/DCP-004/DCP-007 qualification; see current status for evidence and next diagnostics.

SEC-009 now composes account-scoped policy and native consent in the local application candidate; integration is unchanged. Baseline denial, exact sender/session/origin checks, document revocation, scope separation, pending-dialog cancellation and background initiation have sensitivity-proven targets. Earlier focused policy/adapter branch coverage was 100%/96.3%, not final aggregate qualification. Renderer-loss delivery is simulated; native document navigation/destruction and Linux dialog cancellation are real.

DCP-006/DCP-007 product evidence: `accountPermissions.spec.ts` passes on Linux against the activated application. It verifies real foreground eligibility, separate notification/microphone/camera approval, permission-state publication through Wire's existing event, and notification/microphone denial after reload. Synthetic devices are required and tracks stop immediately; native dialog responses are controlled by the test runner, without a production bypass. The pre-activation target fails. Existing lifecycle/restart fixtures pass 2/2 after activation. See [current evidence](./status.md).

The isolated preload cannot trust a page override or a supplied permission result: it accepts only an argument-free main command and publishes the validated browser response. Ready/visible eligibility, once-per-document initiation and native retry are tested. Browser `onchange` did not deliver in the probe, so this uses Wire's existing permission-state event. No notification was displayed during these probes.

The subsequent PR #48 native/platform and authenticated calling gates pass as recorded in status.md. Final integration still requires #47 qualification. Display-source choice is CAP-003/M4; its absence does not waive M3 denial or imply CAP-003 completion.

DCP-008 now has a real Linux legacy-capture deny target after camera/microphone consent. It targets only its own fixture window and stops any returned tracks immediately. Removing the empty-media-types guard makes it return video and fail; the restored three-test media suite passes. This proves that denial guard, not modern source-selection compatibility; the callback distinction remains an open design issue in DEC-009.

CAP-001 removal failure: native regression reproduces indefinite loading after cleanup rejects. The shell now receives only a boolean failure flag and offers exact-account removal retry, leaving the profile record intact and not presenting reload as cleanup retry. The React target fails before implementation and passes after it; schema tests accept the boolean and reject strings. This does not change the data-deletion scope or establish final platform qualification.

CAP-001 environment approval: seven local native tests protect exact-candidate preservation, owner binding, localized canonical-origin display, cancellation and invalid/closed-owner rejection. Changing the dialog default to Connect fails the characterization test. Dialog presentation is stubbed; actual platform presentation and complete programmatic destination policy are not established by these tests.

CAP-001 background destination checkpoint (`496e636a`): approved cross-origin changes replace only the owning view and registry origin while retaining its session; former-origin metadata is rejected afterward. Retaining the old destination fails the new origin assertion. A separate regression reproduces and fixes selection of a missing foreground view after a background server change, preserving the foreground error and keeping background content hidden. The 37-test controller suite and rebuilt Linux lifecycle/restart fixtures pass; final hosted qualification remains open.

CAP-001 add/retry and shell controls: a real native regression detects old-account content underneath a failed new selection; the fix hides it while retaining the old account and verifies retry/switch-back behavior. Renderer tests cover current-snapshot selection, fail-closed profile bootstrap and keyboard native-menu focus, including rejection and disconnected-target paths. Wrong-account/focus perturbations fail their targets. Clean aggregate diff coverage now passes locally; this does not establish final platform or broader CAP-001 acceptance.

CAP-001 persistent cleanup: actual Linux product E2E now verifies removed versus retained account cookies, local storage, IndexedDB and Cache Storage after app exit/restart. Cookies are explicitly persistent. A cookies/cache-only cleanup mutation leaves the removed account's local storage and database behind and fails the new assertions. Platform CI explicitly includes all account modules and startup routing rather than relying on incidental test-name matches; that expanded gate passed on Windows, macOS and Linux at `23fb964b` in [run 34372128865](https://github.com/adamlow-wire/wire-desktop/actions/runs/34372128865). Residual directories and forensic erasure are not claimed complete.

CAP-001 session-cleanup checkpoint: the old cookie/cache sequence leaves HTTP credentials cached. Native synthetic-auth tests reproduce that gap; the replacement clears browser data and authentication state and closes connections only on the target session. Cookies, local storage, IndexedDB and Cache Storage are checked alongside another session's retained state. API errors propagate and retry works. The API contract is documented in [Electron 43.4.0 session documentation](https://github.com/electron/electron/blob/v43.4.0/docs/api/session.md). Rebuilt Linux removal/restart fixtures pass, but raw persistent-directory removal and complete residual-data/platform acceptance are not established.

CAP-001 deletion-route retirement: PR #47 removes the legacy production deletion binder and shell capability. A read-only runtime assertion fails against the old handler registration and passes after rebuilding; Linux lifecycle/restart fixtures still verify native removal, exact-account cookie/log clearing and retained-account data. Legacy helper tests do not imply the channel remains registered. Exact-target native clearing/retry/restart and final platform qualification pass through merged PR #47. Browser-data clearing does not imply forensic erasure.

CAP-001 native log-cleanup checkpoint: strict main-owned filesystem cleanup now propagates errors before account removal. Tests verify supported layouts, linked-path denial, unaffected external/other-account files and profile retention/retry after failure. Rebuilt Linux product E2E checks actual removed/retained account log markers after profile removal completes. Persistent partition-directory cleanup, legacy-delete authority retirement and final Windows/macOS qualification remain open; this is not complete DCP-004 acceptance.

CAP-001 cleanup prerequisite: log-directory selection now rejects invalid account IDs and out-of-root candidate paths while preserving supported layouts. Both new deny targets reproduce unsafe selection before the guards. This strengthens candidate validation only; native filesystem cleanup, linked-path safety and failure propagation remain open for DCP-004.

CAP-001/CAP-006 ordered-action checkpoint: a reproduced simultaneous login/join race is fixed with bounded main-only desktop dispatch and recovery after rejected commands. Rebuilt Linux product E2E emits real protocol events back-to-back and verifies the new account alone receives the invitation. Startup/running location, login reuse and ordering now have local evidence; final cross-platform and packaged protocol/focus acceptance are still open.

CAP-001/CAP-006 focus checkpoint: regression tests cover restore/focus adapter calls for visible/minimized windows and startup-buffered login requests, while plain location links retain non-focus behavior. All three targets fail before the fix. Packaged OS focus behavior and ordered multi-action delivery still need qualification.

CAP-001/CAP-006 start-login checkpoint: native tests preserve unfinished-view/session reuse, bounded creation, unchanged state at the account limit and extra-argument denial. The target fails before the missing route is implemented. Startup focus, ordered multiple actions and final product/platform qualification remain open.

CAP-001/CAP-006 incoming location/join checkpoint: validated native actions stay with the account selected at handling time and wait for that account's current webapp readiness. Local rebuilt lifecycle E2E now launches with a protocol argument and emits a running-app `open-url` event, asserting each changes only its intended account's hash. Shared startup hash injection is removed. Wrong-account routing fails the native target. Start-login/focus/multi-action ordering and final cross-platform evidence remain required for DCP-010.

CAP-001/CAP-006 startup buffering checkpoint: native tests verify bounded retention of recognized incoming actions before the primary window/native binding exists, exactly-once flushing and no early shell delivery. The binding-gap perturbation fails its target. Complete startup acceptance still requires location/join/start-login routing, removal of shared startup hash state, focus and multi-action ordering; this is not a full DCP-010 pass.

CAP-001/CAP-002 draft automated-SSO initiation: local native tests now cover validated codes, isolated creation, unfinished-account/session reuse, main-private SSO state, account-limit warnings and hiding the previous account when the SSO destination fails. Code-propagation removal fails the navigation target. Running-app initiation does not prove pre-window link delivery, hosted final-head compatibility or the controlled live IdP checkpoint; DCP-003 remains open.

CAP-001 draft native reload/About checkpoint: controller tests verify all-account reload with exact session/cookie retention, preserved selection, queued-authority revocation and healthy-account recovery after partial failure. About's actual request path uses the main-owned native binding; version requests remain tied to the account selected when invoked. These local tests strengthen DCP-002/DCP-004 without establishing final cross-platform cutover acceptance or changing confidence ratings.

SEC-012 preview contract: public default-port HTTP(S) only; no cookie replay or ambient proxy/account credentials. Private, authenticated-proxy and cookie-challenge previews may be unavailable without affecting ordinary login/backend traffic. Known title/description/site/type/URL and image fields, entities and title/first-image fallback remain compatible; arbitrary metadata paths and unused audio/video trees are excluded. PR #44 passed all final-head gates and merged as `62435cc6`; SEC-012 is complete without claiming broader certificate/configuration or M3 closure.

SEC-008 merged in PR #38 after all hosted gates passed. Main creates SSO windows because renderer-created children do not adopt the requested isolated session. Desktop close/focus and initiating-page cleanup are covered, but the window has no opener: the backend's normal web verdict still uses `window.opener.postMessage`, so successful IdP completion is not established. CAP-002 now reproduces and repairs that path locally using native redirects without restoring opener privileges; the one-time callback and cookie-scope tests pass. PR #43 passed final-head all-platform packages and authenticated Windows/macOS E2E before merge. Controlled live IdP evidence remains required. HTTPS enterprise IdP navigation remains supported; local HTTP initial origins remain available for deterministic fixtures. PiP preserves calling's empty/about:blank window, and account navigation/redirects remain on the registered origin.

SEC-006 checkpoint (2026-09-08, [PR #37](https://github.com/adamlow-wire/wire-desktop/pull/37)): all-platform package and authenticated Windows/macOS E2E gates passed with explicit OS sandboxing. Image copy is main-owned; save-image retrieval retains the account session. The `environment` bridge exposes read-only metadata/lookups, not settings mutation or main-process policy/diagnostic helpers. Bundled preloads use console diagnostics, not filesystem writes. This closes sandboxing, not the remaining capability-specific packaged/manual obligations.

SEC-005 checkpoint (2026-09-07, [PR #35](https://github.com/adamlow-wire/wire-desktop/pull/35)): isolated immutable compatibility APIs and fixed event adapters now have real-Electron tests. Authenticated Windows/macOS login, multi-account, logout, menu, notification, and calling E2E passed, alongside package baselines on all three platforms. Windows needed one unchanged rerun after a notification timeout. These results strengthen DCP-001–DCP-008 and DCP-013–DCP-016 without raising their confidence ratings: sandboxing, production view migration, platform key stores, and the listed packaged/manual obligations remain open.

A capability cannot reach `high` confidence solely through unit mocks. Security-sensitive capabilities also require the corresponding invariant tests. A platform-independent policy may be tested once, but each native integration must have representative evidence on every platform claimed in its row.

CAP-005 packaged configuration preparation adds an external normal-startup fixture for the actual packaged executable. It checks exact immutable App-lock policy and denied page Node access before/after a real test-owned OS backend value and restart. Local development-binary sensitivity/restoration passes; native package results remain pending. This does not qualify MDM deployment or certificate exceptions. The prepared managed package launch also exercises a real HTTP proxy challenge using the normal native environment-credential path; local callback-denial sensitivity and restoration pass, and hosted results remain pending.

CAP-005 proxy session regression: a real non-default account HTTP proxy challenge and native prompt reproduced an unrelated default-session routing change. The candidate now applies settings and cancellation reload to the challenged web contents. The same authenticated request succeeds while the unrelated session remains `DIRECT`; 67 focused native backend/proxy/IPC targets pass locally. Final native/package qualification remains required.

CAP-005 credential qualification: real proxy challenges also expose missing-credential prompt failure and foreign-proxy credential transmission. Automatic credentials now require the native challenge host/port to match configured settings; otherwise native prompting is required. No global credential fallback is permitted. The sensitive fixture checks zero foreign credentials, successful authentication and unrelated-session routing. PR #49 final-head gates remain required.

SEC-010 candidate preserves seven fixed role-specific `wire-app` resources for shell, About and proxy documents, exact session/document identity, and existing IPC capabilities. The established relative CSS/logo, prompt initialization and production/development ordinary-script CSP denial baselines remain intact. This branch now includes CAP-005 final candidate `2fd2f5d1`; renewed local and final hosted qualification is required. Older 821-test/eight-fixture results apply only to their recorded earlier composition.

CAP-006 preserved candidate prevents ordinary Windows lock losers from saving stale configuration or scheduling updates while retaining exact installed Squirrel lifecycle flags. Its actual secondary-process fixture uses the same short isolated profile and checks clean exit, exact selected-account delivery and unchanged account count. Current composition includes protocol `e31ff276` and proxy `fd3c5c35`; renew local tests and final integration-base native Windows/macOS/Linux/E2E gates before acceptance.

The earlier SEC-008 cutover was accepted after [PR #49](https://github.com/adamlow-wire/wire-desktop/pull/49) merges4f04a8a0: exact-origin navigation and default-denied popups, fixed SSO/PiP preferences, adversarial external policy, main-owned custom-backend approval and unreadable Windows machine-policy denial have combined [native/package qualification](https://github.com/adamlow-wire/wire-desktop/actions/runs/34848889664) and [authenticated E2E/report](https://github.com/adamlow-wire/wire-desktop/actions/runs/34848889715) at66f9d9db. Both authenticated platforms pass43 cases initially. CAP-005 certificate policy and CAP-002 live provider acceptance remain open. TST-006 later reopened SEC-008 as F-035 after a real-Electron foreign-child noreferrer popup opened an external URL on all three hosted platforms; the original top-level-only tests missed that case. The original cutover run remains historical evidence, not current acceptance of the child-frame origin boundary.

SEC-010 is complete through [PR #50](https://github.com/adamlow-wire/wire-desktop/pull/50), merged18235a5a. Normal shell/About/proxy content uses seven fixed role-specific scheme assets with minimal privileges and unchanged sandbox/preload/document authority; only conditional script-disabled legacy import accesses the former file origin. Actual relative assets, no-eval production/development CSP, package startup and account migration/restart pass all final-head platform and [authenticated E2E/report](https://github.com/adamlow-wire/wire-desktop/actions/runs/34857460712) gates. DEC-010 is accepted; no CAP-002 live provider or CAP-005 certificate-policy criterion is waived.

DCP-010 acceptance: CAP-006/SEC-013 close with reviewed PR #51 mergedc74d116e. Existing strict bounded parser/external deny paths and startup/running selected-account authorization combine with actual Electron-to-Electron handoff, zero loser exit, no stale writes and retained exact Squirrel handling. Final [native/package34862334704](https://github.com/adamlow-wire/wire-desktop/actions/runs/34862334704) passes all platforms; [authenticated E2E/report34862337125](https://github.com/adamlow-wire/wire-desktop/actions/runs/34862337125) passes43/platform (macOS43 initial, Windows41 initial/two retry passes), no skips or worker errors. Dedicated live SSO/E2EI product acceptance remains separately required by CAP-002/Q-011.

CAP-005 retained pinning policy: the maintainer requires product parity. Built-in pins and the existing native override remain; no MDM pinning policy exists in this desktop. Tests require pinning by default, continued denial without selection, denial of the triggering request, process-wide Chromium delegation only after explicit native selection, and mandatory Chromium-error denial despite the override. A fresh verifier initializes enabled; this module test is not presented as an actual OS-process restart test. The override is never saved by this class. Broad override scope remains a concern for independent security review; there is no new renderer authority or administrator-enforcement claim.

## September 14 maintainer-approved QA split

CAP-002 M3 acceptance now covers automated desktop-owned boundaries and supported-platform qualification. Live enterprise SSO and Keycloak/OIDC/ACME enrolment, verified-device persistence, restart and renewal remain **unqualified** and move to downstream QA under [qa-sso-e2ei.md](qa-sso-e2ei.md). This supersedes earlier notes making live environment access an M3 blocker. DCP-003/DCP-022 customer compatibility must not be claimed until that checklist is signed off. No application security contract changes.

September15 final audit: CAP-006 reopens non-secret diagnostics under INV-010. Sensitive action dispatch, settings round trips and stored-proxy startup regressions cover synthetic credentials while preserving exact native delivery and persistence. The focused correction is qualified and merged in PR #55: all native platforms and full 47-case Windows/macOS E2E/report pass; lifecycle and pinning evidence above remains valid.

September15 startup correction: DCP-001/CAP-001 is reopened after a retained macOS retry and native reproduction show ERR_ABORTED during an approved same-origin client redirect with an initial resource pending. The correction must wait for the replacement document to finish and stop loading, preserve account identity and deny failure/cancellation/stale/foreign completion. Prior lifecycle evidence is retained; the corrected head needs its own final platform qualification.

M3 final qualification: [PR #56](https://github.com/adamlow-wire/wire-desktop/pull/56) closes the reproduced DCP-001 startup defect with all supported native platforms and 48-case Windows/macOS E2E/report. The complete aggregate and criterion audit is in [m3-acceptance.md](m3-acceptance.md). DCP-022 remains `none` for actual enrolment/renewal despite accepted desktop-boundary fixtures; display sharing, full PiP/OS parity and release qualification remain separately owned.

CAP-003 local candidate (not yet accepted): DCP-008/DCP-009 now use the proposed DEC-011 local chooser/broker and parent-bound PiP. Bridge version2 removes legacy enumeration; direct native remote capture remains denied. Local native synthetic-source allow/deny, selected-video delivery, original/clone Stop, revalidation and failure bounds pass. All15 combined Linux product regressions pass;23 renderer cases include sensitive local WebRTC encode/decode of selected pixels. Final hosted platform and authenticated calling checks remain required. Source lists and thumbnails never reach the account/PiP. Real OS screen-recording and portal/privacy qualification is not inferred from synthetic frames.

September15 cleanup follow-up: [PR #59](https://github.com/adamlow-wire/wire-desktop/pull/59), merged51d04739, closes queued console writes recreating deleted account logs. Separate baseline e4cac57f and drain-removal sensitivity fail; restoration passes23 focused/925 full native cases. Exact root/ancestor/target checks, original write failures and deletion retry behavior remain intact. All native platforms and48-case authenticated E2E/report per platform pass on reviewed7d5c34bb; macOS hasone login retry and Windows has two post-removal/proxy-startup retries. Neither run contains ENOTEMPTY failures. This restores CAP-001 removal confidence without changing the explicit live QA/release limits.

Functional M4 acceptance at 41066868 is mapped in [m4-acceptance.md](m4-acceptance.md). Confidence remains at most medium for these migrated capabilities because visible packaged OS behavior, real capture/hardware and live enterprise QA are separate obligations. The earlier local-candidate statements above are historical checkpoints. No actual E2EI enrolment/renewal confidence is inferred.

### TST-006 follow-up: SSO diagnostics (DCP-003 / INV-010)

Authentication-page console text and native exceptions may carry credentials or one-use callback secrets. CAP-002 now explicitly requires fixed bounded desktop diagnostics, preserving useful failure events without those payloads. Separate baseline `1a1093f5` records nine failing Node-only targets; the local correction and cleanup/external-error sensitivity pass. The authored real Electron failed-load regression and retained native SSO suite await hosted qualification. This does not change provider navigation, backend callback syntax, cookie isolation or live Keycloak/E2EI QA requirements.

### TST-006 follow-up: bounded diagnostics (CAP-001 / INV-010)

Log admission is best effort at explicit resource limits: at most 256 active/queued entries, 1MiB of encoded queued payload and 64KiB per UTF-8 entry including line ending/markers. Oversized text is truncated with a fixed marker; overflow resolves without another file-log error, and the next admitted entry reports a bounded dropped count. Queued paths/messages are copied, rotation collision lookup is finite, and filesystem failure releases reservations while still rejecting the original write. Existing FIFO, cross-file progress, flush-tail snapshots and producer-close-before-exclusive-deletion remain required. Baseline `aaa7e250` and sensitive local tests pass after correction; actual account-console/deletion target `cbf24648` awaits hosted execution. These resource limits do not claim complete log-content redaction or whole-process memory bounds.

### TST-006 follow-up: build diagnostics (PKG-001 / INV-010)

Build configuration and original native errors can contain signing credentials. The CLI's diagnostic contract now requires fixed stage/failure messages and a failed exit status for unsupported platforms. Separate baseline `7eb299f3` distinguishes eight retained compatibility controls from ten failing targets; the local correction and sensitivity pass all 18 cases using inert builders. This does not qualify real wrapper failure propagation, metadata recovery, packaged inputs or signing order, which remain PKG-001/SEC-011 work.

PKG-001 package-input candidate (TST-006 F-004): own application payload now has an explicit runtime allowlist for Windows/macOS/Linux, with required native dependency assets preserved. Real copy-filter tests cover secrets/unrelated inputs, default/nested/space-containing application paths and malformed configuration;179 local cases pass, including retained runtime assets and deny paths. Actual archives and startup remain unqualified for this change; the finding stays open.

PKG-001 actual Linux artifact evidence at `f7c1c359`: preflight35037409000 passes on all three platforms. Read-only inspection of the Linux AppImage verifies its downloaded digest and ASAR (12449 entries;22 required runtime files; forbidden owned inputs absent). New CI content checks and complete artifact retention are still unqualified; see [packaging verification](packaging-verification.md). No signed-release or installer acceptance follows.

CAP-001/DCP-001 September23 account-preload diagnostic candidate: inert baseline `f655d236` passes event forwarding and fails private rejected-Error confidentiality. Scoped `3bfa7434` logs a fixed rejection message while forwarding the same event on the same channel. Raw-Error restoration fails the confidentiality target; fixed focused/adjacent tests, types, scoped lint and production build pass. Native/final-platform and broader diagnostic review remain open. [Evidence](renderer-rejection-confidentiality-review.md#active-account-preload-rejection-follow-up).

PKG-001/ELC-003 September23 composed Linux directory check: scoped root-package exclusion `7280d5d9` retains declared runtime closure and fixes actual ASAR high/critical version matches on combined source `725efb5c`. The verified Linux directory has 7,338 ASAR entries/all 22 required files and 407 installed package instances; its exact inventory matches the approved zero-high/critical scan. This is local unsigned directory evidence, not Windows/macOS installer, native startup or final handoff acceptance. [Artifact evidence](shipped-linux-asar-audit.md#scoped-package-input-correction-and-composed-linux-check).

PKG-001 September23 F-022/F-023 static installer checkpoint: the current verifier requires eight generated About/tray/badge images missed by the former 22-file gate. All three actual internal Linux installers at source `9e058e73` pass 30 required files, desktop-branding/identity and fuse checks with an unchanged approved 407-package inventory. The hosted all-format workflow is authored and locally syntax/static-tested only; native launch, actual installation and final composed platform acceptance remain open. [Exact evidence](shipped-linux-asar-audit.md#f-022f-023-branded-linux-installer-rebuild).
